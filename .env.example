# 이 파일을 .env 로 복사해서 씁니다. .env 는 절대 커밋하지 않습니다 (.gitignore).

# true 면 외부 호출 없이 픽스처로 구동됩니다. 키가 없어도 전 파이프라인이 돕니다.
MOCK_MODE=true

# ① Extractor 용. 비어 있으면 MOCK_MODE 와 무관하게 휴리스틱으로 대체 구동됩니다.
ANTHROPIC_API_KEY=
EXTRACTOR_MODEL=claude-sonnet-5

# ② SafetyKorea Open API.
# 호스트와 인증 방식은 「Open API 인터페이스 설계서 v2.0」 기준입니다.
# 인증키는 제품안전정보센터 수동 승인 대기 중 (핸드오프 §6).
KATS_BASE_URL=
KATS_SERVICE_KEY=
