# 프로젝트 핸드오프 — sourcing-guard

**작성 기준일: 2026-08-31** / 제출 마감 2026-09-20 / 본선 발표 10-07 / 데모데이 10-17

이 문서는 에이전트가 이 프로젝트를 이어받기 위한 단일 진입점이다. 여기 적힌 **결정 사항은 이미 검토를 거친 것이므로 다시 뒤집지 말 것.** 뒤집어야 할 근거가 새로 생기면 §7에 기록하고 사람에게 먼저 알린다.

## 읽는 순서

1. **이 문서** — 맥락, 결정, 그 이유
2. `CLAUDE.md` — 코드 작성 시 불변 규칙 (R1~R7)
3. `01_기획서_안심소싱돋보기.md` — 제출용 사업/기술 기획안
4. `README.md` — 코드 구조와 실행법

---

## 1. 무엇을 만드는가

**서비스명**: 안심 소싱 돋보기 (리포명 `sourcing-guard`)

상품 상세페이지 URL 하나로 **KC 인증번호의 유효성·리콜 이력·유해물질 규제 저촉 가능성**을 정부 원문 링크와 함께 확인하는 이커머스 셀러용 리스크 스크리너.

**절대 하지 않는 것**: 합법/불법 판정. 우리는 판정하지 않고 근거를 붙인 플래그만 제시한다. 이 경계선이 제품 설계 전체를 지배하며, 동시에 사업 모델의 전제다 (§3.2).

---

## 2. 대회 사실관계 (초기 기획안이 틀렸던 부분)

최초 기획안은 "예선 심사위원단(라이너, 업스테이지, 크래프톤)"을 전제했는데 **사실과 다르다.** 확인된 내용:

| 항목 | 사실 |
|---|---|
| 예선 배점 | 원티드랩 **내부 심사위원 80% + 온라인 투표 20%** |
| 예선 기준 | 기획력 · 실현 가능성 · 확장성 · **AI 활용의 적절성** |
| 본선 기준 | 기획력 · 확장성 · 기술력 · **발표 전달력** |
| 본선 심사위원 | 강정구(라이너), 김호민(스파크랩), 김덕중(퍼브), 조정석(크래프톤), 정기수(원티드랩) |
| 크래프톤 | 심사가 아니라 **메인 파트너** |
| 업스테이지 | **명단에 없음** |
| 제출물 | 실제 작동하는 배포 서비스 **링크** + 문제/AI 활용 방식/사용 도구 |
| 상금 | 대상 1,000만원, 총 1,700만원 |

### 여기서 파생된 세 가지 설계 제약

- **온라인 투표 20%** → 셀러가 아닌 일반인이 링크 하나로 30초 안에 가치를 체감해야 한다. B2B 전용 UX만 만들면 20점을 통째로 버린다.
- **AI 활용의 적절성이 평가 항목** → "AI 판정을 금지한다"는 논리만 내세우면 "그럼 AI는 왜 쓰나"에 답을 못 한다. LLM이 아니면 불가능한 지점을 전면에 세워야 한다 (기획서 §4).
- **9/20 제출 후 10/7까지 서비스가 살아 있어야 한다** → 마지막 날 배포하고 손 떼는 계획은 실패한다.

---

## 3. 확정된 설계 결정

### 3.1 판정은 코드가, 추출은 LLM이

LLM 역할은 **추출·분류·설명** 셋뿐이다. 위험도 판정은 `scorer.py`의 결정론 코드가 단독으로 한다. 법령 도메인에서 할루시네이션은 오답이 아니라 손해배상 사유다.

이 원칙은 문서가 아니라 코드로 강제된다.
- `Finding`은 근거 URL 없이 **생성 자체가 실패**한다
- "안전합니다" 같은 단정 문구는 생성자에서 거부된다
- `ProductFacts`는 `extra="forbid"` — 판정 필드가 몰래 추가될 수 없다

### 3.2 초록불은 보증이 아니다 (가장 중요한 결정)

논의 중 "결국 국표원·식약처에 안 걸린다는 보증 아니냐"는 질문이 나왔고, 답은 **아니오**다. 우리는 상세페이지 텍스트를 읽고, 단속은 실물을 수거해 시험한다.

확인 불가능한 것: 인증번호 도용 여부, 실제 유해물질 함유량, 미래 리콜 가능성, 식약처 소관(화장품·식품), 상표권·전파인증·원산지·수입요건.

**이 한계는 결함이 아니라 사업 모델의 전제다.** "안 걸립니다"라고 말하는 순간 사후 리콜 책임이 우리에게 온다. 그러면 OMS(플레이오토·사방넷)가 우리를 탑재할 이유가 사라진다. 그들이 이 기능을 안 만든 이유가 정확히 그 책임 문제다.

→ UI는 초록불에 **점검 범위를 항상 병기**한다. "안전"이라는 단어를 쓰지 않는다.

### 3.3 워치리스트가 유일하게 보증 가능한 것

시점 판단인 스캔으로는 "지금 안전한가"를 보증할 수 없지만, **"나중에 리콜 공표되면 가장 먼저 알린다"는 보증할 수 있다.** 그래서 구독 명분은 스캔 횟수가 아니라 워치리스트다.

**여기서만 오류 비대칭이 뒤집힌다** (CLAUDE.md R6). 스캔은 모르면 UNKNOWN으로 물러서지만, 알림은 놓치는 쪽이 훨씬 비싸다. 그래서 매칭은 관대하게 하되 `MatchStrength`(정확/유사/약한 일치)를 함께 노출한다.

### 3.4 수집은 사용자 브라우저에서

서버에서 1688·쿠팡을 크롤링하지 않는다. ToS 위반이고 봇 차단으로 데모 중 죽는다.

**단, 크롬 확장은 v1에서 뺐다.** 이유는 두 가지다. (1) 투표자가 확장을 설치할 리 없다 — 링크 클릭이 최대치다. (2) 3~4일이 그대로 프론트엔드로 간다. 대신 **"상세페이지 내용을 복사해 붙여넣기"** 텍스트 박스를 쓴다. 사용자가 직접 복사하니 ToS 문제가 원천적으로 없고 엔지니어링 비용이 0이다.

확장은 본선 데모데이용 무기로 남기고 기획서에는 로드맵으로 쓴다.

### 3.5 단일 모델

Claude만 쓴다. DeepSeek-R1을 3번 에이전트로 두는 초기안은 폐기했다. 이유: (1) 20일 안에 프롬프트 품질을 두 벌 관리할 여력이 없다 (2) B2B 납품 시 소싱 데이터의 국외 전송 경로가 늘면 컴플라이언스 검토를 통과하지 못한다.

### 3.6 대체 상품 "링크" 추천 폐기

상품 DB가 없으므로 LLM에게 대체 상품 링크를 물으면 존재하지 않는 URL을 만들어낸다. 링크 대신 **구매 요구조건**(예: PVC → TPE/PP, KC 안전확인 신고번호 보유)과 도매 사이트용 **검색 쿼리**를 생성한다.

---

## 4. 검증된 API 사실 (추측 금지)

출처: 「Open API 인터페이스 설계서 v2.0」(2025-06-30, 국가기술표준원 SafetyKorea). 원문 대조 완료. 상세는 `sourcing_guard/data/kats_field_map.yaml`.

- **인증은 HTTP 헤더 `AuthKey`**. 쿼리 파라미터가 아니고 대소문자를 구분한다.
- **IP 제한**: 결과코드 `4001 Invalid IP`. 서비스 ID가 등록 IP에 묶인다.
- **평문 HTTP**. HTTPS 페이지에서 브라우저가 직접 못 부른다(mixed content). 반드시 백엔드 경유.
- 페이징 파라미터 없음. **다만 "최대 1,000줄"은 실측과 다르다.**

  설계서 p.2 는 "모든 조회목록은 최대 1,000 줄까지만 제공됩니다" 라고 적고 있으나,
  2026-09-01 실측에서 `conditionKey=all&conditionValue=%` 로
  **국내 4,243건(5.42MB, 2.0초)·국외 33,070건(32.84MB, 5.7초)이 단일 응답**으로 왔다.
  상한이 실재하지 않거나 훨씬 높다. **날짜를 잘라 대량 수집하는 설계는 불필요하다.**

  `conditionValue` 는 **접두 부분 매칭**이 된다. 범위 문법(`~` `-` `,`)은 통하지 않는다.

  | conditionValue | 국내 | 국외 |
  |---|---|---|
  | `20260723` (날짜) | 53건 | — |
  | `202607` (월) | 55건 · 79KB · 0.16초 | 239건 · 353KB · 0.23초 |
  | `2026` (연) | 116건 | 2,406건 |
  | `20260101~20260901` | **0건** | — |

  ⚠ `conditionValue` 를 **비우면 4005(Invalid Parameter)** 다. 전량 조회는 `%` 를 넣어야 한다.
  "전체 조회는 값을 비우면 되겠지" 가 통하지 않는다.

  ⚠ `all=%` 는 **설계서에 명시된 사용법이 아니다.** `conditionValue` 는 검색어 자리인데
  와일드카드로 전량을 받는 것이다. 신청서 안내문에 "개발 명세서 포맷을 어길 시 별도
  통보 없이 인증이 취소될 수 있다" 고 되어 있으므로 **초기 적재 1회에만 쓰고 반복
  호출에는 쓰지 않는다.** 일일 동기화는 월 단위 접두 조회로 한다.

  응답 정렬은 스코프마다 다르다 — **국내는 정렬 보장이 없고** 국외는 publishDate
  내림차순이다. 국내 응답의 앞부분만 잘라 보면 안 된다.
- `recallModelName`과 `certNum`은 **콤마로 묶인 목록**. 통짜 비교하면 다중 모델 리콜을 전부 놓친다.
- **`certState`는 존재가 아니라 유효성**이다. 적합 / 안전인증취소 / 개선명령 / 표시 사용금지 2·4개월 / 안전확인신고 효력상실 / 반납 / 청문실시 / 기간만료. **조회됐다고 초록불을 주면 안 된다.**
- 국내/국외 리콜은 같은 필드명이 다른 뜻이다. `accidentCaseDscr`은 국내에서 위해정보, 국외에서 소비자 행동요령.
- `searchPop?certNum=` 팝업은 **인증키 불필요** → 근거 링크(R2)로 사용.

### 유해물질 기준 API는 존재하지 않는다

납 용출 90mg/kg, 총 납 함유량 100mg/kg, 프탈레이트 총합 0.1% 같은 기준치는 「어린이제품 공통안전기준」과 「완구·학용품 안전기준 부속서」에 **HWP/PDF 문서로만** 있다.

**"납 300mg/kg" 은 쓰지 말 것.** 초기 기획안이 반복해서 적었으나 공통안전기준에 없는 값이다. 완구 부속서의 옛 기준치였고 100mg/kg 으로 개정되어 공통안전기준과 통일됐다. 오래된 자료에 300 이 보이면 그건 폐지된 값이다.

→ 이걸 파싱해 구조화한 `hazard_rules.yaml`이 **프로젝트의 유일한 진입장벽**이다. 누구나 KC 조회는 붙일 수 있지만 이 DB는 만들어야만 생긴다.

**만약 유해물질 기준 조회 API를 쓰는 코드를 작성하려 한다면 그것은 환각이다. 멈추고 확인할 것.**

---

## 5. 현재 코드 상태

```
sourcing_guard/
  models.py       스키마. Finding/RecallAlert는 근거 없이 생성 불가
  extractor.py    ① Claude 추출. 키 없으면 휴리스틱 대체 구동
  kats_client.py  ② SafetyKorea 어댑터 (설계서 v2.0 반영 완료)
  verifier.py     ② 결정론 검증 + 규칙 DB 로더
  scorer.py       판정. 순수 함수 (I/O·시각·난수 없음)
  watchlist.py    리콜 워치리스트 매칭
  main.py         FastAPI. /healthz, /api/v1/scan, /api/v1/watch, /watch/sweep
  data/
    kats_field_map.yaml   verified: true (실응답 대조 완료 2026-09-01)
    hazard_rules.yaml     draft 17건, verified 0건  ← 다음 작업 (원문 검수)
scripts/probe_kats_schema.py
tests/  171 passed
```

### 동작 원칙 (테스트로 강제됨)

- GREEN은 인증·리콜 **두 축의 적극적 증거**가 모두 있어야 나온다. 침묵은 증거가 아니다.
- 선언된 커버리지만으로 부족하다. 해당 품목군에 `verified` 룰이 없으면 커버되지 않은 것으로 처리한다. **그래서 지금은 완구도 UNKNOWN이 나온다. 의도된 동작이다.**
- UNKNOWN이면 점수를 0으로 강제한다. "모릅니다" 옆에 안심시키는 숫자를 띄우지 않기 위해서다.
- 안전인증취소·효력상실·표시 사용금지는 RED, 개선명령·청문실시는 AMBER.
- **기간만료·반납은 AMBER다(RED 아님).** 완구 인증의 67%가 기간만료라 RED로 두면
  정상 상품 대부분에 빨간불이 켜진다. 재인증되면 번호 뒤에 문자가 붙고
  (`JU071047-12002` → `JU071047-12002C`) 옛 번호가 만료로 남는 것이 정상이다.
  반납의 실제 사유는 "인증기관이 FITI 시험연구원으로 변경됨" 같은 행정 사유였다.
- **조회 실패는 "조회 결과 없음"과 다르다.** `LOOKUP_FAILED`가 붙고 GREEN이 막힌다.

### 미구현

프론트엔드(0%), 리콜 로컬 동기화 스케줄러(`kats_client.recalls_published_on()`은 준비됨), Extractor 실제 프롬프트와 골든셋, 알림 발송(v1 범위 밖).

워치리스트 영속 저장은 `storage.py`에 SQLite로 구현됨.

---

## 6. 진행 상황과 대기 중인 것

**완료 (8/31)**: 인터페이스 설계서 확보 및 코드 반영, 사용신청서 PDF 작성, **인증키 신청 메일 발송**, 리포 생성.

**완료 (9/1)**: **인증키 수령.** 회신에 **"IP 제한은 없습니다"** 가 명시됐다 — §7에서
배포처를 결정한다던 미해결 항목이 해소됐다. 키는 `.env` 의 `KATS_SERVICE_KEY` 에만
두고 커밋하지 않는다.

**실연동 검증 완료 (9/1)**: 설계서 대조로만 채웠던 필드 매핑이 실응답과 전부
일치했다. 다만 실측이 설계서와 다른 지점이 세 개 나왔고 §4에 반영했다 —
1,000줄 상한, 인증 실패 시 302 리다이렉트, certState 실제 분포.

---

## 7. 미해결 사항

| 항목 | 상태 |
|---|---|
| ~~인증키 발급~~ | **해소 (9/1 수령)** |
| ~~IP 등록 정책~~ | **해소.** 회신에 "IP 제한은 없습니다". 서버리스 배제 사유도 함께 사라졌다 |
| ~~배포처~~ | **해소.** Fly.io 배포 완료 — https://sourcing-guard.fly.dev |
| 규칙 DB 원문 | **미확보.** 「어린이제품 공통안전기준」은 확보(draft 17건). 부속서 6(완구)·11(학용품) 미확보 |
| 프탈레이트 물질 수 | 확보한 원문(2020년판)은 6종. 현행 제2022-0220호를 인용한 2차 자료는 DIBP 포함 7종. `substances_pending` 에 분리해 뒀다 |
| 리콜 로컬 동기화 | 미구현. 초기 적재는 `all=%` 2회, 일일 동기화는 월 단위 접두 조회 (§4) |
| 캐시 기준일 표시 | 미구현. 동기화가 들어가야 표시할 기준일이 생긴다 (§8 인프라 리스크) |

---

## 8. 남은 일정

| 날짜 | |
|---|---|
| 완료 | API 규격 확정, 신청서 발송, 리포 생성 |
| ~9/2 | CI 통과, **목 모드 그대로 공개 URL 확보** |
| 9/3–9/5 | 실연동 + 리콜 로컬 동기화 DB (키 도착 시) |
| 9/6–9/9 | **규칙 DB 검수** — 완구·학용품·아동용 섬유 3개 품목군 |
| 9/10–9/12 | Extractor 프롬프트 + 골든셋 30건 (정확도 ≥85%) |
| 9/13–9/17 | **프론트엔드 5일 통째로** |
| 9/18 | 캐시 프리로드, 데모 샘플 3종 고정, 시연 영상 3분 |
| **9/19** | **제출** (마감 하루 전) |
| 9/20 | 예비일 |
| 9/20–10/7 | 무중단 유지 + 투표 유도 |
| 10/7 → 10/17 | 본선 발표 → 데모데이 |

### 배포를 앞당기는 이유

18일차 배포는 해커톤 실패의 전형이다. 목 모드라도 첫 주에 공개 URL을 확보하고 그 위에 기능을 얹는다. 제출 폼도 이때 열어봐서 요구 항목을 미리 확인한다.

### 스코프 방어선

밀리면 버리는 순서: ① 소명 서류 자동화 → ② 대체 스펙 추천 → ③ 이미지 OCR → ④ 워치리스트 알림 발송.

**KC번호 검증 + 리콜 대조는 어떤 경우에도 남긴다.** 환각이 구조적으로 불가능하고 데모 가치가 가장 크다.

### 인프라 리스크

- **정부 API 트래픽**: 투표 기간에 낯선 사람들이 링크를 눌러댄다. 인증·리콜을 로컬에 일 1회 동기화해 **공개 트래픽이 정부 API를 건드리지 않게** 한다. 화면에 캐시 기준일을 표시한다.
- **LLM 비용**: 페이지 텍스트 해시 캐싱 + 일일 상한. 상한을 넘어도 프리로드된 샘플 3종은 항상 즉시 동작하도록 분리한다.

---

## 9. 데모 설계 (투표 20% 대응)

- 첫 화면에 **실제 상품 URL 3개 버튼**. 클릭 한 번에 RED / AMBER / GREEN이 각각 나온다.
- **"우리 집 물건 검사하기"** — 집에 있는 완구·충전기 KC 번호를 넣어본다. 셀러가 아닌 사람도 자기 일이 된다. 실제 리콜 제품에서 RED가 뜨는 화면이 클라이맥스.
- 캐시 히트 3초 이내. 느린 데모는 투표를 못 받는다.
- **커뮤니티 배포 시 카피**: "1인 셀러용 B2B SaaS"가 아니라 **"우리 집 물건 KC 번호 무료 조회기"**. 기획서상 정체성은 B2B로 두되 공개 진입점 문구만 바꾼다. 같은 서비스인데 투표 모수가 열 배 차이 난다.

---

## 10. 다음 작업

1. `.gitignore` 먼저 (`.env` 커밋 금지). `git status`에 `.env`가 보이면 멈춘다.
2. 로컬 `pytest -q` → 30 passed 확인
3. GitHub Actions 워크플로 추가 → 초록불 확인
4. **고정 IP 호스트에 배포, `/healthz` 응답 확인**
5. 규칙 DB 원문 확보 → `hazard_rules.yaml` 초안 작성 (전부 `status: draft`로)

### 규칙 DB 작업 시 반드시 지킬 것

LLM이 문서에서 뽑은 초안은 `status: draft`로 두고, **사람이 원문과 대조한 뒤에만** `verified`로 승격한다. draft 룰은 스코어링에서 제외된다.

기준치 하나를 잘못 넣으면 셀러에게 틀린 숫자를 보여주게 되고, 그것이 이 서비스에서 가장 비싼 오류다. 초안에는 원문 페이지 번호를 함께 남긴다.
