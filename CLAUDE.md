# CLAUDE.md — 안심 소싱 돋보기 (Safe Sourcing Guard)

이 파일은 이 저장소에서 작업하는 Claude를 위한 상시 지침이다. 아래 규칙은 개별 요청보다 우선한다. 사용자가 "빠르게", "일단 되게" 라고 요청하더라도 §2 불변 규칙은 완화하지 않는다. 완화가 필요하다고 판단되면 코드를 고치지 말고 먼저 그 이유를 사용자에게 말한다.

---

## 1. 이 프로젝트가 하는 일

이커머스 셀러가 소싱하려는 상품 페이지를 읽어, **KC 인증번호의 실재 여부·리콜 이력·유해물질 규제 저촉 가능성**을 정부 원문 링크와 함께 제시한다.

**하지 않는 일**: 합법/불법 판정, 인증 대행, 법률 자문. 우리는 사실과 근거만 제시하고 판단은 사용자가 한다.

법령 도메인이므로 잘못된 자신감은 손해배상 사유다. **틀린 정답보다 정직한 "모름"이 항상 낫다.**

---

## 2. 불변 규칙 (Non-negotiable)

### R1. LLM은 판정하지 않는다

LLM의 역할은 **추출(extraction)**, **분류(classification)**, **설명(explanation)** 세 가지뿐이다.

- ❌ 금지: 프롬프트에 "이 제품이 위험한지 판단해줘", "안전한가?", "위법인가?" 를 넣는 것
- ❌ 금지: LLM 출력 스키마에 `risk_score`, `is_safe`, `verdict`, `is_legal` 같은 판정 필드를 두는 것
- ✅ 위험도 연산은 `sourcing_guard/scorer.py`의 결정론적 파이썬 코드에서만 일어난다

새 기능을 요청받았을 때 "LLM에게 물어보면 되겠다"는 해법이 떠오르면, 그것이 판정인지 추출인지 먼저 구분한다. 판정이면 규칙 엔진에 넣는다.

### R2. 근거 없는 출력은 존재할 수 없다

모든 `Finding`은 `source_url`과 `source_label`을 필수로 갖는다. 비어 있으면 `Finding` 생성자가 `ValueError`를 던진다. 이 검증을 우회하는 코드는 작성하지 않는다. 렌더링 단계에서 한 번 더 거른다.

### R3-b. RED 는 정부 DB 가 문제를 적어둔 경우에만

부재는 증거가 아니다. RED 는 취소·효력상실·표시 사용금지·리콜 목록 일치처럼
**정부 DB 가 적극적으로 말해준 사실**에만 준다.

전안법은 위해도 4단계이고, 가장 낮은 **공급자적합성확인(SCoC)** 대상은
제조·수입자가 스스로 시험해 확인하므로 조회 DB 에 인증번호가 없는 것이 정상이다.
미조회를 RED 로 두면 정상 상품에 반복해서 빨간불이 뜨고, 셀러가 모든 RED 를
무시하게 된다. 그러면 진짜 취소된 인증도 안 보게 되어 애초에 고치려던 문제로
돌아간다.

미조회·인증번호 없음은 AMBER 다. `scorer._HARD_RED` 에 새 kind 를 넣기 전에
"정부 DB 가 문제를 적어뒀는가" 를 먼저 물을 것.

### R3. UNKNOWN을 GREEN으로 반올림하지 않는다

데이터가 없으면 `UNKNOWN`이다. "아마 괜찮을 것"이라는 추정은 코드에도 프롬프트에도 넣지 않는다. 규칙 DB가 커버하지 않는 품목군은 커버하지 않는다고 표시한다.

### R4. 서버에서 상거래 사이트를 크롤링하지 않는다

수집은 **사용자 브라우저에서** 한다. 서버가 상거래 사이트를 긁지 않는다.

⚠ **v1 의 실제 입력 경로는 "상세페이지 내용을 복사해 붙여넣기" 다.** 크롬 확장은
v1 에서 뺐고(핸드오프 §3.4 - 투표자가 확장을 설치할 리 없다) 본선용으로 남겼다.
2026-09-11 에 확인: **확장 코드는 리포에 없다.** 이 절이 "크롬 확장이 DOM 을
읽는다" 로만 적혀 있어 실제 경로를 오독하게 했다 - [4-r] 보고에서 "크롬 확장이
상품명만 보내는 경로다" 라고 두 번 적었는데 확장은 존재하지 않는다.

원칙은 그대로다 — 서버 사이드 Playwright/requests 크롤러를 새로 추가하지 않는다. 공개 페이지 URL 페치는 명시적으로 승인된 화이트리스트 도메인에 한한다.

승인된 도메인은 아래가 전부다. 여기 없는 호스트로 서버가 나가면 안 된다.

| 도메인 | 용도 | 쓰는 곳 |
|---|---|---|
| `safetykorea.kr` | KC 인증 조회, 리콜 공표 (Open API) | `kats_client.py` |
| `emsit.go.kr` | 전파인증 번호 조회 (Open API, 인증키 불필요) | 예정 `rra_client.py` |
| `rra.go.kr` | 전파인증 모델명 검색·부적합 현황 (HTML) | 예정 `rra_client.py` |
| `law.go.kr` | 고시·별표·부속서 원문 (DRF OpenAPI, `OC=test`) | `verifier.py` 근거 URL · `scripts/` 자료 수집 |
| `domeggook.com` | 도매꾹·도매매 상품 상세 (**공개 Open API 전용**) · 썸네일 내려받기 | `domeggook_client.py` · `scripts/build_showcase.py` |
| `openapi.foodsafetykorea.go.kr` | 식약처 회수·판매중지 (I0490) | **`scripts/` 전용** — 배포본은 부르지 않는다 |
| `sgkatsrelay.internal` | 국표원 조회 **경유지** (sin · fly 사설망) | `kats_client.py` — `KATS_BASE_URL` secret 으로 켠다 |

`sgkatsrelay.internal` 하나를 빼면 전부 정부 도메인이며 상거래 사이트가 아니다.
도메인을 추가하려면 이 표에 먼저 적는다.

⚠⚠ **`sgkatsrelay.internal` 은 나가는 곳이 아니라 지나가는 곳이다** (2026-09-20 · P7).

  fly 사설망(6PN) 주소다. 공개 인터넷에 없고, 같은 조직의 우리 앱만 부를 수 있다.
  **경유지가 밖으로 나가는 곳은 `www.safetykorea.kr` 하나**이므로, 서버가 실제로
  닿는 공개 호스트 집합은 이 줄을 더해도 **늘지 않는다.** R4 가 막으려던 것은
  상거래 사이트 무단 크롤링이고 그것과 무관하다.

  왜 생겼나 — **도쿄(nrt)에서만 safetykorea 로 연결이 안 된다.** 같은 이미지·
  같은 명령으로 fly 세 리전에서 쟀다 (각 2회 · 2026-09-20 14:06~14:08 UTC):

      sin  http  404 · 152.99.46.37 · connect 1.247s      붙는다
      sin  https 404 · 152.99.46.37 · connect 0.242s      붙는다
      nrt  http/https  000 · 20초 타임아웃 (curl 28)       안 붙는다
      syd  http/https  000 · 호스트 이름 못 풂 (curl 6)     **다른 종류의 실패**

  ⚠ syd 는 판별에서 뺀다. DNS 가 안 풀린 것은 "국외라 막혔다" 가 아니다.
    잰 범위 — fly 세 리전 · 각 2회 · 한 시점. "국외 전반" 이 아니라 **도쿄만**이다.

  ⚠⚠ **같은 날 두 번 쟀고 syd 가 갈렸다.** 커밋 `3a9767c` 의 회차(23:09 KST ·
    별도 앱 `sg-region-probe`)는 `syd http=200 connect=2.022`, 위 회차
    (23:06~23:08 KST · `sg-netprobe`)는 `syd` 양쪽 다 DNS 실패다. **어느 쪽도
    지우지 않는다** - 한 시점씩 잰 것이고, syd 는 회차를 타는 것으로 읽는 편이
    맞다. sin 의 코드가 200 과 404 로 갈린 것은 **경로가 다르기 때문**이다
    (`/` 는 200 · `/openapi/api` 는 404 - 파라미터가 없어 한국에서도 404 다).
    **두 회차가 일치하는 것은 nrt 다** - 둘 다 connect 0.000000 · 20초 타임아웃.
    판정이 선 자리가 거기다.

  ⚠⚠ **키가 이 호스트를 통과한다.** 국표원 인증은 헤더 `AuthKey` 이고
    (`kats_client.py` `_AUTH_HEADER`), 경유지는 그것을 그대로 넘긴다. 그래서
    경유지는 **키를 저장하지 않고 접근 로그를 끈다** (`deploy/kats-relay/Caddyfile`).

  ⚠ **리전을 옮기지 않았다.** 볼륨은 리전을 못 넘고, 옮기면 워치 항목과 투표자
    지연을 건드린다 (총괄 판정 2026-09-20). 조회만 지나가게 한다.

  ⚠ 되돌리기는 `fly secrets unset KATS_BASE_URL` 한 줄이다. 그러면
    `kats_field_map.yaml` 의 기본값(safetykorea 직결)로 돌아간다 - 그래서
    `safetykorea.kr` 도 표에 그대로 남는다.

⚠⚠ **이 표는 "서버가 나가는 곳" 이다. 브라우저가 받아 오는 곳은 여기 없다.**

  2026-09-20 에 재서 확인했다 - 화면 여덟 장이 전부 `fonts.googleapis.com` ·
  `fonts.gstatic.com` 에서 웹폰트를 받고 있는데 **둘 다 `ALLOWED_HOSTS` 에
  없고**, 표와 코드를 대조하는 `test_the_list_matches_the_r4_table` 은 통과한다.
  둘은 애초에 다른 것이다:

      ALLOWED_HOSTS   서버 프로세스가 `httpx` 로 나가는 곳. `ensure_allowed`
                      가 나가기 **전에** 막는다. 셀러 자료가 실려 나간다
      브라우저 호스트   투표자의 브라우저가 받아 오는 곳. 우리 서버는 안 나간다.
                      실려 나가는 것은 **없다** - 받아 오기만 한다

  그래서 웹폰트·CDN 을 표에 넣지 않는다. 넣으면 `ensure_allowed` 가 서버에서
  그 호스트로 나가는 것을 허용하게 되고, 그건 우리가 원한 적이 없는 일이다.

  ⚠ 대신 **브라우저가 받는 곳도 기록은 남긴다.** 지금은 셋이다:

      fonts.googleapis.com · fonts.gstatic.com   Noto Sans KR (본문)
      cdn.jsdelivr.net                           Pretendard Variable (제목)

  ⚠⚠ `cdn.jsdelivr.net` 는 2026-09-20 에 늘렸다. **표를 먼저 고치고 코드를
    고쳤다** (총괄 지시 순서). 고른 경로와 근거:

        https://cdn.jsdelivr.net/npm/pretendard@1.3.9/dist/web/variable/
          pretendardvariable-dynamic-subset.css

        · **npm 경로**다. `gh` 경로의 같은 CSS 는 폰트를 `../../../packages/…`
          로 가리켜 경로가 저장소 밖으로 나간다 - 둘 다 200 이지만 npm 쪽이
          `./woff2-dynamic-subset/…` 로 곧다 (실측)
        · **variable · dynamic subset** 이다. 정적판 CSS 는 613KB 인데
          가변판은 56KB 이고, 쓰는 글자가 든 조각만 받는다 (실측)
        · 버전을 **박는다**(`@1.3.9`). 안 박으면 최신이 바뀔 때 화면이
          말없이 달라진다 (2026-09-20 확인: 1.3.9 가 최신)

    글꼴 이름은 **`Pretendard Variable`** 이다. `Pretendard` 가 아니다 -
    가변판 `@font-face` 가 그 이름으로 선언한다(실측).

  ⚠ 같은 사실을 `design/README.md:70` 이 이미 적고 있다("Google Fonts 는
    사용자 브라우저가 요청한다. 서버는 나가지 않으므로 R4 와 무관하다").
    **두 곳에 있으므로 한쪽을 고칠 때 나머지를 같이 본다** (미완 §1-aa).

  새 호스트를 브라우저 쪽에 더할 때 물을 것 - **투표 기간에 그 호스트가
  죽으면 화면이 어떻게 보이나.** 폰트는 대체 글꼴로 떨어지므로 견딜 만하고,
  스크립트는 아니다. 스크립트는 자기 서버에 둔다.


`law.go.kr` 은 런타임 조회가 아니다. 고시 원문을 받아 `data/` 로 옮기는 자료
수집 경로이고, `verifier.py` 가 근거 URL 로 표시한다. 기준치·조항 번호를 기억으로
쓰지 않기 위한 통로다 (R5).

`domeggook.com` 은 **공개 Open API 만** 쓴다. HTML 스크래핑은 금지다.

⚠⚠ **`openapi.foodsafetykorea.go.kr` 은 `scripts/` 안에서만 부른다** (2026-09-14
    총괄 판단). 배포본 앱은 이 호스트로 나가지 않는다.

    이유가 둘이다. **키가 URL 경로에 들어가고**(`/api/{키}/I0490/json/1/5`)
    **HTTPS 가 안 된다**(총괄 실측: https → connection reset ×2 · http → 200).
    배포본이 매일 직접 부르면 키가 평문으로, 그것도 fly 로그·예외 메시지에
    URL 째로 남는다.

    우리 리콜 동기화는 이미 "받아서 로컬 사본을 쓴다" 구조이므로 같은 모양으로
    간다 — `scripts/sync_mfds_recalls.py` 가 받아 사본을 만들고 앱은 그 사본만
    읽는다. **fly secret 은 넣지 않는다.**

    ⚠ 다음 사람이 배포본에서 부르려 한다. 그때 이 줄을 읽어라.

    ⚠⚠ **시간 제한의 방향** — 09~19시 **동안 막힌다.** 그 시간대에 열리는
      것이 아니다. 우리 메모가 처음에 거꾸로 적혀 있었고, 실측으로 잡았다
      (2026-09-14 17:15 KST · 실호출 1회):

          HTTP 200 · total_count='0'
          RESULT={'CODE': 'ERROR-503',
                  'MSG': '09시~19시에는 서비스가 제한됩니다. 이용에 참고바랍니다.'}

      부르려면 **19시 이후나 09시 이전**이다.

    ⚠⚠ **HTTP 200 이 성공이 아니다.** 위 응답이 그 본보기다 - 상태 코드만
      보면 "받았다" 가 되고 **빈 목록이 정상처럼 흐른다.** `RESULT.CODE` 를
      보고, 모르는 코드는 성공이 아니다 (R3). `resultCode` 를 안 봐서
      kats·rra 양쪽에서 이미 겪은 자리다 (§6).

    저장은 `sourcing_guard/mfds_pii.py` 의 `write_sanitized` 로만 한다 -
    남길 목록(11개) 밖은 전부 버려진 뒤에 디스크에 닿는다.

⚠ 키가 문자열에 남는 자리는 `sourcing_guard/masking.py` 가 지운다. 새는 자리를
  재고 만들었다(2026-09-14) — `HostNotAllowedError` 의 `str`·`repr`·`.url` 이
  **유일하게 확인된 누출**이었고, httpx 예외는 메시지에 URL 을 담지 않는다.

허용: `https://www.domeggook.com/ssl/api/` (`mode=getItemView`, `aid`=API Key).
      상품번호로 상세 정보를 받는다. 이 API 에는 `detail.safetyCert[].no`
      (인증번호 - **배열이고 없을 수 있다**) · `detail.country` ·
      `detail.manufacturer` · `detail.model` 이 구조화 필드로 들어 있어,
      상세페이지 텍스트를 파싱하지 않아도 된다.

⚠⚠ **썸네일은 받아서 우리가 서빙한다** (2026-09-20 · [P4]). 도매꾹 약관이
  "제공된 이미지를 다운로드 받아 사용하고 있는 이미지호스팅 서비스에 저장하여
  사용하시기 바랍니다" 라고 명시한다 - **hotlink 하지 않는다.**

  호스트는 `cdn1.domeggook.com` 이고 `domeggook.com` 의 하위 도메인이라
  `ensure_allowed` 를 그대로 통과한다(접미사 인정). 새 도메인이 아니지만
  **쓰임이 새로워서** 표의 '용도' 칸에 적었다 - 표는 "서버가 나가는 곳" 이고,
  거기서 무엇을 받아 오는지가 바뀌면 그것도 기록이다.

  ⚠ 원본을 그대로 커밋하지 않는다. 리포가 공개다. `THUMB_MAX_PX`(200px) webp
    로 줄인 것만 넣고, 원본 URL 은 **사본에 남기지 않는다** - 남기면 다음
    사람이 그것을 `<img src>` 에 넣는다 (`showcase_pii` 머리 주석).

금지: 검색 결과·상품 페이지 HTML 파싱. 이 세션에서 실상품 239건 표본을 만들 때
      검색 결과 HTML 을 읽었지만 그것은 **조사용 일회성**이었고 프로덕션 경로가
      아니다. 구분을 여기 적어 두지 않으면 다음 사람이 스크래핑을 프로덕션에
      넣는다.

R4 가 막으려던 것은 상거래 사이트 무단 크롤링이지 제공되는 API 가 아니다.
셀러 자신의 소싱처이고, 공개 API 이며, 키로 접근을 관리한다.

⚠ `safetyCert` 는 **공급사가 입력한 값**이다. 그 자체가 검증된 사실이 아니다 -
  우리는 이 번호를 **입력**으로 받아 SafetyKorea 에 대조하고, 값의 진위는 여전히
  우리가 확인한다. `no` 가 `'-'` 이거나 `useNo='N'` 이면 R3-b 에 따라 부재의
  의미를 등급으로 해석한다.

✅ **확인됨 (2026-09-08)**: 호출 쿼터와 키 종류.

  **쿼터** — 「OPEN API 이용 방법」 원문:

  > 최대접속 허용량은 분당 180회, 하루 15,000회입니다. 이 허용량을 넘어서는
  > 경우에는 접속을 자동차단하여 HTTP Response Code 429 를 반환합니다.

  출처: <https://openapi.domeggook.com/main/guide/start>

  ⚠ 09-04 에 "이 문서에 없다" 고 적은 것은 **상품상세정보 문서**를 본 것이고,
    숫자는 이용 방법 문서에 실재했다. 그 작업로그는 날짜 기록이므로 고치지
    않는다 - 그때 무엇을 못 봤는지가 기록이다.

  **키 종류** — 이 키는 **Open API(상품·카테고리 조회)** 다. 상품등록·구매·
  판매 API 는 Private API 로 별도 신청이고 우리는 필요 없다.

  ⚠ 키가 셀러 계정에 종속인지(셀러가 각자 발급해야 하는지)는 **아직 미확인**
    이다. 발급 화면 문구로 확인할 것 - 종속이면 셀러가 키를 입력해야 하고
    UI 가 늘어난다.

  ⚠ **호출 IP 를 등록받는다.** 등록되지 않은 IP 에서의 호출은 거부된다 -
    401/403 이 나면 IP 변경부터 의심한다. 그래서 배포본에서 부르려면 나가는
    IP 가 고정이어야 하고, 확정 전까지 도매꾹 호출은 `scripts/` 안에서만 한다.

✅ **코드가 강제한다 (2026-09-08).** 목록은 `sourcing_guard/allowed_hosts.py`
의 `ALLOWED_HOSTS` 이고, 어댑터는 나가기 **전에** `ensure_allowed(url)` 을
부른다. 승인되지 않은 호스트면 `HostNotAllowedError` 로 막힌다.

  전에는 "문서에만 있고 코드가 강제하지 않는다 … 어댑터가 둘 이상이 되면
  호스트 검사를 코드로 옮기는 것을 검토한다" 였다. `domeggook_client.py` 가
  생겨 어댑터가 둘이 됐으므로 그 조건이 충족됐다.

  ⚠ 이 표와 `ALLOWED_HOSTS` 는 **같아야 한다.** 어긋나면 검사가 깨진다
    (`tests/test_allowed_hosts.py`). 호스트를 추가할 때는 표에 먼저 적는다.

  ⚠ 하위 도메인은 접미사로 인정한다 - `www.domeggook.com` 은 통과하고
    `evildomeggook.com` 은 막힌다.

⚠⚠ **도매꾹 응답에는 제3자 사업자·개인정보가 들어온다. 리포에 모아 두지 않는다.**

  전자상거래법 표시 의무로 상품 페이지에 공개된 정보이지만, **공개된 것과 우리
  리포에 모아 두는 것은 다르다.** 제출용 저장소이고 한 번 커밋하면 git
  히스토리에서 지우기 어렵다.

  실측 (2026-09-08 · 상세 100건):

      seller.company.cno    사업자등록번호   100/100 필드 · 98 실제값
      seller.company.boss   대표자 실명       98/100
      seller.company.addr   사업장 주소       98/100
      seller.company.phone  전화번호        100/100 필드 · 98 실제값
      seller.id / nick                    100 / 90
      return.addr.address1  반품 주소       100/100
      return.addr.mobile                   83/100
      detail.infoDuty.item[].desc  A/S 전화  17/100
      desc(계) 전화번호                       5/100
      desc(계) 사업자번호                      0/100

  **저장 전에 제거한다.** `sourcing_guard/domeggook_pii.py` 의
  `write_sanitized()` 가 유일한 저장 경로이고, 정제 뒤에도 패턴이 남으면 파일을
  만들지 않고 던진다. 원문 응답은 커밋·보고·로그에 넣지 않는다.

  ⚠ **`detail.safetyCert` 는 마스킹 대상이 아니다.** 인증번호가 전화번호 모양을
    띤다 - 실측 `YU101649-22001` · `HU071406-18008A`. 이 프로젝트에서 가장 중요한
    필드를 마스킹하면 정상 인증이 "미조회" 가 되어 R3-b 를 정면으로 어긴다.

  ⚠ **정정 (2026-09-08).** 같은 날 앞선 기록(커밋 e1ffbcd ·
    `tests/fixtures/도매꾹_수집_2026-09-08/README.md`)이 "desc HTML 안 사업자번호
    84/100" 이라 적었는데 **재현되지 않는다.** 같은 원문을 다시 재면 하이픈 표기
    사업자번호는 desc 안에 0건이고, desc 의 '사업자' 문자열 2건은 도매꾹 UI 라벨
    `사업자회원전용` 이다. 84 가 어느 계산에서 나왔는지 재구성하지 못했다. 대신
    앞선 기록이 **놓친 것**이 있었다 - `return.addr`(반품 주소 100% · 휴대전화
    83%)는 목록에 아예 없었다. 옛 수를 옮겨 적지 않고 다시 잰 수를 쓴다.

### R5. 값을 지어내지 않는다

API 필드명, 엔드포인트 경로, 법령 조항 번호, 기준치 수치를 기억이나 추측으로 코드에 넣지 않는다. 확인되지 않았으면 `TODO(unverified)` 주석과 함께 설정 파일에 두고 사용자에게 확인을 요청한다. **특히 `hazard_rules.yaml`의 기준치와 조항 번호는 반드시 원문 확인 후에만 추가한다.**

### R5-b. 갈리는 것은 고시가 정한다 — 우리 감각이 아니다

R5 가 "코드에 값을 지어내지 않는다" 라면, R5-b 는 **판정의 기준을 어디서 가져오는가** 다.
품목 판정 · 검수 정답 · 등급 · 적용범위 — 화면이 근거 링크를 내미는 모든 것에 적용된다.

**왜.** 화면이 셀러에게 내미는 근거가 고시 원문 링크다. 그런데 검수 정답을 우리
감각으로 매기면 **화면이 선 기준과 숫자가 선 기준이 갈린다.** "77.0% 는 무엇을
기준으로 맞은 것인가" 에 답할 수 있는 문장은 **"고시 표에 적힌 대로"** 하나뿐이다.
"저희가 보기에 맞아 보여서" 는 그 자리에서 무너지고, 무너지면 다섯 숫자 전부가
같이 무너진다.

**세 갈래.**

    ① 고시가 적어 뒀다      그대로 따른다. **상식과 어긋나도 고시가 이긴다.**
    ② 고시를 아직 못 읽었다  **모름이다** (R3). 우리 판단으로 메우지 않는다.
    ③ 고시가 갈라 주지 않는다 우리가 정하되 **정했다는 사실과 근거를 같이 적는다.**
                            나중에 이것이 고시 근거로 읽히면 안 된다.

⚠ **원문은 개정된다.** 근거를 적을 때 품목명만 적지 말고 **행정규칙일련번호 ·
  시행일 · 부속서 번호**를 같이 적는다. 안 적으면 6개월 뒤 어느 판을 본 것인지
  모른다. 오늘도 웹 화면의 `admRulSeq` 가 옛 판이었다(아래).

⚠⚠ **감각을 버리라는 규칙이 아니다. 감각은 어디를 파야 하는지 알려준다.**
  2026-09-14 에 시피님이 "가방은 뭔가 다르다" 고 한 덕에 부속서를 열었고, 거기서
  적용범위 첫 문장이 **"유아용 및 아동용 섬유제품을 제외한 만 14세 이상의"** 라는
  것을 찾았다. 우리 표에서 「유아용 섬유제품」은 안전확인, 「아동용 섬유제품」은
  공급자적합성확인이다 — **등급이 두 단계 다르다.** 아무도 안 보고 있던 자리였다.
  정작 감각이 짚은 축(가방이냐 모기장이냐)은 고시가 같은 칸에 넣어 뒀다.
  **감각은 자리를 찾고, 판정은 고시가 한다.**

**실제 사례 — 2026-09-14 미검수 20줄 검수**

    「안전기준준수대상생활용품의 안전기준」 부속서 1 가정용 섬유제품 [표 1]
      외의류      … 모자, 숄, 머플러, … 스카프, 앞치마, … 벨트류, 멜빵 등
      중의류      … 타올, 장갑, … 헤어밴드, 가발, 귀마개, 토시 등
      기타 제품류  가방, 쿠션류, 방석류, 모기장, 커튼, 수의, 덮개 등
    3.6 "기타 제품류" 라 함은 성인용 섬유제품 중 **직접 착용하지 않는** 제품

    → 가방과 모기장은 **같은 칸**이다. "몸에 드는 것도 의류" 는 우리 감각이었고,
      고시는 드는 것을 착용으로 보지 않는다. 고시를 따른다.
    출처 행정규칙일련번호 2100000263200 · 시행 20260827 · 국가기술표준원

**원문 조회 경로 (두 번 검증 — ⑦-a 전파 별표 · 오늘 섬유 부속서)**

    ① `lawSearch.do` 로 **현행 행정규칙일련번호를 먼저 찾는다.** 검색 결과나 웹
       화면의 `admRulSeq` 는 옛 판일 수 있다 — 오늘 실제로 그랬다
       (2100000135271 → 현행 2100000263200).
    ② `lawService.do` 는 `ID=<행정규칙일련번호>` 다. **행정규칙ID 가 아니다** (⑦-a).
    ③ 별표·부속서는 **제목으로 고른다.** 번호가 겹친다 (⑦-a).
    ④ 호스트가 연결을 끊는다. 3회 · 3초 간격 재시도.
    ⑤ `별표내용` 은 문자열이 아니라 **중첩 리스트**로 올 수 있다. 평탄화하고 쓴다.

### R6. 워치리스트에서는 오류 비대칭이 뒤집힌다

R3(모르면 UNKNOWN)은 **스캔**에 적용된다. **리콜 알림**에는 적용하지 않는다.

스캔에서는 잘못 안심시키는 것이 비싼 오류다. 그러나 알림에서는 **놓친 알림이 우리가 하는 유일한 약속을 깨뜨린다**(기획서 §6.1). 스팸 알림은 셀러의 1분을 쓰지만, 놓친 리콜은 상점을 잃게 한다.

따라서 매칭은 관대하게 하되, 약한 매칭을 조용히 버리지 않고 `MatchStrength`(exact/strong/weak)를 함께 노출한다. 알림 문구는 "리콜입니다"가 아니라 "유사 일치하는 항목이 공표되었습니다, 원문 확인 필요"로 쓴다.

단, 3자 미만 모델명은 우연 충돌이 심하므로 매칭 대상에서 제외한다.

### R7. 추출기는 GPT 한 벌 + **떨어진 것을 화면이 말한다** (2026-09-20 개정)

**개정 이력**: Claude 단일 → Claude·GPT 두 벌(2026-09-08) → **GPT 한 벌(2026-09-20)**

**왜 되돌렸나.** 2026-09-19 에 Anthropic 잔액이 0 이 되어 2순위가 다시 죽었다
(400 `credit balance is too low`). 두 벌을 유지하려면 쓰지 않는 벤더에 계속
과금해야 하고, **죽은 2순위는 안전망이 아니라 문서의 거짓말**이다 — 코드·문서·
제출문이 "두 벌" 이라고 말하는데 실제로는 한 벌인 상태가 §9 가 막는 바로 그
모양이다.

**09-08 의 진짜 실패는 벤더가 죽은 것이 아니라 죽은 것을 화면이 말하지 않은
것이었다.** 그 관측은 살아 있다:

    ScanMeta.extraction_path == "heuristic"  →  index.html 메타 푸터가
    "추출 휴리스틱(LLM 응답 없음)" 이라고 적는다

⚠ **2026-09-20 실측으로 확인했다.** 두 키를 다 비우고 스캔하니 화면 바닥이
  그대로 나왔다 — `추출 휴리스틱(LLM 응답 없음) · 정부 조회 인증 성공 · …`.
  **이 한 줄이 이 개정의 유일한 전제다.** 이 문구가 사라지면 개정을 되돌린다.

**벤더 수는 수단이었고, 지키려던 것은 관측이다.** 그것이 이제 R7 의 본문이다.

⚠ `_call_claude` 코드는 **지우지 않았다.** 프롬프트·후처리가 공유라 유지 비용이
  거의 없다. 되살리려면 `EXTRACTOR_ORDER=gpt,claude` 한 줄 + 잔액이면 된다.

⚠ `/healthz` 는 **순서에 든 벤더만** 키·모델을 낸다. 전에는 `claude_key: true`
  를 냈는데 그때 키는 있고 잔액이 0 이었다 — 쓰지 않는 것의 준비 상태를
  초록으로 적지 않는다.

---

#### 아래는 2026-09-08 개정의 기록이다. 왜 두 벌이었는지의 근거라 지우지 않는다

**그때 개정 전**: "모델은 Claude 단일. 다른 벤더 모델을 추가하지 않는다."

**그때 왜 바꿨나.** 2026-09-08 에 Claude 크레딧이 소진돼 배포본이 매 스캔마다 400 을 받고 휴리스틱으로 떨어졌다. 앱은 죽지 않았지만(설계대로 폴백) `product_name`·`legal_item_name`·`category` 세 필드가 비어, 등급표 조회가 원본 상품명으로 되돌아갔다. **발표 숫자(단건 83.7%)가 화면과 어긋나는 상태**였고, 그걸 알아채는 데 시간이 걸렸다. 가용성이 한 벤더에 묶여 있던 것이 원인이다.

두 벌을 두는 목적은 **가용성**이었다. 답이 서로 달라지길 바란 것이 아니다.
2026-09-20 에 그 가용성이 다시 없어져 한 벌로 돌아왔다(위).

#### 지켜야 할 것 — **한 벌이 된 지금도 그대로다**

- **프롬프트·few-shot·후처리는 공유한다.** 갈라지는 것은 호출 형식뿐이다 (`_call_claude` / `_call_gpt`). 프롬프트가 갈라지면 "발표 숫자가 어느 추출기 기준인가" 를 말할 수 없게 된다.
- **어느 벤더가 뽑았는지 센다.** `ExtractionStats.by_vendor` 다. 출력 모양으로 추론하지 않는다 — 그 클래스 주석이 이미 "11건 중 4건을 오탐했다" 고 적어 뒀고, 2026-09-08 에 같은 실수를 한 번 더 했다.
- **추출기를 바꾸면 235건을 다시 잰다.** 발표 숫자는 추출 결과를 입력으로 하므로, 벤더나 모델을 바꾸는 것은 측정값을 무효화한다. 보고할 때 어느 추출기 기준인지 함께 적는다.
- **R1 은 그대로다.** 어느 벤더든 추출·분류·설명만 한다. 판정은 `scorer.py` 다.

#### 국외 전송 경로 — **다시 하나가 됐다** (2026-09-20)

2026-09-08 에 "새로 생긴 위험" 으로 적어 둔 절이다. 셀러의 소싱 상품 정보가
Anthropic 과 OpenAI 두 곳으로 갈 수 있다는 것이었고, **B2B 납품 심사에서 답해야
하는 항목**이었다.

한 벌로 돌아오면서 **경로가 하나다.** 이건 좋아진 것이고, 납품 심사에서 한 줄로
답할 수 있다.

- 서비스 안에서 나가는 곳은 **OpenAI 한 곳**이다 (`gpt-5.4-mini`)
- 어느 요청이 어디로 갔는지 센다 (`by_vendor`)
- 개인정보는 애초에 보내지 않는다 — 우리가 보내는 것은 상품 상세페이지 텍스트다
- ⚠ **개발 도구로 Claude Code 를 쓰는 것은 별개다.** 런타임과 개발 도구를
  가려서 적는다 - 제출문도 그 구조로 되어 있다

#### 설정

    EXTRACTOR_ORDER=gpt           **코드 기본값** (2026-09-20). 죽으면 휴리스틱으로
                                  내려가고 **화면이 그 사실을 적는다**
                                  (되살리려면 `gpt,claude`)
    GPT_MODEL=gpt-5.4-mini        /v1/models 목록에서 고른 실제 이름 (R5)
    EXTRACTOR_MODEL=claude-sonnet-5

#### ⚠⚠ 기준 추출기는 **GPT(gpt-5.4-mini)** 다 (2026-09-11 결정)

**발표 숫자는 GPT 기준이다.** Claude 는 가용성 대비 2순위다.

    기준선 (GPT · 단건 · 상품명만 · 분모 135)
      ① 검수된 정답  104 (77.0%)      ② 미검수   0
      ③ 상한        104 (77.0%)      애매 4 · 오답 8 · 미매칭 19
      ④ 비대상 부착    0               ⑤ 애매 부착  3

⚠⚠ **이 표는 `sourcing_guard/baseline.py` 를 옮겨 적은 것이다.** 두 곳에
  적힌 같은 판단이므로 `tests/test_claude_md.py` 가 대조한다 - 한쪽만 고치면
  검사가 깨진다 (§6 "같은 판단을 두 곳에 적지 마라").

  ⚠ **지금은 검사로 묶지만 그게 끝이 아니다.** 마커(`<!-- BASELINE:BEGIN -->`)
    사이를 스크립트가 렌더하면 소유자가 하나가 되고 **어긋날 수가 없다** -
    검사는 어긋난 뒤에 알려 줄 뿐이다. 본선 과제로 미완 목록에 있다.

  ⚠ 2026-09-14 에 실제로 갈렸다. ⑦-c 검수로 다섯 숫자가 움직였는데(95→104 ·
    18→0 · 113→104) **이 표만 옛 값을 들고 있었고, 내가 그걸 상시 지침으로
    읽으며 반나절을 일했다.** 그래서 검사를 붙인다.

**왜 바꿨나.** 2026-09-08 에 Claude 잔액이 0 이 되어 fly secret 으로
`EXTRACTOR_ORDER=gpt,claude` 를 걸었다. 그 상태로 계속 운용했고 **화면은 GPT 로
돌고 발표 숫자는 Claude 기준**인 불일치가 이어졌다. 임시 우회를 정상 상태로
받아들이고 기준을 거기에 맞춘다 - 둘이 갈린 채로 두는 것이 더 나쁘다.

⚠ 전에 이 자리에 "장애 우회를 코드 기본값으로 굳히지 않는다. 굳히면 발표 숫자의
  기준 추출기가 조용히 바뀐다" 고 적혀 있었다. 그 걱정은 옳았고, 실제로 그렇게
  됐다. **그래서 조용히가 아니라 명시적으로 바꾼다** - 기본값·문서·기준선 표·
  기획서 숫자를 한 커밋에서 함께 옮긴다.

⚠ fly secret `EXTRACTOR_ORDER` 는 **지웠다.** 기본값이 같으므로 secret 이
  필요 없고, 남겨 두면 "왜 secret 이 있나" 를 다시 묻게 된다.

#### 되돌릴 때 — **235건을 다시 재야 한다**

기준을 Claude 로 되돌리는 것은 발표 숫자를 무효화하는 변경이다:

    1. `EXTRACTOR_ORDER` 기본값을 `claude,gpt` 로
    2. **새표본235 전수를 다시 잰다** (LLM 235회). 재생이 아니라 실측이다 -
       추출 결과 자체가 달라지므로 원자료가 새로 필요하다
    3. 기준선 표(`audit_tally.BASELINE`)·기획서·제출문의 다섯 숫자를 함께 옮긴다
    4. `[기준선 변경]` 커밋 규칙대로 다섯 숫자 전/후를 적는다

⚠ `tests/fixtures/단건경로_claude_235.json` 은 **대조군으로 남긴다.** 지우지
  않는다 - "추출기를 바꿔도 등급 결과가 크게 안 흔들린다"(검수 뒤 105 vs 104 ·
  1건 차이)가 우리 주장의 증거다.
  ⚠ 전에 "상한 둘 다 113" 이라고 적었다. 그 113 은 미검수를 다 맞았다고 친
    수였고, 검수해 보니 둘 다 내려갔다.

#### 이미지 입력 — **GPT 경로 검증했다** (2026-09-08)

`kc_numbers_from_image` 경로가 이미지를 LLM 에 넘긴다. Anthropic 형식(`{"type":"image","source":{...}}`)을 OpenAI 형식(`{"type":"image_url","image_url":{"url":"data:..."}}`)으로 옮기는 것이 `_openai_content` 다.

**실제로 쳐 본 입력**: 760×300 PNG(base64 31,340자)에 상세페이지에 흔한 모양으로 다섯 줄을 그렸다 — "블록 완구 세트 100pcs" / "안전확인신고번호 : CB061R2170-3018" / "품명 : 합성수지제 조립블록" / "사용연령 : 3세 이상" / "제조국 : 중국". 텍스트는 `(이미지만 있는 상세페이지)` 만 넣었다.

받은 답:

    kc_numbers_from_image  ["CB061R2170-3018"]   ← 이미지에서 읽었다
    kc_numbers             []                     ← 텍스트에는 없으므로 비었다
    product_name           "블록 완구 세트 100pcs"
    target_age             "3세 이상"
    legal_item_name        "완구"
    category               children_toy

두 필드가 **올바르게 분리**됐다. 이미지에서 읽은 번호를 `kc_numbers` 에 섞으면 0/O 오독이 정상 인증을 "미조회" 로 만든다 — extractor 주석이 적어 둔 그 이유다.

⚠ 검증한 것은 **한 장·PNG·한국어 표 모양** 하나다. 여러 장·JPEG·저해상도 스캔은 안 쳐 봤다.

---

## 3. 아키텍처

```
셀러가 상세페이지를 복사해 붙여넣기  (v1 · 크롬 확장은 본선용)
   → POST /api/v1/scan
   → ① extractor.py   Claude · 구조화 추출 → ProductFacts
   → ② verifier.py    결정론 · KATS API + 규칙 DB → Finding[]
   → ③ reporter.py    Claude · Finding[]만 입력 → 사용자용 문장
   → ScanResult
```

**③ Reporter의 제약**: 입력은 `Finding[]`뿐이다. 새로운 사실을 만들 수 없다. 프롬프트에 "주어진 findings 밖의 정보를 추가하지 말 것"을 명시하고, 후처리에서 findings에 없는 인증번호/법령명이 출력에 등장하면 경고 로그를 남긴다.

### 디렉터리

```
sourcing_guard/
  models.py       Pydantic 스키마 (ProductFacts, Finding, ScanResult)
  extractor.py    ① Claude 추출
  kats_client.py  국표원 API 어댑터 (필드 매핑은 설정 주입)
  rules.py        규칙 DB 로더 + 매칭
  verifier.py     ② 검증 오케스트레이션
  scorer.py       위험도 연산 (순수 함수, 부작용 없음)
  reporter.py     ③ Claude 설명
  config.py       환경설정
  data/
    kats_field_map.yaml   API 응답 필드 매핑 (probe 결과로 채움)
    hazard_rules.yaml     자체 구축 규칙 DB
scripts/probe_kats_schema.py   API 스키마 탐침
tests/
```

---

## 4. 데이터 소스

| 소스 | 상태 | 비고 |
|---|---|---|
| SafetyKorea Open API | 사용 | 설계서 v2.0(2025-06-30) 대조 완료. `kats_field_map.yaml` 참조 |
| 유해물질 기준치 | **API 없음** | 고시·부속서(HWP/PDF)를 파싱해 `hazard_rules.yaml`로 자체 구축 |
| 제품안전정보센터 리콜 공표 | 사용 | 신규 리콜 갱신 |

**중요**: 유해물질 기준 조회 API는 존재하지 않는다. 만약 그런 API를 쓰는 코드를 작성하려 한다면 그것은 환각이다. 멈추고 사용자에게 확인한다.

---

## 5. 규칙 DB (`hazard_rules.yaml`) 작성 규정

한 룰의 필수 필드: `id`, `substance`, `aliases`, `applies_to`(품목군), `limit_value`, `unit`, `legal_basis`, `source_url`, `verified_by`, `verified_at`.

- 원문에서 확인한 것만 넣는다. LLM이 파싱한 초안은 `status: draft`로 두고, 사람 검수 후 `status: verified`로 승격한다.
- `status: draft`인 룰은 프로덕션 스코어링에서 제외된다.
- 기준치는 원문 표기 그대로 적는다. 단위 환산은 코드에서 한다.

### 별칭과 룰의 원칙이 다르다 — 표본 0건의 의미가 반대다

| | 하는 일 | 틀리면 | 표본 0건이면 |
|---|---|---|---|
| **별칭** | 매칭을 **넓힌다** | **오답이 생긴다** — 없는 의무를 만든다 | **넣지 않는다** |
| **룰** | 이미 매칭된 품목의 **기준을 말한다** | 매칭이 맞으면 룰도 맞는다 | **넣을 수 있다** — 검수 순서만 뒤로 |

별칭은 "이 상품이 그 품목인가" 를 넓히는 것이라 표본으로 재지 않으면 오답을
못 본다. 그래서 크레파스 원칙이 적용된다 — 실측에서 걸린 것만 넣는다.

룰은 "그 품목에 어떤 기준이 걸리는가" 를 말하는 것이고, 품목이 이미 표에
있으면 그 기준은 표본과 무관하게 참이다. 2026-09-07 에 부속서 15(킥보드)를
표본 본체 0건인데 넣은 이유가 이것이다 — 표에 '킥보드' 품목이 있고 우리가
이미 등급을 말하고 있으므로, 실제 킥보드가 오면 그 등급 위에 요건이 붙는다.

⚠ **이 구분을 안 적으면 다음 사람이 크레파스 원칙을 룰에도 적용해서 수집을
  멈춘다.** 룰의 표본 0건은 "넣지 마라" 가 아니라 "검수 순위가 낮다" 다.

### 커버리지 단위는 **품목**이다 (2026-09-07 변경)

`RuleBook.covers(facts)` 는 **이 상품에 적용되는 verified 룰이 있는가** 를
본다. 예전에는 `coverage.categories` 선언과 품목군 룰 존재를 함께 봤는데,
그러면 두 방향으로 틀렸다.

| 선언 | 결과 |
|---|---|
| 없으면 | verified 룰이 있어도 안 쓰인다. `household` 의 안전모·레이저·속눈썹기 룰 4건이 그렇게 죽어 있었다 |
| 있으면 | 룰 없는 품목이 "커버된다" 가 된다. 우산에 걸리는 룰이 없는데 초록불로 갈 수 있다 — **R3 위반** |

단위를 좁히는 것 말고 출구가 없었다. 지금은 갈래가 둘이다.

- `rule_type: substance` — 공통안전기준이다. 스스로 "만 13세 이하 어린이가
  사용하는 물품과 그 부분품·부속품에 공통 적용" 이라 적으므로 **품목군 전체**에
  걸린다. 품목을 가리지 않는다.
- `rule_type: requirement` — 품목별 부속서다. **그 품목이어야** 적용된다.
  승차용 안전모 부속서 52 가 우산에 걸릴 이유가 없다. `aliases` 로 가린다.

실측 결과:

```
승차용 안전모 오토바이 헬멧   covers=True   룰 1건
속눈썹 열 성형기 뷰러        covers=True   룰 2건
우산 양산 자동우산          covers=False  룰 0건   ← 회색이고 그게 맞다
블록 완구                  covers=True   룰 14건 (공통안전기준)
전기주전자                 covers=False  룰 0건   ← electrical 은 verified 0
```

⚠ `coverage.categories` 는 **서술용으로만 남겼다.** 게이트에 쓰지 않는다.
  실제와 어긋나면 검사가 깨지므로 룰을 승격할 때 함께 갱신할 것.

⚠ **GREEN 조건은 건드리지 않았다.** 초록불은 인증·리콜 두 축의 적극적 증거로만
  나오고, 커버리지는 유해물질 축의 수록 여부만 답한다.

### 룰 갈래를 `rule_type` 으로 나눈다

`substance` 필드에 "승차용 안전모 안전요건"·"속눈썹 열 성형기 정격전압" 같은
비물질이 섞여 있었다. 화면 문구가 달라야 한다.

| rule_type | 필드 | 화면 문구 | 셀러가 할 일 |
|---|---|---|---|
| `substance` | 물질 · 기준치 · 단위 | "납 100mg/kg 기준이 적용됩니다" | **초과 가능성** — 시험성적서를 봐야 안다 |
| `requirement` | 요건명 · 기준치 · 단위 · 확인방법 | "정격전압 24V 이하여야 합니다" | **충족 여부** — 규격표로 확인된다 |

⚠ `requirement_type`(substance / performance)과 다르다. 저쪽은 "값이 없는 시험
  절차 규격인가" 를 가리고, 이쪽은 "물질인가 요건인가" 를 가린다. 속눈썹 룰
  2건은 `rule_type: requirement` 이면서 기준치가 있다.

---

## 6. 코딩 규약

- Python 3.11+, 타입 힌트 필수, Pydantic v2로 경계 검증
- 외부 API 호출은 반드시 어댑터 계층을 거친다. 비즈니스 로직에서 직접 `httpx`를 호출하지 않는다
- `scorer.py`는 순수 함수. I/O·랜덤·현재시각 참조 금지 (동일 입력 → 동일 출력이 테스트로 강제됨)
- 시크릿은 `.env`에서만 읽는다. 서비스 키를 코드·커밋·로그에 남기지 않는다
- **`audit_tally.BASELINE` 을 바꾸는 커밋은 메시지 첫 줄에 `[기준선 변경]` 을 붙이고
  다섯 숫자를 전/후로 적는다.** 검수된 정답 · 미검수 · 상한 · 비대상 부착 ·
  **애매 부착** 다섯이다. 2026-09-08 에 기준을 넷만 세웠고, 다섯째(애매 부착)가
  1 → 2 로 움직였는데 보고에서 빠졌다 - 기준을 네 개만 세면 다섯째가 조용히
  움직인다. 기준선을 정당하게 옮기는 것과 조용히 옮기는 것은 다르다.
- **외부 응답을 저장하는 코드는 개인정보 제거 함수를 거치지 않으면 저장할 수 없게 짠다.**
  "수집 뒤 마스킹" 이 아니라 **디스크에 닿기 전에** 제거한다 - 원문이 어딘가 남는
  구조 자체를 없앤다. `.gitignore` 로 막는 것은 "커밋하지 않는다" 일 뿐이다.
  제거 함수는 (1) 무엇을 몇 개 지웠는지 스스로 세서 사이드카에 적고, (2) 정제
  뒤에도 패턴이 남으면 **파일을 만들지 않고 던진다** - 우리 제거 목록은 우리가 본
  표본에서 나온 것이라 새 필드는 목록에 없다. 본보기: `domeggook_pii.py`

  ⚠ **두 모듈의 방향이 반대다. 둘 다 맞다.**

      domeggook_pii   **지울 목록**. 쓰는 필드가 많고 구조가 깊다
      mfds_pii        **남길 목록**. 쓰는 필드가 11개뿐이라 가능하고,
                      그러면 API 가 필드를 늘려도 기본이 "버린다" 가 된다

    지울 목록은 목록에 없는 새 필드가 **남는다.** 쓰는 필드를 셀 수 있으면
    남길 목록 쪽이 강하다 - 새로 만들 때 먼저 이것부터 따져 볼 것.
- **`data/*.yaml` 은 텍스트 편집만 한다. `yaml.safe_dump` 으로 덮어쓰지 않는다 — 주석이 자산이다.**
  `hazard_rules.yaml` 은 스스로 "프로젝트의 핵심 자산" 이라고 적고 있고, 주석 123줄에
  각 룰의 원문 근거와 판단 이력이 들어 있다. 2026-09-07 에 `rule_type` 을 넣으려고
  `safe_dump` 을 썼다가 **주석 113줄을 날렸다.** `git checkout` 으로 되돌리고 줄 단위
  삽입으로 다시 넣었다. 필드를 추가할 때도 파싱해서 다시 쓰지 말고 텍스트로 끼운다.
- **동작 확인은 목 모드로 한다.** `MOCK_MODE=true` 로 돌리고, 실호출은 **측정에서만**
  하고 회차를 기록한다. 2026-09-09 에 `extract_traced` 동작을 보려고 그냥 돌렸더니
  로컬 `.env` 에 키가 있어 **실제 GPT 를 1회 호출**했다. 한 번은 싸지만 습관이 되면
  발표 숫자의 회차를 셀 수 없게 된다 - `by_vendor` 가 확인용 호출과 측정용 호출을
  구분하지 못한다.
- **배경 작업을 재실행하기 전에 이전 프로세스를 먼저 확인하고 죽인다.**
  LLM 을 쓰는 스크립트는 시작할 때부터 진행 상황을 한 줄씩 즉시 흘려보낸다
  (`python -u`, 버퍼링 금지). 2026-09-07 에 첫 실행이 버퍼링돼 진행이 안 보여
  재실행했는데 이전 프로세스를 죽이지 않았다. 둘이 동시에 API 를 때려 요청이
  두 배로 나가고 429 를 맞았고, **27분 동안 몰랐다.** LLM 77회를 헛썼다.
  헛쓴 것보다 오래 몰랐던 것이 문제다.
- **가드를 만들 때 반대 방향도 한 줄로 재라.** 우리가 주석에 적어 둔 방향은
  이미 막혀 있다 — **안 적은 반대 방향이 뚫려 있었던 것이 세 번**이다.

      recall_clear      "부재의 증명은 약하다" 를 적어 뒀는데 실제 결함은
                        "늘 붙어서 지표가 안 움직인다" 였다
      test_commit_refs  "내 PC 에서만 통과하는 검사" 를 지적한 가드가
                        스스로 그렇게 됐다 (NFC/NFD)
      rra resultCode    docstring 이 "400 HTML 을 없음으로 흘리면" 을
                        걱정했는데 실제는 **"있음으로 흘림"** 이었다 —
                        점검 페이지가 "전파인증 확인됨" 이 됐다
      갈림 + 인증부재    주석이 "느슨한 쪽을 골라 감점을 빼면 화면에서 막은
                        '한쪽 단정' 을 신호등에서 하는 셈" 이라며 **느슨한
                        쪽만** 막았다. 실제 결함은 반대였다 — 품목이 갈려
                        인증번호가 **있어야 하는지조차 모르는** 줄에
                        "찾지 못했습니다" 라는 **없는 것이 문제라는 틀**을
                        씌우고 있었다 (판정 4)

  ⚠⚠ **넷이면 규칙이 아니라 이 저장소의 실제 결함 패턴이다.** 네 건 모두
    **주석이 걱정한 방향은 막혀 있었고 안 적은 반대 방향이 뚫려 있었다.**
    주석은 그때 생각한 위험의 기록이지 실제로 잰 것이 아니다 - 걱정을 적었으면
    반대쪽을 한 줄 재고 그 수를 같이 적는다.

  ⚠⚠ **'없는데 있다고 하는' 쪽이 '있는데 없다고 하는' 쪽보다 비싸다.**
    전자는 잘못된 초록불이고, 비대상 부착 0 을 지켜 온 이 제품에서 그것은
    존재 이유를 뒤집는다. 후자는 셀러가 한 번 더 확인하게 만들 뿐이다.

  ⚠ 특히 **성공 판정이 "오류 표시가 없다" 에 기대는 자리**를 의심할 것.
    `resultCode` 가 **없으면** 성공으로 읽던 것이 kats·rra 양쪽에 있었다.
    "우리가 이해하지 못한 응답" 은 성공이 아니다 (R3).

- **가드는 「재려는 그것」을 재는지 먼저 확인한다.** 셋이 흔한 빗나감이다.

  ① **주석을 먼저 걷는다.** 규칙을 설명한 주석이 규칙 대신 잡힌다 — 무엇을 왜
     그렇게 했는지 적으려면 금지어를 인용해야 하고, **잘 쓴 주석일수록 가드를
     조용히 무력화한다.** `tests/srccheck.markup_only` 같은 걷개를 가드를 쓰는
     **그 자리에서** 통과시킨다.

     ⚠⚠ 그리고 **다듬는 도구가 텍스트를 바꾼다.** `code_only` 는 주석을 걷으면서
       토큰 사이에 공백을 넣는다 — `asyncio . sleep ( interval )`. 문자열로 찾으면
       빗나가고, `str.index` 면 **`ValueError` 로 죽는다**(실패가 아니라 오류라
       더 헷갈린다). 다듬은 뒤 **찾을 문자열 하나가 실제로 잡히는지 먼저
       확인한다.** 가드는 FAILED 로 죽어야지 예외로 죽으면 안 된다.

  ② **사용자가 보는 자리에서 잰다.** 내부 함수가 아니라 **응답**에서, 로컬
     렌더가 아니라 **배포본**에서. 중간이 맞아도 끝이 틀릴 수 있다.

  ③ **조각을 셀 때는 합이 전체와 같은지 함께 단정한다.** 합이 전체를 넘으면
     같은 것을 두 번 센 것이다.

  실제로 걸린 것 (2026-09-21 하루):

      ①  가드가 자기 감사 주석을 규칙으로 봤다     오늘 세 번 · 저장소 **열네 번**
         (`test_landing` 주석이 그 수를 적어 두고 있다)
      ②  `did_note` 를 `_axes()` dict 에서 재고 통과 → **프로덕션에서 None**
         이었다. 사이에 Pydantic 모델이 있고 필드가 없어 통째로 버렸다
      ③  진행도 조각이 `2 확인 + 1 주의 + 1 미수록 = 4` 인데 축은 **셋**이었다.
         한 축이 두 조각에 들어갔고, 사양 자체가 처음부터 그랬다

  ⚠⚠ 셋 다 **"반대 방향도 재라"** 로 잡힌다. 가드를 만들면 그 자리에서
    **가드를 한 번 무력화해 보고**, 안 물면 가드가 틀린 것이다.

- ⚠⚠ **로직을 고치면 그 로직으로 미리 만들어 둔 것을 전부 센다.**

  고침은 **코드에 닿고 데이터에는 안 닿는다.** 사본·시드·픽스처·캐시가 옛
  코드의 답을 들고 있으면 고친 코드는 **불리지도 않는다.**

  2026-09-22 에 `_pick_exact`(정확 일치)를 넣었는데 **같은 날 두 번 우회당했다**:

      showcase.json   옛 `rows[0]` 로 기록된 사본이 조회를 아예 안 한다
      cert_seed.json  옛 `rows[0]` 로 만든 시드가 **캐시에서 조회를 끝낸다**

  둘째가 특히 나쁘다 - 코드는 고쳐져 있고 검사도 통과하는데, 실행 경로가
  캐시에서 끊겨 새 코드에 닿지 않는다.

  고친 뒤에 물을 것 - **이 로직의 답을 미리 받아 적어 둔 파일이 무엇인가.**
  `scripts/audit_recorded_certs.py` 가 그 세기의 본보기다(실호출 0회 ·
  「적힌 값 vs 근거」를 파일마다 맞춰 본다).

  ⚠ 세는 도구도 **못 재는 쪽으로 틀린다.** 그 도구를 만들면서 두 번 틀렸다 -
    한 번은 가장 가까운 상위로 덮어 「담긴 것끼리」 비교해 불일치 0 이 나왔고,
    한 번은 형제 노드를 못 이어 근거 URL 108개가 통째로 안 잡혔다. 둘 다
    출력은 **깨끗해 보였다.** 「짝지어진 것」 수를 같이 내게 하고 나서야 보였다.

- **되돌릴 때는 무엇이 같이 바뀌었는지 먼저 센다. 한 파일을 되돌린 것은
  한 작업을 되돌린 것이 아니다.**

  2026-09-22 에 사본 재기록을 되돌리면서 `showcase.json` 만 `git checkout`
  했다. 그 실행은 **썸네일 4장도 지웠다**(`prune_thumbs`). 29장 사본에
  썸네일이 4장 없는 상태가 됐고, 화면이 깨지는 것을 `git status` 에서
  우연히 봤다 - 안 봤으면 그대로 배포됐다.

  되돌리기 전에 물을 것 - **그 실행이 만진 것이 몇 개인가.** 스크립트는
  대개 하나만 쓰지 않는다: 사본 · 사이드카 · 정적 파일 · 부산물 기록.
  `git status` 를 먼저 읽고 되돌릴 목록을 만든다.

  ⚠ 그리고 **적용되지 않은 회차의 부산물은 지운다.** 같은 날
    `RED_제외.md`(뺀 건수 0건)가 남았는데, 사본을 되돌렸으므로 그 기록은
    적용된 적이 없다. 남기면 다음 사람이 적용된 것으로 읽는다.

- **규칙이 커밋 메시지에만 있으면 규칙이 아니다. 일이 일어나는 자리에 둔다.**

  2026-09-21 에 정적 자산 주석의 `⚠` 가 이모지 가드에 걸렸다. 고치면서
  「이 파일 주석은 평문」이라고 **커밋 메시지에 적었다.** 다음 날 같은 자리에
  또 걸렸다 - 커밋 메시지는 그 파일을 여는 사람이 읽지 않는다.

  둘 자리를 고를 때 물을 것 - **이 규칙을 어길 사람이 무엇을 보고 있나.**
  파일을 여는 사람이면 그 파일 머리에, 함수를 고치는 사람이면 그 함수 위에,
  명령을 치는 사람이면 `--help` 에 둔다. 커밋 메시지·보고·작업로그는
  **그때 무엇을 했는지**의 기록이지 다음 사람에게 닿는 자리가 아니다.

  ⚠ 같은 이유로 **우리가 만든 기록의 머리글도 읽는다.** 2026-09-22 에
    「502 가 relay 냐 origin 이냐」를 하루 종일 몰랐는데, 답이 우리가 만든
    실호출 기록 첫 줄의 `"note": "… 직결"` 에 있었다. 기록은 남기는 것으로
    끝이 아니라 **읽는 것까지가 측정**이다.

- **검사의 이름이 주장하는 상태를 스텁이 실제로 만드는지 확인한다.**
  만들지 않으면 그 검사는 통과가 아니라 **침묵**이고, 침묵이 결함을 굳힌다.

  스텁을 한 번 **반대로** 세워 보고(성공을 실패로, 하나를 둘로), 검사 이름이
  여전히 참이면 스텁이 이름을 배신하고 있는 것이다.

  두 번 났다:

      is_empty ⟺ size          표본의 모든 행이 번호로 닿아서 세 정의가 우연히
                               겹쳤다. 셋 중 둘을 뒤집어도 검사가 안 물었다
      a_successful_sync…       스텁이 overseas 를 빈 목록으로 줬다. 그러면 전량
                               적재가 완료로 안 찍혀 오류가 남는다 - **이름은
                               successful 인데 성공이 아니었다.** `any(fetched)`
                               기준으로는 참이라 통과했고, 그 통과가
                               `last_sync_ok_at` 의 반쪽-성공 결함을 지키고 있었다

  ⚠⚠ 이것은 바로 위 「0개를 보는 검사」의 사촌이다. 저쪽은 **잴 대상이 없어서**
    침묵하고, 이쪽은 **잴 상태를 안 만들어서** 침묵한다. 둘 다 초록불을 준다.

- **지표에서 빼는 것으로 끝내지 마라 — 왜 그 값이 그런지 물어라.**
  증상만 치우면 원인은 다른 화면에 남는다.

  `recall_clear` 가 두 번 걸렸다. [E] 에서 "늘 붙어서 지표가 항상 1.0 이다" 로
  걸렸을 때 **지표에서 빼는 것으로 끝냈고, 왜 늘 붙는지를 묻지 않았다.**
  답은 `verifier` 가 대조 가능 여부를 `RecallIndex` 와 **다른 조건으로 판단**
  해서 `product_name` 만 있어도 "대조했고 없었다" 를 말한 것이었다 - 실측
  **233/233 = 100%**(상품명만 경로). 그 질문을 그때 했으면 4-r 이 그때 잡혔고,
  그 사이 화면은 계속 거짓 대조를 말했다.

- **우리가 하는 일의 정확한 이름을 쓴다.** 커밋 메시지·주석·보고 어디에도
  "취약점 · 우회 · 침투 · 공격 · 익스플로잇 · 퍼징" 같은 말을 쓰지 않는다.

      오류 주입 시험      →  "잘못된 입력에 대한 동작 확인" · "경계 시험"
      개인정보 스크러빙   →  "저장 전 마스킹"
      화이트리스트 우회   →  "허용 호스트 검사"
      차단 탈출구        →  "실측용 예외(SG_LIVE_NET)"

  ⚠ 분류기 때문만이 아니라 **그 편이 실제로 정확하기 때문**이다 - 우리는 공격을
    시험하는 것이 아니라 **남의 장애가 우리 화면에 닿지 않는지**를 본다. [4-q] 가
    한 일이 정확히 그것이고, 그때 찾은 것도 "잘못된 초록불" 이지 취약점이 아니다.

  ⚠ 2026-09-11 에 구현 에이전트 모델을 Fable 5.1 로 바꿨다가 안전 분류기 오탐으로
    되돌렸다(작업로그 2026-09-12 §0). 우리 작업 - 경계 시험 · 개인정보 마스킹 ·
    허용 호스트 검사 · 키 관리 · 네트워크 차단 - 이 하필 그 모양과 겹친다.

- **아키텍처·규칙 문서에 아직 없는 것을 쓸 때는 "예정"·"미구현" 을 명시한다.**
  읽는 사람이 코드를 확인하지 않고 그것을 근거로 판단한다.

  세 번 났다:

      도매꾹 client   R4 표에 "예정 `rra_client.py`" 처럼 적혀 있었다
      R4 게이트       검사 주석이 "어댑터가 넷이 되면 추가한다" 인데 이미 셋이었다
      크롬 확장       CLAUDE.md R4·§3 이 "크롬 확장이 DOM 을 읽는다" 로만 적혀
                      있었다. **v1 에서 뺐고 코드가 없다**(핸드오프 §3.4)

  ⚠⚠ 마지막 것은 **총괄과 내가 같은 문서를 읽고 같이 틀렸다.** "크롬 확장이
    상품명만 보내면 리콜 축이 거짓 안심" 이라는 규모 서술을 양쪽이 썼는데
    그 경로는 존재하지 않았다. **R5(값을 지어내지 않는다)는 코드에만 적용되는
    것이 아니다** - 문서의 서술도 확인 없이 근거로 쓰지 않는다.

  실제 상태를 한 줄로 적을 것: `(v1 미구현 · 본선용)` · `(예정)` ·
  `(2026-09-11 확인: 코드 없음)`.

- **검사가 기대값을 현재 출력에 맞춰 쓰면 버그를 고정한다. 기대값은 "무엇이
  옳은가" 에서 와야 한다.**

  `watch_sweep` 의 카운터가 문자열(`"5"`·`"1"`)로 새 나갔는데, 검사가
  `== "1"` 로 **현재 출력을 그대로 적어 두어** 잡지 못했다. 화면이 그걸 받으면
  비교·합산이 조용히 틀린다(`"5" > "10"` 이 참이다).

  검사를 쓸 때 출력을 붙여넣기 전에 한 번 물을 것 - **이 값이 이 타입·이 모양
  이어야 하는 이유가 있나.** 없으면 그 검사는 버그를 지키고 있다.

- **판정 재료로 쓰는 숫자는 "걸린 줄" 이 아니라 "결과가 달라지는 줄" 로 센다.**

  2026-09-12 [판정 3] 에서 미완 §1 이 "여는 쪽으로 가면 도매꾹 14건 + 새표본
  23건이 포괄 규정으로 답을 받는다" 고 적었다. 실제로 결과가 달라지는 줄은
  **새표본 2 · 도매꾹 상세 1** 이었다. 두 가지를 안 뺐다:

      이미 답을 받은 줄   19건 중 13건이 이미 등급을 받는다 - 게이트가
                          `not _graded` 도 요구하므로 열려도 안 바뀐다
      입력이 다른 줄      4건은 LLM 이 정리한 상품명에서 표지어가 떨어진다

  ⚠⚠ **총괄과 구현자가 같이 틀렸다.** 재료가 과대였고 그대로 받아들여졌다.
    이번엔 대가가 0(비대상 오염 0)이라 결론이 같았지만, 대가가 있었다면
    부풀린 이득으로 잘못된 거래를 했을 것이다.

  세기 전에 물을 것 - **이 줄은 지금 무엇을 받고 있나.** 아무것도 못 받는
  줄만이 "새로 답을 받는 줄" 이다.

- ⚠⚠ **소스에서 위치를 문자열로 찾는 코드는 읽든 쓰든 주석을 빼고 본다.**
  오너는 `tests/srccheck.py` 다 (`code_only` · `markup_only`).

  2026-09-20 에 같은 모양이 **하루에 세 번** 났고, 두 번은 읽는 쪽이라 고쳤는데
  **쓰는 쪽을 안 고쳤다**:

      읽기   "금지 문자열" 을 주석이 설명하다가 자기 검사에 걸림
      쓰기   `s.index(":root{")` 가 **머리 주석 안의 `:root{`** 를 먼저 물어
             파일 두 개의 머리를 부쉈다 (`^:root{` 앵커로 고침)

  ⚠⚠ **그리고 검사가 못 잡았다.** `design/tokens.css` 와 `app.css` 를 대조하는
    검사가 있는데 **둘이 똑같이 부서지면 통과한다.** 파일을 제자리에서 고쳐
    쓰는 스크립트는 쓴 뒤에 그 파일이 아직 성한지 **스스로 단정**해야 한다 —
    머리 주석이 남아 있는가 · `:root` 가 하나인가 · 변수 개수가 줄지 않았는가.
    그 한 줄이 이번 건을 막았다.

  ⚠⚠ **커밋 경계가 곧 복구 가능성이다.** 이 저장소에서 두 번째다 - 3안을
    덮어썼을 때 `git checkout` 이 살린 이유가 그것만 커밋돼 있었기 때문이고,
    이번에도 `tokens.css` 는 커밋돼 있어 살았고 `app.css` 는 아니라 손으로
    조각을 빼냈다. **한 단계를 끝내면 다음을 건드리기 전에 커밋한다.**

- **검사는 실제로 나가는 것만 본다. 규칙을 적은 주석이 그 규칙에 걸린다** —
  저장소에서 열두 번 넘게 났다. 주석 제거의 오너는 `tests/srccheck.py` 다:
  파이썬은 `code_only`, 마크업은 `markup_only`.
  검사 파일마다 정규식을 새로 쓰면 **이 규칙 자체가 여러 벌**이 된다
  (2026-09-20 실측: 파일 열둘이 자기 정규식을 들고 있었다. 총괄 추정은
  다섯이었다 - 세 보고 적는다).

  ⚠ `//` 를 줄 끝까지 지우면 `https://` 가 사라진다. 금지어가 URL 뒤에 숨어
    검사가 조용히 통과한다 — "Claude" 가 "Claude Code" 에 걸린 것과 같다.
    `markup_only` 는 **앞이 공백이거나 줄머리일 때만** 주석으로 본다
    (`(?<![:/])` 만으로는 `http://a//b` 의 경로 `//` 에 걸려 그 뒤가 지워졌다).

- **포함 검사(`x in body`)는 틀렸을 때 실패하는지를 같이 단정한다.**
  "있어야 할 것이 있다" 만 재면 **다른 곳에 우연히 있는 것**에 걸린다.
  2026-09-20: `"Claude" in body` 가 개발도구 언급("Claude Code")에 걸려
  런타임 벤더 주장이 한 번도 안 잠겨 있었다. 기대값은 코드에서 끌어내고
  (`settings.extractor_order`), **없어야 하는 경우도 함께 단정한다.**

- **완전성 검사는 "몇 개를 봤는가" 를 같이 단정한다. 0개를 보고 통과하면
  그건 검사가 아니다.**

  2026-09-20 에 세 번 났다. 셋 다 **실패하지 않는데 잴 대상이 사라졌다**:

      "Claude" in body        개발도구 언급에 걸려 벤더 주장을 한 번도 안 잠갔다
      흰 글자 대비             라이트만 재고 다크는 아무도 안 봤다 (실측 1.72~1.98)
      _kinds_in(표)           상수가 튜플 → 데이터클래스로 바뀌자 **빈 집합**을
                              보고 통과했다. 바뀐 것을 알아채라고 있는 검사다

  ⚠⚠ **가장 위험한 검사는 실패하는 검사가 아니라 아무것도 안 보는 검사다.**
    실패하는 검사는 고쳐진다. 0개를 보는 검사는 초록불을 주면서 우리를
    안심시킨다 - "가드가 있으니 괜찮다" 가 그 자리에서 거짓이 된다.

  세는 것을 같이 적을 것 - `assert len(found) == N` · `assert actual == expected`
  처럼 **대상의 수나 집합**을 단정한다. `assert not bad` 만으로는 부족하다.

  ⚠⚠ **세는 것은 양쪽으로 틀린다. 세기 전에 "무엇을 셀 것인가" 를 먼저 적는다.**

    위 규칙은 **한 방향만** 본다 - 덜 세는 쪽이다. 2026-09-20 에 반대쪽이
    같은 날 났고, 그래서 이 줄이 생겼다:

        덜 세면   잰 범위가 **전수로 읽힌다**
                  총괄이 히어로를 **세 장** 재고 "여덟 장 갈림 해소" 라고
                  적었다. 나머지 네 장은 34px 이었다
        더 세면   대상이 아닌 것이 **결함으로 읽힌다**
                  `loading="lazy"` 로 미뤄진 21장까지 세어 "썸네일 23장이
                  안 뜬다" 로 읽었다. 스크롤하면 29/29 였다

    **센 수와 전체 수를 같이 적는다.** `29/29` 는 되고 `29장 떴다` 는 안 된다.

    ⚠ **선택자로 긁어서 세지 않는다.** 그것은 "그 선택자로 잡히는 것" 을
      센 것이지 "재려던 것" 을 센 것이 아니다. 히어로 제목은 화면마다
      `h1`·`h2` 가 섞여 있어 `.intro h2` 로 긁으면 절반이 안 걸린다 -
      여덟 장이 다 걸린 것은 운이었고, 구조가 다른 화면이 하나만 있었어도
      "히어로 제목 없음" 이라고 적었을 것이다.

      **대상을 하나씩 지목해 목록을 만들고 `assert len(목록) == N` 을 먼저
      단정한 뒤** 값을 잰다. 세는 순서가 거꾸로면 수가 맞아도 우연이다.

- **화면 검사는 틀이 아니라 틀에 붓는 값을 본다.** 정적 파일만 훑는 가드는
  "이 화면이 무슨 말을 하는가" 를 못 본다 — 말은 대개 자료에서 온다.

  2026-09-20 에 「우리가 틀린 것」 화면을 캡처로 보니 셀러에게 이렇게 떠
  있었다:

      … 상품명만으로는 고르지 못한다 (R3). ⚠ 우리가 정한 선이다: …

  `R3`·`R5-b` 는 **이 문서의 규칙 번호**이고 `⚠` 는 우리 주석 기호다.
  그 화면에는 검사가 넷이나 걸려 있었지만 전부 `misses.html`·`misses.js` 를
  읽었고, 정작 그 문장은 `data/misses.json` 의 `why` 였다. 틀은 깨끗했다.

  ⚠⚠ **이모지 가드는 그 기호를 잡도록 이미 짜여 있었다** (`⚠` 는 U+26A0 으로
    구간 안이다). 못 잡은 이유는 규칙이 약해서가 아니라 **그 파일을 안 봐서**
    다 - "가드를 만들 때 반대 방향도 한 줄로 재라" 와 같은 모양이고, 이번 것은
    반대 **방향**이 아니라 반대 **경로**였다.

  ⚠ 그리고 같은 날 `test_frontend` 의 `PAGES` 가 화면 넷만 들고 있었다.
    `batch`·`samples`·`misses` 와 스크립트 셋은 **이모지·단정 표현 가드 밖**
    이었다. 그 목록 바로 위 주석이 "화면을 늘리면서 가드 목록을 안 늘리면 새
    화면만 무방비다" 다 - 적어 놓고 그대로 했다.

  화면을 하나 만들면 **가드 목록 · 자료 칸 둘 다** 늘린다.

- **새 격자를 만들 때 `minmax(N,1fr)` 의 하한이 곧 최소 폭이다.
  `min(N,100%)` 로 감싼다.**

      랜딩 2026-09-18   `1fr`(= `minmax(auto,1fr)`)이 글자의 min-content 를
                        하한으로 삼아 320px 에서 페이지가 19px 밀렸다
      /unknown 09-20    `minmax(320px,1fr)` 의 하한 320 이 좌우 여백과 합쳐
                        320px 화면을 넘겼다 (실측 가로 넘침 True)

  ⚠ 눈으로는 안 보인다 - 1280 에서는 둘 다 멀쩡하다. **폭을 재는 캡처가
    아니면 못 잡는다** (미완 §1-l · §1-v).

- **같은 판단을 두 곳에 적지 마라.** 조건이 갈리면 한쪽만 고쳐도 나머지가
  거짓말을 계속한다. 갈릴 수 있는 판단은 **소유자를 하나 정하고** 나머지는
  그것을 부른다 - `RecallIndex.can_compare()` 가 그 본보기다. 검사로 재발을
  막을 것(호출부에 조건을 다시 적으면 실패하게).

- **히스토리 rewrite · force push · 원격 브랜치·태그 삭제 · 커밋 되돌리기는
  총괄 확인 없이 하지 않는다. 파일을 안 바꾸는 변경이라도 마찬가지다 — 해시가
  우리 검증 좌표다.**

  2026-09-09 에 이 규칙을 어겼다. 기여 그래프를 채우려고 author 이메일만 바꾸는
  rewrite 를 **묻지 않고** 했다. 파일은 한 글자도 안 바뀌었지만(트리 `e533e56`
  동일) **총괄이 검증해 온 해시 전부가 더 이상 `main` 의 조상이 아니다**
  (`git merge-base --is-ancestor` NO). 처리가 아니라 절차가 문제였다 - 되돌릴 수
  없는 공유 히스토리 변경이고, 되돌리는 것조차 또 한 번의 force push 다.

  ⚠ **이 규칙이 지키는 것은 "공유 이력" 이다.** 2026-09-20 에 내가 넓게 읽어,
    푸시 전 로컬 커밋 둘을 합치는 것까지 물었다. 묻는 것 자체는 옳았지만
    범위를 적어 두지 않아 생긴 지연이다 (총괄 판정 §1). 좁혀 적는다:

        ✗ 푸시된 커밋 · 태그 · 브랜치 이력      총괄 확인 필수
        ✓ 푸시 전 로컬 커밋의 squash·amend·reorder   판단으로 한다

    ⚠ 합치기 전에 **최종 트리가 같은지** 확인한다 (`git rev-parse <전>^{tree}`
      와 `HEAD^{tree}`). 같으면 내용이 한 글자도 안 바뀐 것이다.

- **캡처를 근거로 쓸 때는 장수가 아니라 높이를 센다.** 2026-09-20 에 "화면
  21장" 을 보고했는데 그중 **14장이 정확히 900px**, 즉 `full_page` 가 안 걸린
  뷰포트 조각이었다. 랜딩은 폰에서 6,428px 이므로 머리 한 칸만 보고 "폰도
  봤다" 가 될 뻔했다. 같은 수가 여러 장 반복되면 그게 신호다 (미완 §1-v).

  ⚠ 그리고 **장수는 결론이 아니다.** "21장 떴다" 는 셈이고, 결론은 "이상한
    화면이 있었나, 있었으면 어디가" 다. 없었으면 "없었다" 라고 적는다.

- ⚠⚠ **`backup/pre-rewrite-2026-09-09` 브랜치와 같은 이름의 태그는 영구
  보존이다. 지우지 않는다.**

  이 ref 가 rewrite 전 커밋 168개를 reachable 하게 붙들고 있는 **유일한 끈**
  이다. 지우면 `docs/작업로그_*.md` 의 옛 해시 **97건이 죽은 링크**가 되고,
  `docs/해시_대응표_rewrite_2026-09-09.md` 의 옛 해시 열도 조회 불가가 된다
  (대응표는 새 해시만 확인 가능해진다).

      브랜치   backup/pre-rewrite-2026-09-09
      태그     pre-rewrite-2026-09-09
      옛 HEAD  1ebf65f089765c37a9ed6c8d35057dd74f5e95c7

  ⚠ 저장 공간 정리·브랜치 목록 정돈 같은 이유로 지우려는 유혹이 생긴다. 44KB
    수준이고, 잃는 것은 6개월 뒤 "왜 09-04 로그의 해시가 main 에 없나" 에 대한
    답 전부다.

- **2026-09-09 이전 커밋 해시는 대응표를 거친다.** author·committer 이메일만
  바꾸는 rewrite 로 **커밋 해시 168개가 전부 바뀌었다**(파일 내용은 한 글자도
  안 바뀌었다 - 최종 트리 `e533e56` 동일). 살아 있는 참조 71건은 새 해시로
  옮겼고, `docs/작업로그_*.md` 의 97건은 **그 날의 기록이므로 고치지 않았다.**
  작업로그에서 옛 해시를 만나면 `docs/해시_대응표_rewrite_2026-09-09.md` 로
  새 해시를 찾는다. 옛 히스토리는 원격 `backup/pre-rewrite-2026-09-09`
  (옛 HEAD `1ebf65f08`)에 그대로 있다.
  회귀 가드는 `tests/test_commit_refs.py` 다 - **"실재하는가" 로 검사하지 말 것.**
  백업 ref 가 옛 커밋을 붙들고 있어 실재 검사는 전부 통과하며 아무것도 잡지
  못한다(실제로 그렇게 짰다가 검증에서 걸렸다). 대응표의 옛 해시 열과
  **문자열로** 대조한다.
- **결론을 적을 때 잰 범위를 문장 안에 넣는다.** "넘친 곳이 하나다" 가 아니라
  **"재 본 두 폭에서 하나다"**. 범위를 뺀 결론은 다음 사람이 **전수로 읽는다.**

  2026-09-18 에 양쪽에서 **네 번** 걸렸다:

      리졸버 셋 중 둘이 답했는데 "국외에서 안 풀린다" 로 제목을 달았다
      두 폭만 재고 "재서 넘친 곳이 여기 하나였다" 고 썼다
      두 폭만 재고 내비 수정을 끝으로 봤다 (11개 폭으로 넓히니 하나 더 있었다)
      **주입**으로 잰 CSS 값을 "검증했다" 고 말했다 (파일을 고치면 순서 때문에
      다른 값이 나온다 - 실제로 108px 이 나왔다)

  ⚠ 이것은 §6 의 "걱정을 적었으면 반대쪽을 한 줄 재고 그 수를 같이 적는다" 와
    같은 뿌리다. 저쪽은 **안 잰 방향**이고 이쪽은 **잰 범위**다.

- **백틱이 들어가는 커밋·태그 메시지는 파일로 넘긴다.** zsh 가 `` ` `` 를 명령
  치환으로 읽어 그 자리가 **조용히 빈칸이 된다.**

      ✗ git tag -a … -m "… `last_sync_ok_at` 분리 …"
      ✓ git tag -a … -F /tmp/tagmsg.txt

  2026-09-18 프리즈 태그에서 물렸다. 주어가 사라져 "… 분리 · 화면 시각을
  KST 로" 가 됐고, **고치려면 태그를 또 지워야 해서 안 고쳤다.** ref 삭제에
  총괄 확인을 요구하는 것은 그 일을 드물게 만들기 위해서인데, 문장 반쪽
  때문에 두 번째를 쓰면 문턱이 싸다고 가르치는 것이 된다.

- 사용자 노출 문자열은 한국어, 코드·주석은 영어 허용

## 7. 테스트

- `tests/test_scorer.py`의 결정성 테스트는 절대 skip하지 않는다
- 새 규칙 추가 시 해당 룰의 hit/miss 케이스를 함께 추가한다
- 외부 API는 항상 목킹한다. 테스트가 네트워크에 의존하면 안 된다
- Extractor는 골든셋 30건에 대한 필드 단위 정확도로 평가한다

## 8. 20일 제약과 스코프 방어선

일정이 밀릴 때 버리는 순서:

1. 소명 서류 자동화
2. 대체 스펙 추천
3. 이미지 OCR 경로

**KC번호 검증 + 리콜 대조는 어떤 상황에서도 유지한다.** 이 둘은 하드 데이터라 환각이 불가능하고 데모 가치가 가장 크다.

기능 추가 제안을 받으면, 위 우선순위상 무엇을 밀어내는지 먼저 계산해서 알린다.

## 9. 사용자 노출 문구

모든 결과 화면과 내보내기 문서에 고정 표기:

> 본 결과는 공개된 정부 데이터에 기반한 참고 정보이며, 법적 판단이나 안전 인증을 대체하지 않습니다.

"안전합니다", "합법입니다", "판매 가능합니다" 같은 단정 표현은 UI 카피에 쓰지 않는다. "조회되었습니다", "리콜 이력이 없습니다", "확인이 필요합니다"로 쓴다.
