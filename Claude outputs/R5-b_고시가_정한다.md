# [규칙] CLAUDE.md §2 에 R5-b 를 넣는다 — 갈리는 것은 고시가 정한다

총괄 지시입니다. `CLAUDE.md` §2 의 **R5 바로 뒤**에 아래를 넣으세요.
텍스트 삽입만 합니다(§6 — yaml·문서 다시 쓰지 않는다).

---

## 넣을 내용

```markdown
### R5-b. 갈리는 것은 고시가 정한다 — 우리 감각이 아니다

R5 가 "코드에 값을 지어내지 않는다" 라면, R5-b 는 **판정의 기준을 어디서 가져오는가** 다.
품목 판정 · 검수 정답 · 등급 · 적용범위 — 화면이 근거 링크를 내미는 모든 것에 적용된다.

**왜.** 화면이 셀러에게 내미는 근거가 고시 원문 링크다. 그런데 검수 정답을 우리
감각으로 매기면 **화면이 선 기준과 숫자가 선 기준이 갈린다.** "70.4% 는 무엇을
기준으로 맞은 것인가" 에 답할 수 있는 문장은 **"고시 표에 적힌 대로"** 하나뿐이다.
"저희가 보기에 맞아 보여서" 는 그 자리에서 무너지고, 무너지면 다섯 숫자 전부가
같이 무너진다.

**세 갈래.**

    ① 고시가 적어 뒀다      그대로 따른다. **상식과 어긋나도 고시가 이긴다.**
    ② 고시를 아직 못 읽었다  **모름이다** (R3). 우리 판단으로 메우지 않는다.
    ③ 고시가 갈라 주지 않는다 우리가 정하되 **정했다는 사실과 근거를 같이 적는다.**
                            나중에 이것이 고시 근거로 읽히면 안 된다.

⚠ **원문은 개정된다.** 근거를 적을 때 품목명만 적지 말고 **행정규칙일련번호 ·
  시행일 · 부속서 번호**를 같이 적는다. 안 적으면 6개월 뒤 어느 판을 본 것인지
  모른다. 오늘도 웹 화면의 `admRulSeq` 가 옛 판이었다(아래).

⚠⚠ **감각을 버리라는 규칙이 아니다. 감각은 어디를 파야 하는지 알려준다.**
  2026-09-14 에 시피님이 "가방은 뭔가 다르다" 고 한 덕에 부속서를 열었고, 거기서
  적용범위 첫 문장이 **"유아용 및 아동용 섬유제품을 제외한 만 14세 이상의"** 라는
  것을 찾았다. 우리 표에서 「유아용 섬유제품」은 안전확인, 「아동용 섬유제품」은
  공급자적합성확인이다 — **등급이 두 단계 다르다.** 아무도 안 보고 있던 자리였다.
  정작 감각이 짚은 축(가방이냐 모기장이냐)은 고시가 같은 칸에 넣어 뒀다.
  **감각은 자리를 찾고, 판정은 고시가 한다.**

**실제 사례 — 2026-09-14 미검수 20줄 검수**

    「안전기준준수대상생활용품의 안전기준」 부속서 1 가정용 섬유제품 [표 1]
      외의류      … 모자, 숄, 머플러, … 스카프, 앞치마, … 벨트류, 멜빵 등
      중의류      … 타올, 장갑, … 헤어밴드, 가발, 귀마개, 토시 등
      기타 제품류  가방, 쿠션류, 방석류, 모기장, 커튼, 수의, 덮개 등
    3.6 "기타 제품류" 라 함은 성인용 섬유제품 중 **직접 착용하지 않는** 제품

    → 가방과 모기장은 **같은 칸**이다. "몸에 드는 것도 의류" 는 우리 감각이었고,
      고시는 드는 것을 착용으로 보지 않는다. 고시를 따른다.
    출처 행정규칙일련번호 2100000263200 · 시행 20260827 · 국가기술표준원

**원문 조회 경로 (두 번 검증 — ⑦-a 전파 별표 · 오늘 섬유 부속서)**

    ① `lawSearch.do` 로 **현행 행정규칙일련번호를 먼저 찾는다.** 검색 결과나 웹
       화면의 `admRulSeq` 는 옛 판일 수 있다 — 오늘 실제로 그랬다
       (2100000135271 → 현행 2100000263200).
    ② `lawService.do` 는 `ID=<행정규칙일련번호>` 다. **행정규칙ID 가 아니다** (⑦-a).
    ③ 별표·부속서는 **제목으로 고른다.** 번호가 겹친다 (⑦-a).
    ④ 호스트가 연결을 끊는다. 3회 · 3초 간격 재시도.
    ⑤ `별표내용` 은 문자열이 아니라 **중첩 리스트**로 올 수 있다. 평탄화하고 쓴다.
```

---

## 검사

R5-b 가 문서에만 있고 지켜지지 않으면 §6 의 "예정·미구현을 명시한다" 와 같은 자리가
된다. 가벼운 것 하나만:

    tests/test_claude_md.py (없으면 만든다)
      · CLAUDE.md 에 "R5-b" 절이 있는지
      · 그 절에 행정규칙일련번호 2100000263200 이 적혀 있는지
        (근거 없는 규칙이 되지 않게 — 사례가 사라지면 실패)

---

## 이어서 — `[⑦-c] 유아용·아동용 섬유제품 축`

R5-b 를 처음 적용하는 자리입니다. **30분, ⑦-b 앞에.**

    ⓐ 「어린이제품 안전 특별법」 계열 고시에서 **유아용 섬유제품 · 아동용 섬유제품**
       부속서의 **적용범위**를 원문으로 읽는다. 위 조회 경로를 쓴다.
       ⚠ 「안전확인대상생활용품의 안전기준」(일련번호 2100000275290)에는 **없다** —
         확인했다. 74개 부속서에 섬유 항목이 없다. 어린이제품 쪽 고시다.
    ⓑ 다음 여덟 줄이 「의류 이외의 섬유제품」(안전기준준수)인지, 「유아용 섬유제품」
       (안전확인) 또는 「아동용 섬유제품」(공급자적합성확인)인지 **원문으로** 가른다.

           유모차 메쉬 모기장 / 아기 기저귀 가방 백팩 / 아기침대 텐트 모기장
           접이식 기저귀 가방 아기침대 / 싱글 모기장 아기침대 / 모기장 텐트 아기 침대
           원터치모기장 캐노피 침대 아기 / 오랑우탄인형가방 백팩 소풍가방 책가방

    ⓒ 원문이 갈라 주지 않으면 **③번 갈래**다 — 우리가 정하고, 정했다고 적는다.
    ⓓ 판정을 `새표본235_오답.tsv` 와 `[검수했고 정답]` 절로 옮긴다.
    ⓔ **다섯 숫자를 다시 센다.** `[기준선 변경]` 커밋이고 전/후를 적는다.

⚠ 이 여덟 줄이 다 맞으면 정답 112(83.0%), 다 틀리면 104(77.0%) 다. **6%p 가
  여기 걸려 있다.** 내 예측이니 옮겨 적지 말고 **다시 세서 나온 수**를 적을 것.

⚠ 방향이 중요하다. 우리가 지금 안전기준준수(번호 불필요)라고 말하는데 실제가
  안전확인이면, 셀러에게 **"번호 없어도 정상"** 이라고 잘못 안심시키는 것이다 —
  §6 이 "가장 비싼 오류" 라고 이름 붙인 방향이다.

---

## 이미 확정된 판정 (에이전트가 그대로 반영)

    [② 미검수 18줄 중]
      오답  UPF200+ 냉감 UV차단 페이스커버 … → LED마스크
      정답  코털제거기 방수형 전동식 쉐이버 → 전기면도기
      정답  말랑이 스퀴시 … 스트레스볼 → 완구
      정답  여름두건 / 멀티스카프 / 핸드워머 ×2 / 비치타월 / 미술 앞치마 → 의류
            (부속서 1 [표 1] 외의류·중의류에 모자·스카프·장갑·타올·앞치마·토시가 실려 있다)
      정답  핏미업 러닝벨트 힙색 슬링백 벨트백 … → 의류 이외의 섬유제품
            (상품명에 '가방' 이 있고 힙색·슬링백·벨트백이 모두 가방이다. [표 1]
             기타 제품류. '벨트류'(외의류)는 옷을 여미는 허리띠다)
      보류  위 ⓑ 여덟 줄

    [부록 2줄 · 애매]
      오답  넉박스 커피 찌꺼기통 ×2 → 커피메이커
            ⚠ 커피메이커는 **안전인증**(가장 높은 등급)이다. ⑤ 애매 부착 3 중
              둘이 오부착으로 확인됐다. 수는 안 변하지만 매처가 볼 자리다.

---

본 결과는 공개된 정부 데이터에 기반한 참고 정보이며, 법적 판단이나 안전 인증을
대체하지 않습니다.
