# [총괄] 제출 이후 — 남은 것 전부

배포본 `72ce8aa43` 기준. 미완 목록을 열어서 정리했다.

    135건 · 정답 104 (77.0%) · 오답 8 · 갈림 4 · 미매칭 19 · 비대상 부착 0

---

## 0. ⚠⚠ 제일 급한 것 — **인증 조회가 프로덕션에서 100% 실패하고 있다**

```
/healthz  kats  calls 2 · failures 2 · failure_rate 1.0 · last_error network
```

**시드 29건이 그걸 가리고 있다.** 데모와 체험 표본은 멀쩡해 보이는데, **셀러가
시드에 없는 인증번호를 넣으면 전부 "조회 실패"** 다. 우리 대표 기능이 실물에서
안 돈다.

원인은 §1-p — **도쿄(fly nrt)에서 safetykorea.kr 이 안 닿는다.** 한국에서는
29/29 성공했다. fly 에 서울 리전이 없어서 리전 이동으로는 못 푼다.

    ⚠ 이건 "개선" 이 아니라 **지금 고장**이다. 나머지 전부보다 먼저다.
    ⚠ 그리고 사용자에게 안 보인다 — 시드가 정직하게 "조회분" 을 달고 나가므로
      화면은 거짓말을 안 하지만, 셀러는 값을 못 받는다.

### 길 셋 (아직 안 쟀다)

    ① 한국에 나가는 경로를 하나 둔다   작은 프록시(오라클/네이버/카페24 무료
                                    등급)로 국표원 호출만 통과시킨다.
                                    ⚠ R4 표에 그 호스트를 먼저 적는다
    ② 호스팅을 한국으로 옮긴다         전체 이전. 비용·작업 크다
    ③ 리콜처럼 동기화로 바꾼다         인증은 상태가 수시로 바뀌어 동기화 대상이
                                    아니라고 이미 판단했다(kats_client 주석).
                                    되돌리려면 그 판단부터 다시 본다

**①부터 재 봐라.** 프록시 한 대면 끝나는지, 아니면 IP 차단이라 안 되는지는
**한 번 띄워 보면 안다.**

---

## 1. 제품을 바꾸는 것 — **받은 다음이 비어 있다**

지금 흐름이 여기서 끝난다.

    검사 → "공급처에 이걸 요청하세요" → **끝**

셀러가 서류를 받아 오면 할 게 없다. 고리가 안 닫힌다.

    닫으면   신고번호를 입력 → 우리가 다시 조회 → 축이 열린다
             시험성적서의 물질·수치를 넣으면 → 적용 기준과 대조
             "확인됨" 상태를 감시 목록에 저장

⚠ **이게 「확인해 주는 서비스」와 「같이 일하는 서비스」를 가른다.** 값으로는
  이것이 제일 크고, 데모에서 "그래서 다음은?" 에 답이 생긴다.
⚠ 판정 규칙은 안 바뀐다. 입력 경로가 하나 느는 것이다.

---

## 2. 정확도 — 셋이 한 덩어리다

    미매칭 19 (14.1%)   품목을 못 찾아 아무 말도 못 한 것
    오답 8 (5.9%)       다른 품목의 기준을 말한 것 — 제일 비싼 종류
    §1-q 흔들림         같은 입력에 실행마다 다른 줄. 셋의 뿌리다

### 뿌리 하나 — **매처가 LLM 의 답을 가린다** (§1-u)

`verifier.py:2294` 가 문자열 별칭을 **먼저** 돌리고, 하나라도 걸리면
LLM 의 `legal_item_name` 을 **아예 안 본다.**

> 「물놀이용 가방」에서 LLM 이 가방이라고 제대로 읽어도 그 줄은 실행되지 않는다.

**못 구분하는 것이 아니라 구분한 것을 버리고 있다.** 그런데 지금 순서에도
실측 근거가 있다(킥보드 헬멧에서 LLM 의 상위 개념 답이 형제 6개를 붙였다).
그래서 "LLM 먼저" 로 뒤집는 것이 답이라고는 못 한다.

    방향   별칭이 **핵심명사**에 걸릴 때만 쓴다. 같은 파일에 부속품명 장치가
           이미 있고 「물놀이」만 그걸 안 거친다
    비용   매처 변경은 재생으로 **LLM 0회** 재측정된다. 추출 프롬프트를
           건드리면 그때는 235회 + 사람 검수다
    ⚠      바뀐 행은 손으로 검수해야 한다. 안 하면 ② 미검수가 0 이 아니게 된다

### 그리고 §1-q 는 따로 재야 한다

같은 입력 → 실행마다 다른 결과. **235 재추출 1회**면 폭이 나온다.
본선·데모데이 전에 필요하다 — 심사에서 두 번 돌리면 드러난다.

---

## 3. ⚠ 날짜가 박힌 것 — **2026-11-01 운용요령 개정 시행** (§1-h)

개정판이 **이미 공포돼 있다.** 11/1 부터 우리 등급표 596건의 근거가 옛 판이
된다. 그날 이후로는 화면이 **폐지된 조항을 근거로 댄다.**

    할 일   개정 전후 표를 대조 → 바뀐 품목만 골라 갱신 → `[기준선 변경]`
    ⚠      이건 미루면 **틀린 근거를 내보내는 상태**가 된다. 달력에 박아라

---

## 4. 사용성

    ⓐ 여러 상품을 붙이면 뭉개진다 (§1-j)   감지가 없다. 안내만 있고 막지 않는다
                                        → 줄 수·인증번호 개수로 감지해 물어본다
    ⓑ 감시 알림이 없다                    이메일·카카오 "준비 중". 지금은 들어와야
                                        안다 — 감시의 값이 절반이다
    ⓒ .xlsx 업로드                       열 선택 UI 가 먼저다. 우리가 고르면
                                        틀린 열을 조용히 검사한다
    ⓓ 모바일 셋 (§1-l)                   실폭 검사 · nav 두 벌 합치기
    ⓔ 유해물질 요약 줄이 근거 계열을 안 본다 (§1-r)
                                        공통안전기준 17 · 부속서 넷이 섞이는데
                                        전부 "공통안전기준" 으로 부른다
    ⓕ 인증상태 '반납' 의 뜻 (§1-n)        UNKNOWN 으로 두고 있다. 확인하면 정해진다
    ⓖ 마스코트 해상도 (§1-m)              히어로 1.0~1.16x. 디자이너 원본이 오면 교체
    ⓗ 글꼴 (§1-aa)                       규칙은 GOV 를 지목하는데 검사는 문자열
                                        전부를 잡는다. 바꾸려면 셋을 같이

---

## 5. 커버리지 — **"저희가 채워야 합니다" 칸**

`/unknown` 의 넷째 갈래다. 지금 우리가 못 채운 것.

    · 유해물질 기준 미수록 품목 (coverage_gap)
    · 안전기준준수 부속서 1 제품류 전체 목록 (§3 · [AA])
    · 식약처 소관 — 지금은 "다른 부처" 로 넘긴다. 열면 커버리지가 크게 는다
      ⚠ 새 호스트다. R4 표가 먼저
    · 전파인증(RRA) 은 붙어 있으나 얕다

---

## 6. 운영

    · 추출기 2순위 (§1-o)        Anthropic 잔액. 지금 GPT 한 벌이라 그것이
                                죽으면 서비스가 선다
    · 시드가 낡는다              2026-09-19 조회분. 날이 갈수록 "조회분" 날짜가
                                멀어진다. §0 이 풀리면 자동으로 해결
    · `kats.failure_rate` 감시    0.2 넘으면 기록하도록 목록에 있다
    · 배포 중 캐시 소실           재배포마다 CertCache 가 비고 시드로 복구된다

---

## 7. 순서 제안

```
지금 바로   §0 국표원 경로       프록시 한 대 띄워서 닿는지만 재라. 반나절
1순위      §1 받은 다음         제품을 바꾼다. 데모의 마지막 장면이 바뀐다
2순위      §2 매처 + 흔들림      재생은 공짜다. 바뀐 행 검수가 진짜 비용
3순위      §3 11/1 개정         날짜가 박혀 있다. 10월 안에
그다음      §4 사용성 · §5 커버리지
```

⚠ **§0 을 먼저 하는 이유**: 나머지를 다 해도 인증 조회가 안 닿으면 셀러가
  받는 것이 절반이다. 그리고 지금은 **시드가 가려서 우리 눈에도 안 보인다.**

⚠ **§3 은 미루면 안 된다.** 다른 건 "덜 좋은" 상태인데 이건 **틀린 근거를
  내보내는** 상태가 된다. 11/1 은 협상이 안 된다.

---

## 8. 조건이 맞아야 하는 것 / 안 하는 것

    ✗ 서울 리전            fly 에 없다. §0 의 ①②로만 푼다
    ✗ 마스코트 재렌더       디자이너 원본이 있어야 한다
    ✗ 크롬 확장            §4-t 에 "존재하지 않는다" 로 측정해 뒀다.
                          만들려면 처음부터다
    △ 추출 프롬프트 손질     LLM 235회 + **사람이 바뀐 줄 전수 검수**.
                          검수 없이 넣으면 ② 미검수가 0 이 아니게 되고
                          ③ 상한이 ①보다 커진다 — 우리가 제일 아끼는 둘이다

---

본 결과는 공개된 정부 데이터에 기반한 참고 정보이며, 법적 판단이나 안전 인증을
대체하지 않습니다.
