# 에이전트에 붙여넣을 프롬프트 — 2026-09-18 (금) 재개

아래 블록을 그대로 전달하세요.

---

```
재개합니다. 오늘이 코드 프리즈 당일입니다 (9/18 금 · 제출 마감 9/20 일).

## 0. 먼저 읽을 것 — 두 곳

① `docs/작업로그_2026-09-14.md` 1584행 **[금요일 재개 순서]** 절
   ← 네가 중단 전에 직접 써 둔 것이다. **오늘 할 일은 거기 이미 정해져 있다.**
     그 절의 판단(호출 시각은 낮이 맞다는 것까지)을 총괄이 확인했고 그대로 간다.

② `Claude outputs/` 폴더
   ← 판정 문서가 여기 들어온다. 채팅에 없더라도 폴더를 먼저 본다.

## 1. 지금 상태 (총괄 확인)

    origin/main   0a91573
    배포본        98be8ca07 · sync 2026-09-17T14:21Z · 리콜 37,391 · 최신공표 20260914
                  ⚠ main 이 배포본보다 세 커밋 앞서 있다. 전부 화면에 안 닿는다
                    (주소 그물 · 설정 분류 · 실측 · 문서). **9/19 최종 배포에서 맞춘다.**
    다섯 숫자      ① 104 (77.0%) ② 0 ③ 104 (77.0%) ④ 0 ⑤ 3
    검사          1653 PASS

## 2. 오늘 순서

### ① 지금 바로 — 식약처 키 종류 판별 (실호출 1회)

**지금 10:08 KST 다. 차단 구간(09~19) 안이라 판별에 딱 맞는 시각이다.**
작업로그 [금요일 재개 순서] ① 의 명령을 그대로 돌린다.

    성공(INFO-000)  → (가) 정식 키. 이 호출이 곧 수집이니 `--compare` 도 닫힌다
    ERROR-503      → (나) 샘플 키. 19시 이후에 1회 더 불러 `--compare` 를 닫는다

⚠ 어느 쪽이든 **화면에는 안 붙인다.** 문구만 문서에 적는다. ⑦-b-4 는 본선이다.
⚠ `--compare` 결과가 "과거분이 빠지는가" 를 답한다. 빠지면 "매일 갱신" 이 아니라
  **"현재 회수 중인 목록"** 이다. 그 차이를 문구에 반영한다.

### ② 미완 목록에 **§1-j** 추가 — 여러 상품을 한 번에 붙이는 경로

문안은 `Claude outputs/` 의 `미완_1-i_여러상품_입력.md` 에 있다.

⚠⚠ **번호를 §1-i → §1-j 로 바꿔서 넣을 것.** §1-i 는 이미 [⑦-d] 가 쓰고 있다
  (미완 2554행). 문안을 쓸 때 총괄이 그걸 몰랐다.

내용 요지 — 코드로 확인한 것:
    이미지 여러 장      **전부 쓴다** (main.py:179 max_length=4 · extractor.py:439)
    이미지 + 텍스트     **둘 다 본다** (한 user 메시지에 이미지들 + page_text)
    여러 상품          **한 상품으로 뭉갠다. 감지 코드가 없다** ← 구멍
                      A 인증번호 + B 재질이 한 상품의 사실로 합쳐진다

텍스트 삽입만 한다. 코드 변경 0. 검사는 새로 만들지 않는다 (미완은 기록이지 가드가 아니다).

### ③ 프리즈 준비 — **새 기능 없음**

    · 배포본 점검 (캐시 없는 창 · `/watch` 포함)
    · 제출문 최종 대조 — 다섯 숫자 · §3 GPT/Claude 둘 다 · 부정 단언 검사 초록
    · `docs/미완_목록.md` 최종 확인 — 본선 항목이 전부 **근거와 함께** 적혀 있는지
    · 전체 검사 1회

### ④ 태그 `freeze-2026-09-18`

⚠ **이 해시가 9/19 최종 배포와 같아야 한다.** 태그 뒤에 커밋하지 않는다.

## 3. 오늘 하지 않는 것

    ✗ 새 기능 · 새 데이터 축 · 새 안내 줄
    ✗ 매처 수정 (장갑→스마트폰 등) — 판정 경로다
    ✗ ⑦-b-4 RecallIndex 연결 · 수집본을 `data/` 로 옮기기
    ✗ 주소 그물의 알려진 빈틈 셋 — `_WAS_A_GAP` 에 박혀 있고 그게 지금 할 전부다
    ✗ 무엇이 떠오르든 — **본선이다**

오늘 새로 들어올 수 있는 유일한 것은 ①의 **문구**이고, 그것도 화면이 아니라 문서다.

## 4. 보고 규칙

보고 첫 줄에 **무엇을 안 했는지** 먼저 적는다. 무엇을 했는지보다 그게 더 비싼
정보다. 지시 순서를 바꿨으면 바꿨다고 먼저 적는다.
```

---

## 시피님 쪽 (오늘~내일)

    □ 제출 이미지 6장 (16:9) — **지금 떠도 된다.** main 에 화면 변경이 없어
      9/19 최종 배포 뒤와 같은 화면이다 (총괄이 diff 로 확인)
      대표: 위험 데모 결과 카드 (모형완구 기차놀이)
      커버 이미지는 이미 드렸다 (`원티드_커버_…1920x1080.png`)
    □ 제출 폼 마무리 · **제출 버튼** — 임시저장은 제출이 아니다
    □ 회사 맥북 규정 · 추적 중인 개인 파일 18개 (공개 저장소다)
    □ `Claude outputs/투자자_다섯_지적_방어.md` 를 뺄지 — 총괄 권고는 빼는 쪽

⚠ 커버 이미지의 리콜 수는 "3만 7천 건" 으로 뭉뚱그려 뒀다. 실제는 오늘 37,391이고
  매일 바뀌므로 커버는 고치지 않아도 된다.

---

본 결과는 공개된 정부 데이터에 기반한 참고 정보이며, 법적 판단이나 안전 인증을
대체하지 않습니다.
