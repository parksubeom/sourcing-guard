# 에이전트에게 보낼 것 — **그 4줄만으로는 부족합니다**

## 빠진 것 넷

    ① §4 표가 없습니다            "이 표대로" 인데 표가 안 갑니다
    ② 순서가 틀렸습니다            **두 줄기 합치기가 먼저**입니다.
                                   지금 `app.css`·`index.html` 을 건드리면
                                   프로덕션(4a779b6e9)과 충돌이 늘어납니다
    ③ 「확실하게」 판정이 빠졌습니다  4줄에는 "문구만 바꾼다" 뿐이라
                                   **어제의 무능한 화면이 그대로 나옵니다**
    ④ 검사가 두 개만 적혀 있습니다   **30곳 · 10파일**입니다

⚠ 그리고 **0단계가 아직 안 됐습니다.** 방금 확인했습니다 —
  GitHub 에 `prod-4a779b6e9` 가지가 **없습니다**. origin 에는 `main` 과
  `backup/pre-rewrite-2026-09-09` 뿐입니다.

---

---

# 【먼저 보낼 것】 프롬프트 A — 두 줄기 합치기

> 아래를 **윈도우 PC 에서 작업하는 에이전트**에게 그대로 보내십시오.

```
프로덕션(4a779b6e9)이 저장소 어디에도 없다. 네가 확인해라.

  git fetch origin
  git log --oneline origin/main..HEAD
  git cat-file -t 4a779b6e9

⚠ 아직 아무것도 덮지 마라. 먼저 잃지 않게만 만든다:

  git branch prod-4a779b6e9 HEAD
  git push origin prod-4a779b6e9

⚠ `main` 에 push 하지 마라 — 갈렸으므로 거절된다.
⚠ `--force` 는 어떤 형태로도 금지다. cf1012c(② 머리글)를 지운다.

여기까지 하고 **멈춰서 보고해라.** `git log --oneline origin/main..HEAD`
결과를 그대로 붙여라. 몇 개이고 무엇인지 총괄이 본다.

그 다음에 합친다 (승인 뒤):

  git switch main
  git merge prod-4a779b6e9

충돌은 app.css 한 곳에 몰린다. 해결 규칙은 총괄이 이미 정했다:

  .chip (통합본, align-self 포함)         → 라이브.  `.lv2 .chip` 삭제
  .lv2-foot → .site-foot (파이썬 포함)     → 라이브
  .intro h1, .intro h2                    → 라이브
  section+section{--s12}                  → 라이브
  body{word-break:keep-all}               → 라이브 (여덟 장)
  --shadow-1:none (다크) + 카드 표면 14개  → 라이브
  nav.pages a[aria-current] 좌우 패딩      → 저장소(cf1012c)
  .masthead ...::after{width:124px}       → 저장소(cf1012c)

배포 전 확인:
  □ 검사 전부 통과
  □ 다섯 숫자 무변화  분모 135 · ① 104 · ② 0 · ③ 104 · ④ 0 · ⑤ 3
  □ 여덟 장 내비 좌표 가짓수 1
  □ 배포 뒤 /healthz.build.commit 이 **origin/main 에 있는 SHA** 로 찍히는가
    → 없는 SHA 가 찍히면 그 자리에서 멈추고 보고해라. 이번 사고의 재발 방지다
  □ /api/v1/showcase 200 · 29장 · gov_lookup cert/recall ok
```

---

---

# 【합친 뒤에 보낼 것】 프롬프트 B — 상세 결과 화면

> 프롬프트 A 의 배포가 끝난 뒤에 보내십시오.

```
상세 결과 화면을 고친다. **레이아웃은 건드리지 않는다.** 말과 무게만 바꾼다.

■ 어디를 만지는가

  sourcing_guard/scorer.py          축 라벨 · 헤드라인의 **소유자**
  sourcing_guard/models.py:451      ResultAxis docstring
  sourcing_guard/static/index.html  1116~1200 결과 렌더러 (data.axes)

⚠⚠ **문구를 index.html 에서 지어내지 마라.** scorer.py 주석에 이미 적혀
  있다 — "축 이름을 셀러의 말로. **화면이 짓지 않게** 여기서 준다."
  화면은 서버가 준 문자열을 그릴 뿐이다. 새 문구는 전부 파이썬에서 온다.

■ 1단계 — 말 (표대로만 쓴다. 네가 고르지 마라)

  지금                    →  쓸 것
  ─────────────────────────────────────────────────────
  정상                    →  소싱 가능
  인증 조회 · 적합         →  인증 조회 · 조회함
  리콜 여부 · 없음         →  리콜 대조 · 대조함 · 일치 없음
  유해물질 검사 · 적합     →  유해물질 기준 · 기준 수록됨
  검출된 유해물질 없음     →  이 품목에 적용되는 기준 N개
                             함유량은 시험성적서로 확인합니다
  상세 리포트 보기         →  적용 기준 보기
  탭 「유해물질 검사」     →  「유해물질 기준」
  공식 데이터 다운로드     →  이 결과 내려받기
  상품코드 (라벨)          →  KC 인증번호   ※ 번호값은 그대로
  KC 인증 확인됨           →  그대로 둔다
  인증 상세 보기           →  그대로 둔다

  하단 고지는 §9 고정 문구다. **한 글자도 바꾸지 마라**:
    "본 결과는 공개된 정부 데이터에 기반한 참고 정보이며,
     법적 판단이나 안전 인증을 대체하지 않습니다."

■ 2단계 — 확실하게 (이게 핵심이다. 1단계만 하면 화면이 무능해 보인다)

  ⓐ 칩      UNKNOWN 은 「판단 보류」 대신 **「한 가지만 더 받으면 됩니다」**
            (남은 축이 하나일 때. 둘 이상이면 「N가지만 더 받으면 됩니다」)

  ⓑ 진행도  큰 숫자 **3 / 4** + 막대 + "네 가지 중 셋을 확인했습니다"
            ⚠⚠ 이건 **상품 점수가 아니라 우리 진행도**다. 그렇게 읽히게
              써라. "이 상품 3점" 으로 읽히면 실패다.

  ⓒ 초록 상자 「지금까지 확인한 것」 — **여기에 초록을 크게 쓴다**
              리콜 대조    37,430 건과 대조 · 일치 없음
              부적합 공표   2,753 건과 대조 · 일치 없음
              품목 등급    어린이제품 · 물놀이기구 로 판별
              적용 기준    9 개 찾아 둠

  ⓓ 행동    안 한 축은 **회색 물음표로 두지 마라.** 다음 행동 카드 한 장:
            보라 테두리 · 3단계 · 버튼 둘
              「공급처에 보낼 문구 복사하기」
              「번호를 받았어요 · 바로 조회」
            문구는 이미 `확인 항목` 에 있다. 새로 짓지 마라.

  ⚠⚠⚠ ⓒ의 숫자를 **박아 넣지 마라.** 매일 바뀐다 (R5).
    살아 있는 값에서 온다:
      /healthz.sync.recalls.domestic + .overseas
      /healthz.sync.rf_noncompliant.count
    값을 못 읽으면 **그 줄을 빼라.** 0 이나 "-" 를 그리지 마라.

  ⚠ 왜 이게 규칙 위반이 아닌가: 초록이 걸리는 대상이 **상품이 아니라
    우리가 한 일**이다. 차이는 길이가 아니라 **주어**다.
      "37,430 건과 대조 · 일치 없음"  → 주어가 우리.  크게 쓴다
      "이상 없음"                    → 주어가 상품.  안 쓴다

■ 3단계 — docstring 교체 (models.py:451)

  지금 문장이 **금지만** 말해서 화면이 소극적으로 나왔다. 이렇게 바꾼다:

    ⚠ label 은 **우리가 한 행위**를 적는다. "대조함" 이지 "이상 없음" 이 아니다.
    ⚠ ok=True 는 "문제 없음" 이 아니라 "그 축을 우리가 수행했다" 는 뜻이다.
      화면은 이걸 **크고 확실하게** 그린다 — 초록이 걸리는 대상이
      **상품이 아니라 우리가 한 일**이기만 하면 된다.
        "37,430 건과 대조 · 일치 없음"   크게 쓴다  (주어 = 우리)
        "이상 없음"                      안 쓴다    (주어 = 상품)
      차이는 길이가 아니라 **주어**다.
      ⚠ 안 한 축은 회색 물음표로 두지 말고 **다음 행동 하나**로 바꾼다.
        (2026-09-21 총괄 — 금지만 적어 뒀더니 화면이 무능해 보였다.)

■ 4단계 — 검사

  ⚠ 금칙어 검사는 두 개가 아니라 **30곳 · 10파일**이다. 전부 돌려라:
    test_frontend · test_scorer · test_landing · test_landing_why ·
    test_unknown_reasons · test_item_grades · test_srccheck ·
    test_showcase · test_experience_samples · test_jurisdiction_map

  □ 검사 전부 통과 (전과 후 개수를 같이 적어라)
  □ 다섯 숫자 무변화  분모 135 · ① 104 · ② 0 · ③ 104 · ④ 0 · ⑤ 3

■ 5단계 — 네 장 그리고 멈춘다

  **GREEN · AMBER · UNKNOWN · RED 를 같은 레이아웃으로 넉 장** 찍어라.
  1440px · 실제 표본으로. **총괄이 보고 승인한 뒤에** 다음으로 간다.

  ⚠ §6 규칙: 「넉 장 떴다」 가 아니라 목록을 먼저 만들고
    `assert len(목록) == 4` 를 단정한 뒤 찍어라.

■ 그 뒤 (승인 뒤에만) — 탭

  탭은 **URL 에 남는다** (`?tab=cert`). 뒤로가기가 먹어야 하고,
  검사가 탭 안의 문구를 볼 수 있어야 한다.
  탭 이름: 전체 결과 · 인증 조회 · 리콜 대조 · 유해물질 기준 · 상세 정보

■ 하지 말 것

  ✗ 검사 30곳 풀기               제품의 정체성이고 심사 근거다
  ✗ 「정상」·「이상 없음」·「리콜 없음」  짧을 뿐 확실하지 않다
  ✗ 「검출된 유해물질 없음」       우리는 시험한 적이 없다
  ✗ 숫자 박아 넣기                37,430 은 매일 바뀐다 (R5)
  ✗ 3/4 를 상품 점수로 읽히게      그건 우리 진행도다
  ✗ index.html 에서 문구 짓기      소유자는 scorer.py 다
  ✗ 레이아웃 바꾸기                말과 무게만이다
  ✗ 하단 §9 문구 손대기            한 글자도 안 된다
  ✗ 네 장 승인 전에 탭 넣기        한 번에 두 가지를 바꾸면 무엇이 원인인지 모른다
```

---

## 같이 보낼 그림

    확실하게_보여주기.png       ← 2단계가 무엇인지 한눈에. **이게 제일 중요합니다**
    상세결과_말고친안.png        ← 1단계 라벨이 실제로 어떻게 생기는지
