# 정부 조회가 살았습니다 — **여기서 동결합니다**

## 0. 실측 (총괄이 배포본에서 직접 쟀습니다)

    배포본       c2e2de209 · 2026-09-20T14:25:27Z
    kats        calls 8 · failures 2 · failure_rate 0.25
                **last_success_at 2026-09-20T14:28:55Z**   ← null 이 아닙니다
                last_error_code 'http'  ← network.connect 가 아닙니다

    실스캔 (seed 에 있는 번호)
      신호        **GREEN**
      gov_lookup  {"cert": **"ok"**, "recall": "ok"}   ← stale 이 아닙니다
      본문        "…조회되었습니다(인증상태: 적합)"
      stale 표시   **False**    ← "조회분으로 표시합니다" 가 붙지 않습니다

    실스캔 (seed 밖 번호)
      축 카드      "조회 실패" 가 아니라 **"조회함 · done:true"**
      신호        UNKNOWN 이 아니라 **AMBER** (번호가 없는 상품이라 정상)

    리콜 동기화    last_sync_at 14:26:57 · **오류 없음**

**인증 조회가 live 로 돌아왔고, 리콜 동기화도 됩니다.** `failures 2` 는 부팅
직후 릴레이가 아직 안 떴을 때의 흔적이고, 그 뒤는 전부 성공입니다.

**P7 이 답이었습니다.** 리전을 옮기지 않고 조회만 싱가포르를 지나가게 한
것으로 끝났습니다. 볼륨도 워치 6건도 투표자 지연도 건드리지 않았습니다.

## 1. ⚠ 제가 틀린 것 — P2 를 검증 없이 통과시켰습니다

지난 답에서 제가 이렇게 썼습니다:

> "P2 는 검증 통과했습니다. 배포본 문구를 확인했습니다."

**확인하지 않았습니다.** 에이전트 보고의 문구를 그대로 옮겨 적었습니다.
그리고 실제로는 **커밋만 되고 배포가 안 돼 있었습니다.**

에이전트가 잡은 방법이 정확히 제가 했어야 할 일입니다 — `/healthz` 의
빌드 시각이 커밋 뒤라 **라벨만 보면 배포된 것처럼 보였는데**, 내용으로
열어 보니 `/api/v1/unknown-reasons` 가 옛 문구였습니다.

⚠⚠ 제 역할이 "에이전트 보고를 검증한다" 인데, **이번엔 보고를 받아 적었습니다.**
  §6 이 이름 붙인 자리이고(문서를 옮겨 적고 측정이라 부른 것), 이 세션에서
  제가 이미 한 번 했습니다("다섯 벌" 건).

그리고 **축 카드에 「잠시 후」가 남아 있던 것**도 제가 못 봤습니다. 검사가
statement 와 unknown_reasons 만 보고 축 카드 note 를 안 봤는데, 그것도
에이전트가 찾았습니다.

## 2. 판정 — **여기서 동결합니다**

투표가 곧 시작이고, 지금 상태가 좋습니다:

    정부 조회     live (인증 · 리콜 둘 다)
    문구         원인을 말하지 않는 쪽으로 · 「잠시」·「일시적」 잔존 False
    히어로        C안 · 세 화면 굵기 일치
    8화면        전부 200
    미완 showcase  **화면에 안 나갑니다** (`/showcase` · `/api/v1/showcase` 404)
                 자료 파일만 들어갔고 라우트가 없습니다

**더 올리지 않습니다.** 새로 올리는 것마다 지금 확인된 상태를 흔듭니다.

### ⚠ P7 이 P4 의 긴급성을 없앴습니다

showcase(도매꾹 리스트)는 **조회 실패를 우회하려던 것**이었습니다. 조회가
살았으니 급하지 않습니다. **10/5 이후**에 제대로 올립니다 — 개인정보 가드와
화면 검사를 갖춰서.

## 3. 배포 권한 — **하나로 정합니다**

에이전트 지적이 맞습니다. 둘이 번갈아 배포하면 마지막 것만 남습니다.

    **지금부터 투표 종료(10/5)까지 배포 없음.**
    긴급 수정이 필요하면 총괄이 판단하고, **그때 한 명만** 올립니다.

⚠ 그리고 배포 방법은 P6 에이전트가 한 대로 — **깨끗한 worktree 를 떠서
  커밋본만** 올립니다. 작업 트리에서 배포하면 남의 미완분이 같이 실립니다.
  이번에 실제로 그럴 뻔했습니다(미커밋 18건).

## 4. 에이전트가 물은 것 둘 — 답

**Q. P6("옮긴다") 와 P7("경유") 중 무엇을 싣는가**
→ **끝난 질문입니다. P7 이 이미 배포됐고 작동합니다.** 리전은 옮기지 않습니다.
  sin 이 붙는다는 사실은 미완 §1-p 에 남기고, 이전은 투표 뒤에 다시 봅니다.

**Q. 병렬 작업이 있는가**
→ **있습니다.** `c2e2de2 [P7]` 이 그쪽 것이고, 이미 프로덕션입니다.

## 5. 「잠시 후」 남은 세 자리 — **그대로 둡니다**

에이전트가 *"우리 쪽 실패라 「잠시」가 참일 수 있어서 판정 없이 넓히지
않았다"* 고 한 것 — **그 판단이 맞습니다.**

    main.py:560   429 요청 과다      → 1분 뒤면 진짜 풀립니다
    main.py:814   등급표 읽기 실패    → 우리 쪽 문제이고 재시도로 낫습니다
    index/watch   fetch 오류 접미사   → 순간 네트워크 오류입니다

**P2 가 고친 것은 「우리가 원인을 모르는데 원인을 말한 자리」입니다.**
이 셋은 우리가 원인을 압니다. 다릅니다.

⚠ **이 구분을 `verifier.py` 주석에 한 줄 적어 두십시오.** 안 적으면 다음
  사람이 "일관성" 을 이유로 셋 다 바꿉니다. 그러면 429 에서 "다시 시도" 를
  못 말하게 되고, 그건 셀러에게 손해입니다.

## 6. 남은 미완 (투표 뒤)

    showcase 개인정보 가드     화면에 올릴 때가 조건입니다. 지금은 라우트가
                             없어 나가지 않습니다
    P4 도매꾹 리스트           10/5 이후
    리전 이전 여부             sin 이 붙는 것은 확보. 워치 덤프→복원 리허설 후
    물총·인형 등급표           고시 원문 확인 후 총괄 판정
    2026-11-01 운용요령 개정   10월 안에

---

## 한 줄

**리전을 옮기지 않고 조회만 싱가포르를 지나가게 해서 정부 조회가 살았습니다.**
투표 전날 밤에 볼륨을 건드리지 않고 끝냈고, 되돌리기는 여전히 secret 한
줄입니다. 여기서 멈춥니다.
