# 작업로그 2026-09-12

## 0. 환경 변경 — 구현 에이전트 모델

2026-09-11 구현 에이전트 모델을 **Fable 5.1** 로 바꿨다가 안전 분류기 오탐(`[cyber]`)으로
**Opus 5** 로 되돌렸다. 원인: 경계 시험·개인정보 마스킹·호스트 게이트 작업이 그 모양으로
읽힌다. 요청 ID `req_011CewxjeLVWtQp7j7ViDy15`.

**막힌 작업**: [I] 전기용품 안전기준(requirement) 조사. `law.go.kr` DRF 에서
「전기용품 안전기준(KC …)」 37건 목록과 한 건의 첨부 링크(`K 10021.pdf`)까지 받아 둔
상태에서 멈췄다. 모델을 되돌린 뒤 그대로 이어간다.

⚠ 우리 작업이 하필 그 분류기가 보는 모양과 겹친다 - 외부 응답에 잘못된 입력을 넣어
  동작을 보는 것([4-q]) · 사업자번호·전화번호 정규식으로 개인정보를 걷어내는 것 ·
  허용 호스트 검사 · 키 관리 · 네트워크 차단. 하나하나는 평범한 방어 작업인데 문장으로
  쓰면 공격 시험처럼 읽힌다. 표현 규칙은 CLAUDE.md §6 에 넣었다.

⚠ Fable 은 제출(9/20) 뒤에 다시 본다. 남은 작업이 전부 그쪽 모양이라 매번 문장을 다시
  쓰는 데 시간을 쓸 수 없다.

---

## 1. 이 날 한 것 (커밋 순)

    b6168f8  인수인계 문서 (세션 인계용)
    9244d2c  인수인계 §0 닫힘 — 배포 회귀 복구 확인
    faab7d0  [D-백] 오답 신고 + 주말 묶음 API 필드 점검
    49a289c  [G-1] 랜딩 / 와 도구 /scan 분리
    a1e5504  [M-1] 도매꾹 카테고리 지도 (호출 2회 · 5,037 카테고리)
    c2ed4fd  [M-2] 중분류 199 × 100건 제목 수집 (호출 199회 · 18,938건)
    14cfdb4  [M-3] 커버리지 지도 (배치 경로 · LLM 0회)
    4738673  [M-4] 별칭 후보 발굴 (자동 삽입 0건)
    6b07dca  [F] 국내 리콜 상품명 등급표 부착
    8e0b56c  [H] 도매꾹 구조 필드 → ProductFacts 어댑터
    f62f7de  [H] 어댑터 NOT_A_VALUE 보강

세부는 각 커밋 메시지와 `docs/미완_목록.md` 4-u ~ 4-y 에 있다.

---

## 2. ⚠ 배포 회귀 — 내가 만든 것

[결정 1] GPT 전환에서 **fly secret `EXTRACTOR_ORDER` 를 새 코드 배포 전에 지웠다.**
그 사이 배포가 세션 종료로 끊겨, 배포본이 약 15분간 옛 기본값 `['claude','gpt']`
(잔액 0 인 Claude 1순위)로 돌았다.

    14:52 secret 삭제 → 배포 끊김 → 15:01 새 코드 기동

**올바른 순서는 "새 코드 배포 → secret 삭제" 였다.** 그리고 `fly deploy` 는 foreground
로 돌린다 - 배경으로 띄우면 세션이 끝날 때 빌드가 죽는다.

---

## 3. [판정 1+2] "이 문자열은 값이 아니다" — 소유자를 하나로

### 3.1 ⚠⚠ 4-r 과 같은 뿌리다

4-r 은 **대조하지 않고 대조했다고 말한 것**이었다. 이것은 **값이 아닌 문자열을
값으로 읽어 대조했다고 말한 것**이다. 둘 다 "오류 표시가 없으면 성공" 의 친척이다.

    4-r          verifier 가 RecallIndex 와 다른 조건으로 대조 가능 여부를 판단 →
                 product_name 만 있어도 "대조했고 없었다" (233/233 = 100%)
    판정 1+2     model_name 이 '해당없음' 인데 그것을 모델명으로 읽어 대조 →
                 can_compare()=True → "대조했고 없었다" (34행)

4-r 의 고침이 `RecallIndex.can_compare()` 를 **소유자 하나**로 만든 것이었다.
그런데 그 소유자가 받는 **입력이 오염**돼 있었다 - 소유자를 세워도 입력이
자리표시자면 같은 거짓말이 나온다. 이번은 그 입력을 고친 것이다.

⚠ 그래서 4-r 은 "고쳤다" 가 아니라 **"절반만 고쳤다"** 였다. 같은 뿌리가 한 층
  아래에 남아 있었고, 그것을 찾은 것은 총괄이 "판정 1 과 2 는 한 문제다" 라고
  묶어 준 덕이다.

### 3.2 흩어져 있던 여섯 곳

    domeggook_fields._PLACEHOLDER_CORE    본문 미루기 표기 (상세설명참조 …)
    domeggook_adapter.NOT_A_VALUE         어댑터가 따로 든 목록
    watchlist._MODEL_PLACEHOLDERS         모델명 자리표시자
    watchlist._MAKER_PLACEHOLDERS         업체명 자리표시자
    kats_client.CERT_PLACEHOLDERS         인증번호 자리표시자
    data/kats_field_map.yaml              certState 미표기 값

**소유자는 `sourcing_guard/placeholders.py` 다.** 나머지 다섯은 그것을 부른다.
`kats_field_map.yaml` 만 그대로 뒀다 - 설계서가 정하는 값이고 "필드명·값을
매핑에서 주입한다" 는 원칙이 걸려 있다. 목록이 같아 보여도 소유자가 다르다.

층이 둘이라는 것도 적어 뒀다:

    층 1  "값이 아니다"          공통 · 소유자 하나        해당없음 · - · N/A
    층 2  "모델명이 아니다"       도메인별 · 층 1 을 부른다   BLACK · 공급자적합성

색상명 `BLACK` 은 **값이다**(materials 에 살아야 한다). 다만 **모델명은 아니다**.
두 판단을 한 목록에 섞으면 한쪽이 반드시 틀린다.

### 3.3 R6 은 이 판단과 무관하다

R6(워치리스트의 오류 비대칭)은 **매칭 강도**에 걸리는 것이지 자리표시자 판정이
아니다. `'해당없음'` 을 모델명으로 받아 관대하게 매칭하면 관대한 것이 아니라
**엉뚱한 상품에 리콜을 붙이는 것**이다. 주석으로 `watchlist` 양쪽 판정 함수에
적었고 검사로 잠갔다(`test_the_watchlist_comment_records_that_r6_is_unrelated`).

### 3.4 잰 것 — 전/후

    다섯 기준 (재생 · GPT · 분모 135)          전 = 후   무변화
      ① 검수된 정답 95 (70.4%) · ② 미검수 18 · ③ 상한 113 (83.7%)
      ④ 비대상 부착 0 · ⑤ 애매 부착 3

    도매꾹 239 · 상세 109                      전 = 후   무변화
      정답 89 · 애매 2 · 오답 2 · 미매칭 16
      kc 25 · rf 9 · kc_verified 22 · revoked 2 · expired 6 · not_found 2
      recall_match 7 · out_of_scope 5
      신호등 {UNKNOWN 80, RED 2, AMBER 25, GREEN 2}
      RED 2 유지  [67] kc_revoked · [137] kc_revoked+recall_match

    can_compare                                34 → 31   **움직였다**
      [ 41] LED 무드등 블루투스 스피커    model='상세페이지'      maker='상세페이지'
      [ 53] 이에스 로봇청소기             model='상세설명참조'    maker='상세설명참조'
      [153] 동아 빼꼼 크레파스 55색       model='해당없음'        maker='상세정보 별도표기'
      세 줄 모두 kc_numbers=[] 이라 대조할 것이 아무것도 없었다
      바뀐 줄의 리콜 축(후) · recall_clear 0 · '대조 정보 부족' 안내 3

    A-4 내용없음      1,627 (76.4%)  →  1,641 (77.0%)
    A-5 내용없음        645 (79.2%)  →    655 (80.5%)
        전부 빈 상품     64 (58.7%)  →     65 (59.6%)
        인증 항목        55 (87.3%)  →     56 (88.9%)

⚠⚠ **총괄이 "④ 상세 109 는 움직인다(그것이 목적이다)" 라고 했는데 안 움직였다.**
  재서 확인했다. 이유는 바뀐 세 줄이 **`recall_match` 가 없었고 이미 UNKNOWN**
  이었기 때문이다 - `recall_clear` 는 ④ 집계에 들어가지 않는다. 움직인 것은
  `can_compare`(34→31)와 **그 세 줄이 화면에서 하는 말**이다:

      전   "리콜 이력이 없습니다"        ← 값 아닌 문자열로 대조했다고 말함
      후   "대조 정보 부족 — 모델명·인증번호가 없어 대조하지 못했습니다"

  ④ 가 안 움직인 것은 고칠 것이 없어서가 아니라 **④ 가 재는 축이 아니어서**다.
  지표를 하나만 보면 이런 고침은 "아무 효과 없음" 으로 보인다 - §6 의 "지표에서
  빼는 것으로 끝내지 마라" 와 짝이 되는 함정이다.

### 3.5 뚫려 있던 반대 방향 둘

    N/A            `/` 가 조각 구분자라 `N`+`A` 로 쪼개져 통과했다 →
                   **전체 문자열을 먼저 본다**
    [상세정보 별도표기]   옛 `domeggook_fields._STRIP` 이 대괄호를 벗겼는데 새
                   `normalize` 는 공백만 벗겼다 → 대괄호 규칙을 소유자로 옮겼다

둘 다 **좁혀지는 쪽**(값인데 값이 아니라 하는 쪽)이 아니라 **넓혀지는 쪽**
(값이 아닌데 값이라 하는 쪽)이었다. §6 이 적은 "없는데 있다고 하는 쪽이 비싸다"
그대로다.

### 3.6 자기 주석에 걸린 검사 — **다섯 번째**

소유자 가드가 `_PLACEHOLDER_CORE` 를 금지어로 찾는데, **왜 금지인지 설명하는
주석**에 그 문자열이 있어 스스로 걸렸다. 같은 함정이 이 프로젝트에서 다섯 번째다.

그래서 `tests/srccheck.py` 로 **소유자를 하나 만들었다** - `code_only()` 가
docstring·주석을 벗긴다. 전에는 `test_domeggook_alias_candidates.py` 안에만
있었고, 그래서 이번 검사가 다시 걸렸다. §6 "같은 판단을 두 곳에 적지 마라" 는
프로덕션 코드만의 규칙이 아니다.

### 3.7 원자료를 되살리니 **딴 것도 한 줄 움직였다** — 낡음의 실제 크기

`도매꾹_필드실측_2026-09-08.json` 을 `--out` 으로 다시 쓰자 판정 1+2 와 **무관한**
줄이 하나 더 움직였다:

    전파번호_정규식미인식   ['MSIP-CMI-YOU-SOUND-T', 'MSIP-CMI-DVT-Rainbow']  →  []

원인은 커밋 `3abc841` **[4-c] RF_NUMBER_RE 에 MSIP- 추가** 다. 이 json 은 그
고침보다 앞선 상태로 멈춰 있었다 - 문서(A-5 §223)는 "4-c 로 0 이 됐다" 고 이미
적고 있었는데 **원자료만 옛말을 하고 있었다.**

⚠ 즉 이 파일은 오늘 것 하나가 아니라 **나흘치 뒤처져 있었다.** 재측정 스크립트는
  `--out` 없이 돌리면 화면에만 찍고 json 을 안 쓰므로, 화면을 보고 "쟀다" 고
  넘어가면 파일은 그대로 남는다. 그래서 낡음 가드
  (`test_the_a5_document_numbers_match_the_measurement_file`)를 붙였다 -
  **문서 ↔ 원자료**를 대조하므로 다음에 어느 쪽이 뒤처져도 빨간불이 뜬다.
