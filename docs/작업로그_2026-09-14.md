# 작업로그 2026-09-14 — sooknise

총괄 명령 2026-09-13 저녁("제출까지 남은 일 전부")의 ① 부터. 레인 잡음은
`docs/작업로그_2026-09-13.md` §9 에 적었다(커밋 77a8259).

## 1. [①] 결과 카드 v2 + [등급 근거] — 2026-09-14

### 1-1. [등급 근거] 결함 — 근거가 틀린 근거였다

총괄 실측대로였다. 고치기 전 실측(목 모드):

```
완구                 label 전기용품 및 생활용품 안전관리 운용요령 별표 1~7 (세부품목)
어린이용 물놀이기구   label 전기용품 및 생활용품 안전관리 운용요령 별표 1~7 (세부품목)
전기주전자           label 전기용품 및 생활용품 안전관리 운용요령 별표 1~7 (세부품목)
유아용 섬유제품       label 전기용품 및 생활용품 안전관리 운용요령 별표 1~7 (세부품목)
```

표의 행이 든 `source` 는 각각 "어린이제품 안전 특별법 시행규칙 별표 2 / 별표 1 /
운용요령 별표 1 / 시행규칙 별표 2" 다. 넷 중 셋이 **자기 품목이 없는 고시**를
가리키고 있었다. 고친 뒤:

```
완구                 어린이제품 안전 특별법 시행규칙 별표 2
                     https://www.law.go.kr/법령/어린이제품안전특별법시행규칙
어린이용 물놀이기구   어린이제품 안전 특별법 시행규칙 별표 1 ·
                     전기용품 및 생활용품 안전관리 운용요령 별표 4      ← 법령이 갈린다
전기주전자           전기용품 및 생활용품 안전관리 운용요령 별표 1
유아용 섬유제품       어린이제품 안전 특별법 시행규칙 별표 2
```

### 1-2. 주소는 200 이 아니라 **본문**으로 확인했다

`law.go.kr` 은 한글주소를 못 찾아도 **200 을 준다.** 대조군까지 재서 확인했다:

```
GET /법령/어린이제품안전특별법시행규칙          200  <title>어린이제품안전특별법시행규칙</title>
GET /행정규칙/전기용품및생활용품안전관리운용요령  200  <title>전기용품및생활용품안전관리운용요령</title>
GET /법령/어린이제품안전특별법시행규칙/별표2      200  <title>국가법령정보센터 | 오류페이지</title>
GET /법령/어린이제품안전특별법시행규칙없는이름XYZ 200  <title>국가법령정보센터 | 오류페이지</title>
```

**별표 단위 주소는 없다.** `curl -sI`(HEAD)는 네 주소 전부 404 를 주므로 그것도
판단 근거가 못 된다. CLAUDE.md §6 의 "오류가 아니면 성공" 자리다 — 상태 코드로
확인했으면 없는 주소를 근거로 붙였다.

### 1-3. 결과 카드 v2

`design/result-card-v2.html` → `index.html render()` + `app.css .rv-*`.

```
머리      얼굴 88 · 칩 16 진한 바탕 흰 글자 · 헤드라인 26/34 + 부제 16/24 (" — " 분리)
읽은 값    알약 15px (제품명 · 인증번호+정부 조회 · 재질 · 대상연령 · 품목 등급)
축 셋      서버 name·label 그대로 · 이름 14/20 · 값 18/26 · 신호 색·체크 없음
그룹      머리말 14/20 = grouped_findings.header · 점 = finding.signal · 문장 16/26 · 링크 15
접기      hazard_rule_applies 4건 이상 → 한 줄 + 물질 알약 4 + "+N" + 펼치기
신고      "이 품목이 아닙니다" 15px · 등급 줄에만
푸터      메타 14px + §9 고정 문구
```

15px 미만 선언은 **전부 정리**했다. 남은 넷은 랜딩 예시 카드(`.rc-*`)이고, 카드 안
한정어 목록은 `check_landing_acceptance.SMALL_OK` 가 들고 있다.

### 1-4. 인수 — **빈 화면을 재지 않게** 고쳤다

인수 스크립트가 도구 화면 셋을 열기만 하고 글꼴·파비콘만 봤다. 세 화면 다 기본
상태가 비어 있어서 **아무것도 재지 않고 통과**하고 있었다. 이제 /scan 은 실제로
검사를 돌리고, /batch 는 네 줄을 넣어 누르고, /watch 는 항목이 있는 응답으로 열고
나서 잰다 — 그려졌다는 증거 선택자를 못 찾으면 그 화면은 실패다.

### 1-5. 확신 못 하는 것

- `_performance_statement`(승차용 안전모·휴대용 레이저 두 룰)가 **죽은 코드**다.
  `if performance` 가지에서 문장을 만든 뒤 아래 `limit` 블록이 그것을 덮어쓴다.
  실측: "이 품목에는 '승차용 안전모 안전요건' 기준이 적용됩니다. 시험성적서로
  확인이 필요합니다." — 통과해야 할 시험 이름과 정부 조사 부적합률이 사라진다.
  **거짓은 아니고 덜 알려주는 쪽이다.** ① 이 여는 범위 밖이라 고치지 않았다.
- 카드 푸터와 페이지 푸터에 §9 문구가 **둘 다** 뜬다. 정본이 카드 푸터에 넣으라고
  했고 페이지 푸터는 결과가 없을 때도 §9 를 지켜야 해서 둘 다 뒀다.

### 1-6. 배포본 확인 (confirm-four)

```
build.commit  1e25bff4e · 2026-09-13T17:05:20Z
실스캔        200 · meta.extraction_path llm · extracted_by LLM(GPT · gpt-5.4-mini)
gov_lookup    {"cert": "ok", "recall": "ok", "rf": "not_attempted"}
watch_sweep   last_full_sweep_at 2026-09-13T17:06:10+00:00 · items 6 · alerts_stored 2
등급 근거      어린이제품 안전 특별법 시행규칙 별표 2        ← 배포본에서 확인
접기          "14건 펼치기" + 안티모니 60mg/kg · 비소 25mg/kg ·
              바륨 1000mg/kg · 카드뮴 75mg/kg · +10
인수          네 화면 × 라이트/다크 × 1440/390 전부 통과 (배포본)
빈 값         열기 7경우 × 화면 4 · 결과 3경우 전부 통과 (배포본)
JS 오류        0
스크린샷       design/preview/applied-scan-v2-deployed-1440.png
```

**실호출 회차: LLM 5회** — 인수 스크립트가 /scan 을 네 번(라이트/다크 × 1440/390)
돌리고, 배포본 확인에서 한 번. `by_vendor {'gpt': 5}` · 실패 0.
정부 API 는 리콜 로컬 사본 위에서 돌므로 인증 조회만 나갔다.

---

## 2. [디자인] 데모 버튼 세로 쌓기 — 시피님 지시

> "이렇게 줄바꿈 될 바엔 차라리 디스플레이 컬럼 형태로 해서 배치하는 게 나을
> 것 같아. 박스의 높이를 늘리더라도."

`/scan` 데모 버튼이 `flex-direction: row` 라 칩·제목·부제가 폭을 나눠 가졌다.

```
전 (실측 · 1440px)   칩 74px · 제목 103px · 부제 157px · 카드 높이 113
                     제목 17px/400 · 부제 17px/400   ← 위계가 없었다
                     "같은 상품에 / 재질 한 줄 추 / 가"
후                   세로로 쌓음 · 카드 높이 150
                     제목 17/26 700 · 부제 15/22 --fg-2
                     한 칸에 한 줄씩 (1440 · 390 둘 다)
```

⚠ 랜딩(`.lv2 .demo`)이 **같은 모양을 따로 들고 있었다.** 공용 `.demo` 한 곳으로
합치고 랜딩은 링크라서 다른 것(밑줄 없음 · hover 이동)만 덧쓴다 (§6).

회귀 가드는 `check_landing_acceptance._demo_stack` 이다. 반대 방향으로 재 봤다 —
`flex-direction` 을 되돌리면 여섯 줄이 걸린다:

```
[light 1440px] 데모 0 가 가로로 붙어 있다 (윗칸 하단 700 > 아랫칸 상단 664)
[light 390px]  데모 1 가 가로로 붙어 있다 (윗칸 하단 748 > 아랫칸 상단 712)
```

가드가 처음에 "넓은 화면에서 한 줄" 로 세었더니 **랜딩 데모 셋이 전부 걸렸다.**
거기는 히어로 한 칸이라 카드가 좁고 두 줄이 정상이다 — 증상(줄 수)이 아니라
원인(셋이 폭을 나눠 가짐)을 센다.

이모지 가드에 **열세 번째**로 걸렸다. CSS 주석에 쓴 `⚠` 다 — "주의(중요):" 로 바꿨다.

---

## 3. [①-b] performance 문장 되살리기

유해물질 분기를 세 갈래로 바로 세웠다. 전에는 `performance` 가지가 문장만
만들고 `continue` 를 안 해서 **아래 공통 블록이 그 문장을 버렸고**,
`requirement` 가지의 `continue` 뒤에 같은 append 가 한 벌 더 놓여 도달하지
못했다.

```
전 (실측)  이 품목에는 '승차용 안전모 안전요건' 기준이 적용됩니다.
           시험성적서로 확인이 필요합니다.
           detail={'rule_id': 'KC-LIFE-HELMET-PERF'}

후         이 품목은 안전확인 안전기준 부속서 52(승차용 안전모) 대상입니다.
           충격 흡수, 유지 시스템 강도, 내관통 시험을 통과해야 하며, 공급처에
           시험성적서를 요구하세요. 참고로 국가기술표준원 해외 구매대행
           안전성조사(2026-05-14)에서 이 품목은 10개 중 7개가 안전기준에
           부적합했습니다. 표적 조사이고 표본이 작아 일반화할 수는 없으나,
           확인 없이 소싱하기에는 위험이 큽니다.
           detail={... test_items: ['충격 흡수','유지 시스템 강도','내관통'],
                    failure_rate: {sample: '10개 중 7개', source: ...}}

           휴대용 레이저용품도 같다 - 겉모양·구조·레이저의 등급 · 10개 중 8개.
```

**왜 아무도 못 봤나.** `tests/test_performance_rules.py` 가
`_performance_statement` 를 **직접 불러서** 재고 있었다. 단위 검사는 통과하는데
화면은 다른 문장을 받는 자리다 — 부르는 쪽을 지나가는 검사가 없으면 죽은 코드가
살아남는다. 이제 `verify()` 를 지나가는 검사 넷을 넣었다(목 모드 휴리스틱으로는
헬멧에 category 가 안 잡혀 룰이 안 걸리므로 `facts` 를 직접 만든다).

반대 방향으로 재 봤다:

```
performance 가지의 continue 를 지우면
  FAILED test_performance_statement_survives_to_the_finding[승차용 안전모…]
  FAILED test_performance_statement_survives_to_the_finding[휴대용 레이저용품…]
continue 뒤에 append 를 한 줄 넣으면
  AssertionError: 도달할 수 없는 줄: [1725]
```

```
검사      1487 → 1492 PASS (+5)
다섯 숫자  95/18/113/0/3 무변화 · 도매꾹239 168/239 무변화
판정      kind·signal 그대로 · scorer·등급표·별칭 무변화 (문장과 detail 만)
실호출     0회
```

---

## 4. [②-abc] 감시 해제 · 신고 멱등 · 조사 병기 없애기

### 4-a. DELETE /api/v1/watch/{id}

소유자가 맞을 때만 지운다. **없는 id 와 남의 id 를 구분하지 않는다** — 둘 다
404 다. 구분하면 "그 id 는 존재한다" 를 남에게 알려 주는 셈이다.

그 항목의 알림도 한 트랜잭션에서 같이 지운다. 남기면 `alerts_for_owner` 가
watch_items 와 조인하므로 화면에서는 사라지지만 행은 남아 `alert_count()` 와
`/healthz` 가 계속 센다.

화면은 항목마다 "감시 해제" 버튼이고, 되돌릴 수 없으므로 한 번 묻는다.
서버가 204 를 준 **뒤에** 목록을 다시 그린다 — 화면에서 먼저 지우면 실패했을 때
"사라진 줄 알았는데 있는" 상태가 된다.

### 4-b. report-miss 멱등

키는 (소유자 · 입력 텍스트 지문 · 정렬한 품목) 의 SHA-256 이다. 원문을 색인에
통째로 넣지 않는다(20만 자까지 온다). 품목은 정렬해서 넣는다 — 순서가 키를
가르면 같은 신고가 두 줄이 된다.

```
1회차  201  4912d6b53b4b  "신고가 접수되었습니다…"
2회차  200  4912d6b53b4b  "이미 접수된 신고입니다…"
miss_reports.total  5 → 6      (두 번 눌러 +1)
품목 다름 201 · 소유자 다름 201 · owner 없으면 멱등 처리 안 함 (201/201)
```

⚠ **배포본 DB 에는 `dedupe_key` 열이 없다.** 열 추가를 스키마보다 먼저 돌린다.
주석이 걱정한 것을 실제로 재 봤다:

```
CREATE UNIQUE INDEX … ON miss_reports(dedupe_key)  →  OperationalError: no such column
```

그 예외는 `__init__` 에서 **DB 손상으로 읽혀 격리**된다 — 워치 항목이 통째로
사라진다. 옛 스키마 DB 를 만들어 열어 보고 확인했다: 격리 None · 옛 신고 2건
그대로 · 열 추가됨 · 멱등 동작.

### 4-c. 조사를 병기하지 않는다

`watchlist.py` 의 `'{subject}' 과(와)` 를 `models.with_particle` 로 바꿨다.

⚠ **와/과 는 나머지 셋과 반대다.** 받침이 없으면 '와', 있으면 '과' — 이/가·
은/는·을/를 은 받침이 없을 때 두 번째 형태를 쓰는데 이것만 첫 번째다.
`subject_particle` 결과를 그대로 뒤집어 쓰면 전부 틀린다. 검사가 그 비대칭을
직접 잠근다.

리포 전체에서 "과(와)·이(가)·을(를)·은(는)" 류 0회다. 가드는 **AST 로 문자열만**
본다 — 주석까지 세면 가드를 설명하는 줄이 가드를 깬다(이 저장소에서 열두 번 넘게
난 모양이라 처음부터 가른다).

반대 방향으로 쟀다:

```
watchlist.py 를 되돌리면
  AssertionError: ["sourcing_guard/watchlist.py:459 '과(와)'"]
```

/watch 의 확인 문구는 목적어를 앞으로 빼서 조사를 아예 없앴다 —
"'…' 감시를 해제할까요?"

```
검사      1492 → 1505 PASS (+13)
다섯 숫자  95/18/113/0/3 무변화 · 도매꾹239 168/239 무변화
인수·빈 값 전부 통과 · 실호출 0회
```

---

## 5. [②-def] /scan 상단 v2 톤 · 메타 줄 한국어 · 버튼 눈금

### 5-d. 상단

랜딩 히어로와 **같은 두 문장**으로 맞췄다. 랜딩이 약속하고 /scan 이 그 약속을
실행하는 화면이라, 누르고 넘어온 셀러가 같은 말을 다시 보는 것이 맞다.
24자 중복 검사의 `_SHARED_BY_DESIGN` 에 이유와 함께 적었다.

```
전  h2 "이 상품, 팔아도 되는지 확인합니다"
    p  "상세페이지 내용을 아래에 붙여넣거나 예시 버튼을 누르세요. 결과의 각
        항목에는 정부 원문으로 가는 링크가 함께 표시됩니다."
후  h2 "상세페이지를 붙여넣으세요."                       32px · 높이 42
    p  "KC 인증 · 리콜 · 유해물질 기준을 정부 원문으로 확인합니다."  19px · 높이 29
```

⚠ 명령의 "예시 셋은 칩 + title, note 는 그 아래 15px 한 줄" 은 **이미 됐다** —
시피님 지적으로 [디자인] cc90bdb 에서 세로로 쌓았다(제목 17/26 700 · 부제
15/22). 다시 만지지 않았고 회귀 가드(`_demo_stack`)가 지킨다.

### 5-e. 메타 줄

`LOOKUP` 의 값 `ok` → `성공`. 나머지 둘은 이미 한국어였는데 성공만 영어였다.

```
후: 추출 휴리스틱(LLM 응답 없음) · 정부 조회 인증 성공 · 리콜 성공 ·
    전파 시도 안 함 · 리콜 대조 기준: 2026-09-08 공표분까지 (매일 갱신)
```

키(`ok`·`failed`·`not_attempted`)는 서버가 주는 것이라 그대로 둔다 — 검사가
값만 한국어인지, 키는 셋 그대로인지 따로 본다.

### 5-f. 버튼 눈금 (좌표 측정)

```
전  /scan 검사 56px/19  ·  지우기 48px/17
후  /scan 검사 60px/19  ·  지우기 60px/17
    /batch 검사 60px/19
    /watch 지금 대조하기 60px/17
랜딩 .btn-primary 60px/19 (기준)
```

보조 버튼은 **높이만** 같다. 나란히 놓였을 때 높이가 다르면 줄이 어긋나 보이고,
무게는 글자 크기와 배경이 가른다. 카드 안 작은 버튼(`.btn-t.sm` 38px)은 그대로다.

회귀 가드 `check_landing_acceptance._button_scale` 은 좌표로 잰다. 반대 방향:

```
높이를 56 으로 되돌리면
  [/scan light 1440px] 주 버튼 '검사' 가 56px/19px 다 (60/19)
```

⚠ 이모지 가드에 **열네 번째**로 걸렸고, 랜딩 라우트 가드에도 한 번 걸렸다 —
둘 다 **내가 쓴 주석**이 원인이다. 라우트 가드는 주석을 걷어내도록 고쳤고
(그 편이 옳다 — 화면에 그려지는 것만 세야 한다), CSS·JS 주석의 `⚠` 는
"주의(중요):" 로 바꿨다.

```
검사      1505 → 1507 PASS (+2)
다섯 숫자  95/18/113/0/3 무변화 · 도매꾹239 168/239 무변화
인수·빈 값 전부 통과 · 실호출 0회
스크린샷   design/preview/applied-scan-v2-top-{light,dark}-{1440,390}.png
```

---

## 6. ② 배포본 확인 — 64acecf30

```
build  64acecf30 · 2026-09-14T01:52:03Z

a. 감시 해제
   등록 id 96a4423c8c51
   남의 id  → 404      없는 id → 404      (구분하지 않는다)
   목록 전 1건 → 내 것 DELETE 204 → 목록 후 0건

b. report-miss 멱등
   전 /healthz  {'total': 0, 'last_reported_at': None}
   1회차 201 5811988ba770 | 신고가 접수되었습니다…
   2회차 200 5811988ba770 | 이미 접수된 신고입니다…
   후 /healthz  {'total': 1, 'last_reported_at': '2026-09-14T01:53:30+00:00'}

   ⚠ 배포본 DB 는 `dedupe_key` 열이 없던 상태였다. 열자마자 열이 붙었고
     격리되지 않았다 - watched_items 6 그대로다.

인수   네 화면 × 라이트/다크 × 1440/390 전부 통과 (배포본)
빈 값   열기 7경우 × 화면 4 · 결과 3경우 전부 통과 (배포본)
실호출  LLM 4회 (인수 스크립트의 /scan 네 번) · by_vendor {'gpt': 4} · 실패 0
```

⚠ **배포본에 워치 항목이 여섯 개 남아 있다.** [C-화면] 확인용으로 등록한 것이
섞여 있는데, 소유자 식별자가 그 브라우저 세션과 함께 사라져 내가 지울 수 없다
(소유자 일치를 요구하는 것이 설계다). 지울지는 총괄이 정한다.

---

## 7. 잡음 — [③] · sooknise

총괄 판정 2026-09-14: ①-b · ② 닫음. 여기부터 ③ [L-2 + L-1(다) + ⑦-d].

⚠ **배포본 워치 6건 중 일부는 확인용이다.** 지우지 않는다 — 지우면
`alerts_stored 2` 가 함께 사라져 감시가 한 번도 안 걸린 것처럼 읽히고, 소유자별
화면이라 투표자에게 보이지도 않는다. 소유자 검사를 우회하는 스크립트를 프리즈
앞에서 만들지 않는다 (총괄 판단).

---

## 8. [③] 안내 축 셋 — 지재권 · 소관 · 원산지

새 kind 셋. **전부 판정이 아니다** — `_PENALTY` 0 · CONTEXT 구획 ·
`NON_SPECIFIC_FINDING_KINDS` · 신호를 바꾸지 않는다.

```
FindingKind   31 → 34      (ip_marker_notice · jurisdiction_notice · origin_mismatch)
표 자리 수     9 그대로      (새 표를 만들지 않고 기존 표에 넣었다)
다섯 숫자      95/18/113/0/3 무변화 · 도매꾹239 168/239 무변화
검사          1507 → 1541 PASS (+34)
```

### 8-1. [L-2] 표기어 — 후보 13개 중 **둘만** 남았다

코퍼스 19,173줄(도매꾹 제목 18,938 + 새표본 235). 크레파스 원칙대로 넓게 던지고
**실제로 걸린 것만** 남겼다. 버린 열하나와 이유는 `data/ip_markers.yaml` 주석에
건수와 예까지 적었다.

```
남긴 것   호환   45건 (도매꾹 44 · 새표본 1)
                 진짜: 갤럭시워치 호환 · 세타필 크림 호환 · 로지텍 K380 호환
                 오탐: USB 호환 · 전기종 호환 · 변기 호환  (일반 규격)
          st     4건 · 한글 바로 뒤만    이케아st · 베이직st · 우드st · 애니st

버린 것   스타일 46  "프리스타일 헌팅캡" · "청바지 스타일" — 옷차림 낱말
          리퍼    9  "네일클리퍼" · "바나나 리퍼블릭" — 낱말 조각
          st(띄고) 4  "ST-8000" · "ST GL003S" 가 3/4 — 모델 번호
          모조    3  "모조지"(종이) · "비트코인 모조 돈"
          이미테이션 1  "샤크 이미테이션 안테나" — 더미 안테나
          유사품  1  "(유사품주의" — 셀러가 자기 짝퉁을 경고. 방향이 반대다
          대체품  1  "립틴트대체품"
          카피    0  (카피바라 29건을 빼면 0)
          레플리카·짝퉁·가품·비정품·정품 아님   전부 0
```

**브랜드명 사전은 만들지 않았다 (R5).** 검사가 그것을 잠근다 — 문장에
'갤럭시' 가 들어가면 실패한다.

### 8-2. KIPRIS 링크 — 브라우저에서 셋을 재 봤다

검색 폼이 **POST** 라 주소로 검색어를 미리 채울 수 없다.

```
?queryTextTop=완구                뒤쪽 검색칸은 채워지는데 탭이 특허이고
                                  결과가 안 돈다(검색 팁 화면)
?tab=trademark&queryTextTop=완구   상표 상세검색 창이 뜨는데 자유검색 칸은 비어 있다
?tab=trademark                    상표 검색 창이 바르게 뜬다   ← 이것을 쓴다
                                  <title>홈 > 지식재산정보 검색 > 상표 검색</title>
```

반쯤 채워진 주소를 쓰면 셀러가 "검색된 줄" 알고 빈 화면을 읽는다. 화면에
**상품명 복사** 버튼을 뒀다. 상표법 조문 링크도 본문 `<title>` 로 확인했다 —
law.go.kr 은 없는 주소에도 200 을 준다.

### 8-3. [L-1(다)] `mode: notice` — 검증을 끄지 않는 소관 안내

`jurisdiction_map.yaml` 에 `mode` 를 넣었다.

```
out_of_scope  (기본)  표지어가 걸리면 인증·리콜 검증을 통째로 건너뛴다
notice                검증을 켠 채로 안내만 붙인다 (`표지어_안내`)
```

새 줄: 자동차 부품·장치 (자동차관리법 / 국토교통부). 표지어 카시트·킥매트·
룸미러·안전벨트, 도매꾹 36 · 새표본 10. 오탐이 있다("임산부 안전벨트"·
"유모차 룸미러"·"강아지 카시트") - 안내 축이라 비용이 낮다.

**화장품은 낱말 하나만 옮겼다.** 실측: 도매꾹 '화장품' 163건 중 **53건(32.5%)이
화장품을 넣는 용기·도구**였다(파우치·필통·케이스·세면백). 그 낱말로 검증을 끄면
합성수지 파우치의 리콜 대조가 사라진다. 하드 신호(화장품책임판매업자·기능성
화장품·EWG·클렌징폼)는 `out_of_scope` 그대로다.

⚠ 그 바람에 4-d-1 의 "덮지 않고 병기한다" 가 **다른 kind 로 옮겨 갔다.** 셀러
공지 배너("3W CLINIC 화장품")가 `substances_mentioned` 로 들어오므로 안내 쪽
haystack 도 거기를 보게 했다 - 안 보면 그 줄이 통째로 사라진다.

### 8-4. [⑦-d] 원산지 대조 — 프롬프트를 건드리지 않았다

`ProductFacts` 에 제조국 필드를 더하면 **R7 대로 새표본 235건을 다시 재야 한다**
(LLM 235회). 제조국 표기는 형식이 고정돼 있어 결정적 코드로 읽을 수 있다 -
`sourcing_guard/origin.py`.

실측(도매꾹 상세텍스트 379건)이 정규화 규칙을 정했다:

```
154  수입산_아시아_중국       → 중국     (칸을 뒤에서부터 본다)
 66  또는 원산지 : 상세정보 별도표기 → None  (값이 아니다)
 29  중국 · 9 국산 · 2 CHINA · 2 중국oem · 3 수입산_유럽_프랑스
  2  국산_충청남도_보령시     → 대한민국  (마지막 칸이 시·군이라 더 앞으로)
```

**양쪽을 다 알 때만** 말한다. 한쪽이 없으면 0줄이고, **모르는 말은 나라로 세지
않는다** — 그러면 '상세정보 별도표기' 와 '중국' 이 서로 다른 나라가 되어 가짜
불일치가 된다. 한 페이지에 나라가 둘이면 고르지 않는다(고르는 순간 판정이다).

```
다름(등록 중국 · 페이지 베트남)  1줄   같음  0줄   페이지에 없음  0줄
값 아닌 문자열  0줄            도매꾹 코드형  1줄   한 페이지에 둘  0줄
```

⚠ **수입요건은 하지 않는다.** HS 부호가 없고 추정은 R5 위반이다.

### 8-5. 곁다리로 고친 것 둘

- `models.direction_particle`(으로/로). 받침이 **ㄹ 이면 '로'** 라는 예외까지
  본다 - 없으면 "브라질으로" 가 된다.
- `scripts/probe_jurisdiction_laws.py` 가 **없었다.** yaml 주석이 "재현:
  … probe_jurisdiction_laws.py" 라고 적고 있었는데 그 파일이 없었다(문서가
  코드보다 앞서 나간 자리). 이제 실재하고, 소관 6줄의 법령·조문·시행일을
  law.go.kr 로 대조한다. 붙어 있던 `SG_LIVE_NET=1` 도 뺐다 - pytest 안에서만
  의미가 있는 주문이었다.

### 8-6. 확신 못 하는 것

- **약사법 소관부처가 law.go.kr 과 다르다.** 원문의 `소관부처` 칸은
  **보건복지부** 하나인데 우리 yaml 은 "식품의약품안전처 (면허·업무는
  보건복지부 공동 소관)" 로 적고 있다. 우리가 식약처를 앞에 둔 근거는
  **확인하지 않았다.** 숨기지 않고 `소관부처_원문`·`소관_비고` 로 yaml 에
  적어 두었고 스크립트가 매번 경고로 띄운다. 총괄 확인 대기.
- 인수 스크립트가 한 번 "Gowun Dodum 이 로드되지 않았다" 로 걸렸다가 재실행에서
  통과했다. Google Fonts 네트워크 흔들림으로 보이고, 목 서버가 Playwright
  부하에서 멈추는 것도 두 번 봤다. 배포본에서 다시 잰다.

### 8-7. ③ 배포본 확인 — e9bd14a03

```
build     e9bd14a03 · 2026-09-14T03:07:03Z · mock_mode false
실스캔     200

[ip_marker_notice]  '호환' 표기가 있습니다. 상표·디자인권은 이 도구가 확인하지
                    않습니다 — 권리자·KIPRIS 확인 필요
                    KIPRIS 상표 검색 ↗ · 상품명 복사 · 상표법 (법률) · law.go.kr ↗
[jurisdiction_notice] '카시트' 표기가 있습니다 — 국토교통부 소관(「자동차관리법」)
                    일 수 있습니다. … 이 도구의 인증·리콜 대조는 그대로 수행했습니다.
                    자동차관리법 제30조의2(자동차부품의 자기인증 등) ↗
[origin_mismatch]   인증번호 'JU071047-12002C' 에 등록된 제조국은 '중국' 인데
                    상세페이지에는 '베트남'으로 적혀 있습니다 — 확인이 필요합니다.
                    어느 쪽이 맞는지는 이 도구가 판단하지 않습니다.

링크      https://www.kipris.or.kr/khome/search/searchResult.do?tab=trademark
          https://www.law.go.kr/법령/상표법
JS 오류    0
인수       네 화면 × 라이트/다크 × 1440/390 전부 통과 (배포본)
빈 값      열기 7경우 × 화면 4 · 결과 3경우 전부 통과 (배포본)
실호출     LLM 5회 · by_vendor {'gpt': 5} · 실패 0
```

⚠ 원산지 줄은 **목이 아니다.** 배포본은 `mock_mode false` 이고 국표원 실 API 가
`JU071047-12002C` 의 등록 제조국을 '중국' 으로 준다.

---

## 9. [③ 후속] 소관 부처 — 어긋난 것은 yaml 이 아니라 내가 고른 칸이었다

총괄이 두 칸을 나란히 보여줬고 그대로 재현됐다.

```
법령        lawSearch 소관부처명        lawService 소관부처.content
약사법       보건복지부,식품의약품안전처    보건복지부
의료기기법    보건복지부,식품의약품안전처    식품의약품안전처
자동차관리법  국토교통부                 국토교통부
식품위생법    식품의약품안전처             식품의약품안전처
화장품법     식품의약품안전처             식품의약품안전처
생활화학제품법 기후에너지환경부            기후에너지환경부
```

`lawService` 는 공동 소관을 **하나로 줄여** 준다. 그 칸만 보고 대조했더니
약사법이 "어긋났다" 로 나왔는데 **어긋난 것은 우리 yaml 이 아니라 내가 고른
칸**이었다. 우리 문구 "식품의약품안전처 (면허·업무는 보건복지부 공동 소관)" 는
목록과 맞는다. 총괄 확인 대기 항목을 닫는다.

⚠ **대표가 어느 쪽으로 오는지는 법마다 다르다** — 약사법은 보건복지부,
의료기기법은 식약처다. **같은 목록인데 대표가 반대다.** 대표로 문구를 정하면
그 순간 두 법이 반대 기관을 가리킨다.

고친 것 셋:

```
probe_jurisdiction_laws.py   lawSearch 소관부처명(목록)으로 대조 · 두 칸을 같이 출력
jurisdiction_map.yaml        줄마다 소관부처_목록 · 소관부처_대표 기록 (6줄 전부)
tests/test_jurisdiction_map  네트워크 없이 잠근다 - 대표는 목록 안에 · 우리 문구의
                             앞 기관도 목록 안에 · **둘이 실제로 어긋나는 줄이 있는가**
```

마지막 검사가 반대 방향이다 — 두 칸이 늘 같다면 두 칸을 적을 이유가 없다.
약사법이 그 자리이고, 그 줄이 사라지면 "왜 두 칸을 적나" 를 다시 물어야 한다.

`생활화학제품법 · 기후에너지환경부` 는 총괄 표에 있지만 **표에 넣지 않았다** —
표지어를 실측하지 않았고, 재지 않은 낱말을 넣으면 오탐만 열어 두는 것이다
(크레파스 원칙).

```
검사   1541 → 1544 PASS (+3) · 다섯 숫자 무변화 · 판정 경로 무변화
실호출  law.go.kr 조회 12회 (조사용) · LLM 0회
```

---

## 10. 잡음 — [⑦-a] · sooknise

총괄 판정 2026-09-14: 소관 부처 항목 닫음. ⑦-a 전파 대상기자재 표 시작.
곁다리로 yaml 주석의 '환경부' → '기후에너지환경부' 로 이름을 맞췄다
(law.go.kr lawSearch 소관부처명 기준).

---

## 11. [⑦-a] 전파 대상기자재 표 — **받았고, 화면에는 안 올렸다**

### 11-1. 받을 때 확인한 넷

```
① ID       행정규칙일련번호 2100000282888 로 받았다 (행정규칙ID 38724 아님)
           응답 기본정보로 되확인 — 시행 20260724 · 발령 2026-4 ·
           소관 국립전파연구원 · 현행 Y. 재시도 3회·사이 3초는 붙여 뒀고
           이번 회차에서는 reset 이 나지 않았다.
② 제목      별표단위[0] 제목이 "적합성평가 대상기자재(제3조 관련)" 임을 먼저 확인.
           **번호로 고르지 않는다** — 응답에 별표가 30개이고 `별표번호 0001`이
           둘이다(별표 1 과 서식 1). 번호로 집으면 신청서 서식을 집는다.
③ 옮긴 수   별표 원문 1989행 · 표 행 367 · 옮긴 행 367 · 버린 행 0
④ 화면 연결  **안 올렸다** — 비대상 오부착 3건 (아래)
```

### 11-2. 버린 행 0 · 대신 옮기면서 판단한 것 넷

표가 세로 병합·줄바꿈이 심해서 처음엔 94행을 "정의 문단이라 이름이 아니다" 로
버리려 했다. **그런데 그 94행이 우리 셀러와 가장 가까운 줄이었다** — 전기청소기류
· 냉장고 · 세탁기 · 공기청정기 · 선풍기 · 에어컨 · 정수기 · 난방기. 버리면 정확히
소비재가 빠진다. 총괄 ③ 이 "버린 줄이 표본에 오는 품목인지 함께 보라" 고 한
자리이고, 답이 "온다" 였으므로 **버리는 대신 이름의 자리를 바꿨다.**

```
① 행의 정체는 기기부호다. ○ 가 든 줄이 행 시작, 그 아래 ○ 없는 줄은 이어짐.
② 병합된 상위 이름은 왼쪽 열이 아래로 늘어난다. 빈 칸은 위에서 물려받고,
   표지(가. 1) ①)가 없는 칸은 **앞 이름의 나머지**다 —
   "라.해상이동업무용 디지털선택호출" / "장치의 기기" 가 두 행에 걸친다.
③ 기기부호도 세로 병합된다 — ⑧⑨⑩ 감자탈피기·전기정미기·전기빵자르개가
   ⑦ 과일껍질깎기의 KCN12 를 함께 쓴다. 빈 부호 3건이 그렇게 채워졌다.
④ **가장 깊은 칸이 늘 이름인 것은 아니다.** 11절 소분류는 정의 문단이라
   이름은 그 위 칸(`1) 전기청소기류`)이고, 문단 안의 "대표적인 품목" 을
   examples 로 담았다 — 정부가 적어 둔 이름이지 우리가 지어낸 것이 아니다 (R5).
```

### 11-3. 별칭이 없으면 매칭이 0건이다

표 이름이 `전기청소기류` 처럼 류 단위라 상품명(`로봇청소기`)과 한 글자도 안
겹친다. **실측 0/239 · 0/235.** 고시가 적어 둔 대표 품목 509개를 별칭으로 주면
붙기 시작한다 — `ItemGradeBook.lookup_all(extra_aliases=…)` 로, **같은 매처**다.

### 11-4. 측정 — 비대상 오부착 **3건**

```
새표본235  붙은 줄 50/235 (21%)   대상 36 · 비대상 3 · 애매 11
도매꾹239  붙은 줄 73/239 (31%)   대상 9 · 분류없음 64

⚠ 비대상 오부착 3건
   '나도컵 꼬깔컵 생수컵 … 정수기컵 위생컵'      → 주방용 전기 액체가열기기류
   '[지앤지] 자동차 시트커버 … 카시트 …'        → 게임기구류 및 오락기구류
   '자동차 킥매트 등받이 보호 시트'              → 게임기구류 및 오락기구류
```

기준은 0 이었다. **3 > 0 이므로 표만 리포에 두고 화면은 "무선 기능 표기가
있습니다" 를 유지한다.** 등급표를 올릴 때와 같은 기준이다.

검사가 그 결정을 잠근다 — `화면연결: false` · 판정·화면 경로가 `rf_equipment`
를 import 하지 않음 · 정적 화면에 "적합인증 대상입니다" 0회 · 측정 스크립트가
0이 아니면 **실패**로 끝남. 다시 재서 0 이 되면 그때 연다.

### 11-5. 확신 못 하는 것

- 오부착 셋 다 **별칭 조각** 때문이다("정수기", "차양", "카시트"). 별칭을
  좁히면 0 이 될 수도 있지만, 그건 고시 원문을 우리가 골라 담는 일이라
  크레파스 원칙과 반대 방향이다 — 총괄 판단이 필요하다.
- 도매꾹239 는 대상분류 표가 없어 **64줄을 분류없음**으로 뒀다. 거기에
  비대상이 더 있을 수 있다. 세려면 그 표본도 전수 분류해야 한다.

```
검사      1544 → 1557 PASS (+13)
다섯 숫자  95/18/113/0/3 무변화 · 도매꾹239 168/239 무변화
판정 경로  무변화 (표는 리포에만 있다)
실호출     law.go.kr 1회 (별표 수집)
```

---

## 12. 잡음 — [⑦-a-2] · sooknise

총괄 판정 2026-09-14: ⑦-a 닫되 **오부착 원인이 보고와 다르다.** 내가 매칭
결과의 품목명만 보고 별칭을 추측했다. 찍어서 확인한 값:

```
'나도컵 … 정수기컵 위생컵'        붙은 별칭 '포함'     (주방용 전기 액체가열기기류)
'[지앤지] 자동차 시트커버 …'      붙은 별칭 '자동차'   (게임기구류 및 오락기구류)
'자동차 킥매트 등받이 보호 시트'   붙은 별칭 '자동차'   (게임기구류 및 오락기구류)

별칭에 있나: 정수기 False · 카시트 False · 차양 True(세 줄에 없음)
```

**보고한 셋 중 둘은 존재하지 않는 별칭이었다.** 그것을 좁혔으면 오부착은 셋
그대로 남고 "좁혔는데 안 줄었다" 는 결론을 얻었을 것이다. §6 의 "판정 재료로
쓰는 숫자는 걸린 줄이 아니라 결과가 달라지는 줄로 센다" 와 같은 자리다 —
**원인도 마찬가지다. 보고 전에 한 줄 찍어 확인한다.**

---

## 13. [⑦-a-2] 괄호 안 쉼표로는 자르지 않는다 — 오부착 3 → 0

### 13-1. 원인을 찍어서 확인했다

```
'나도컵 … 정수기컵 위생컵'        붙은 별칭 '포함'     → 주방용 전기 액체가열기기류
'[지앤지] 자동차 시트커버 …'      붙은 별칭 '자동차'   → 게임기구류 및 오락기구류
'자동차 킥매트 등받이 보호 시트'   붙은 별칭 '자동차'   → 게임기구류 및 오락기구류

별칭에 있나  정수기 False · 카시트 False · 차양 True(세 줄에 없음)
```

내가 보고한 셋 중 둘은 **존재하지 않는 별칭**이었다. 좁혔으면 오부착은 셋
그대로 남고 "좁혔는데 안 줄었다" 는 결론을 얻었을 것이다.

### 13-2. 고친 곳 — 빌더

`A(B, C, D)` 는 "A 인데 B·C·D 형태도 포함한다" 는 뜻이다. B·C·D 는 **A 안에서만**
품목명이고 바깥으로 떼면 고시가 안 쓴 뜻이 된다 — **빼는 것이 R5, 두는 것이
R5 위반**이다. 목록을 손으로 고른 것이 아니라 고시의 문법을 지킨 것이다.

```
커피메이커? 머신(자동머신, 캡슐, 포함)             → '포함'
… 전동모터 장난감(드론, 자동차, 보트 등)            → '자동차'
전동형 롤스크린(전동식 커튼, 셔터, 그릴, 차양, …)   → '셔터'·'그릴'·'차양'
```

### 13-3. 잰 값 — 빌더를 **실제로 다시 돌려서**

```
별칭        509 → 466        (총괄 예측 475 와 다르다. 내 수를 쓴다)
새표본235   붙은 줄 50 → 32 · 대상 36 → 23 · 비대상 3 → 0 · 애매 11 → 9
도매꾹239   붙은 줄 73 → 52 · 대상  9 →  9 · 분류없음 64 → 43
예시 딸린 행 76 → 75          (33자짜리 롤스크린 한 줄이 길이 상한에 걸렸다)
```

**공짜가 아니다. 맞는 줄 8개를 잃었다** — 전수로 세어 적는다:

```
도매꾹239  무선주전자·커피포트·전기포트 4줄   ('전기포트' 가 괄호 안이었다)
           전동 눈썹제모기 3줄               ('제모기(전기·전동기, 레이저제모기 포함)')
           가정용 토스트기 1줄               ('전기토스터(팝업, 오븐 포함)')
```

⚠ **총괄 예측과 다른 것 하나.** "맞는 줄 13개를 잃는다" 였는데, 새표본235 에서
잃은 13줄은 **전부 틀린 품목이 붙어 있던 줄**이었다 — 블루투스 스피커→게임기구류
· 무선청소기→주방용 액체가열 · 전기그릴→전동형스크린류 · 놀이방매트→게임기구류.
`대상/비대상` 은 **상품**의 분류이지 붙은 품목이 맞았는지가 아니다. 실제로 잃은
정답은 도매꾹239 쪽 8줄이다.

### 13-4. 검사 — 셋 다 반대 방향으로 재 봤다

```
test_the_examples_come_from_the_notice_not_from_us   **괄호 밖 등장**으로 좁혔다
   (전 검사는 "정의 문단 안에 있는가" 만 봐서 '포함' 을 못 잡았다.
    동사 어미라 문단에 당연히 있다 — §6 "검사가 현재 출력에 맞춰 쓰면
    버그를 고정한다" 자리였다)
test_a_word_from_inside_parentheses_never_becomes_an_alias   새 회귀 고정물
test_the_three_false_attachments_stay_gone   그 세 줄이 다시 붙으면 실패

되살려 보면: 세 검사가 동시에 FAILED.
```

### 13-5. 오부착이 0 이어도 화면에 올리지 않는다

`화면연결: false` 와 `test_the_table_is_not_wired_to_the_screen` 은 그대로다.
**0 은 필요조건이지 충분조건이 아니다** — 지금 0 은 분류된 표본에서만 잰 값이고
도매꾹239 의 43줄은 분류 자체가 없다. 선행조건을 `docs/미완_목록.md §0` 에 적었고,
측정 스크립트도 "올려도 된다" 대신 **"다만 아직 올리지 않는다"** 로 끝난다 —
다음 사람이 `exit 0` 만 보고 켜지 않게.

```
검사      1557 → 1560 PASS (+3)
다섯 숫자  95/18/113/0/3 무변화 · 도매꾹239 168/239 무변화 · 판정 경로 무변화
실호출     0회 (원자료는 이미 받아 둔 fixture)
```

---

## 14. 잡음 — [⑦-a-3] · sooknise

총괄 판정: ⑦-a-2 닫음 · 머리 낱말은 **연다**. 표에 더 큰 것 하나(제외·조건
문장이 이름인 행)를 총괄이 찾아 줬다. 마감 9/15.

---

## 15. [⑦-a-3] 머리 낱말을 연다 · 제외 문장 22행을 래칫으로 잠근다

### 15-1. 머리 낱말 — 잃었던 8줄을 되찾고 3줄을 덤으로 얻었다

`A(B, C)` 에서 **A 가 품목명이고 괄호는 A 를 좁히는 말**이다. A 를 별칭으로
내는 것은 고시를 읽는 것이다 (R5). ⑦-a-2 가 꼬리를 뺀 것과 **같은 축의
반대쪽**이다 — 머리는 안전하고 꼬리는 안전하지 않다.

```
별칭        466 → 540        (총괄 예측 545)
새표본235   붙은 줄 32 → 38 · 대상 23 → 29 · 애매 9 · 비대상 0 유지
도매꾹239   붙은 줄 52 → 67 · 대상  9 →  9 · 분류없음 43 → 58 · 비대상 0 유지

새로 붙은 21줄 = 맞음 20 · 틀림 1
   전기그릴·전기오븐 6 → 주방용 전열기기류        (이번엔 제 품목으로)
   무선주전자·커피포트 4 · 전기밥솥 3 → 주방용 전기 액체가열기기류
   눈썹제모기 3 → 이?미용기기류 · 토스트기 1 → 주방용 전열기기류
   전기히터·온풍기 3 → 전열기구류
```

⑦-a-2 로 잃었던 8줄이 전부 돌아왔고 전기밥솥 3줄이 덤이다. 총괄 예측
(38/29 · 67/9/58)과 붙은 줄 수까지 같다.

`rstrip(")")` 도 뺐다. 합친 예시에 걸리면 **여는 괄호만 남은 문자열**이 되어
영원히 아무것도 못 맞힌다 — ⑦-a-2 직후 **57개**가 그랬다.

### 15-2. 틀린 한 줄 — 고치지 않고 적는다

```
'온풍기 Y36 윈드키스 미니 히터 난풍기' → 동식물용 전기기기류
머리 `히터` 가 `히터(관상어용·식물용, 동물부화·사육용 히터)` 에서 나왔다.
```

떼어낼 규칙 셋을 재 봤고 **셋 다 이 한 줄에 맞춰 만든 규칙**이었다:

```
머리가 괄호 안에 다시 나오면 뺀다  → 제모기·보일러·거품기까지 같이 빠진다
                                   (제모기는 되찾으려던 3줄이다)
괄호 첫 원소가 '…용' 이면 뺀다     → 표 전체에서 걸리는 것이 이 한 줄뿐
머리 3자 이상만 쓴다              → 79개 머리 중 2자는 '히터' 하나뿐
```

셋째가 제일 유혹적이고 제일 나쁘다 — **지금 출력에 맞춰 숫자를 고른 것**이다.
검사는 "붙으면 실패" 가 아니라 **"이 줄이 여기 붙는다는 사실"** 을 잠근다.
나중에 도매꾹239 를 전수분류해서 비대상으로 판명되면 게이트가 스스로 닫힌다.

### 15-3. 제외·조건 문장이 이름인 행 — 내 기준으로 **22행**

총괄이 표를 훑다 찾아 줬다. 내 정규식이 좁아 처음 13 으로 셌고, 기준을
넓혀 다시 쟀다. 쓴 기준: `^o ` 로 시작하거나 `…한다/…된다` 가 들어 있거나
`제외한다` 가 있는 것.

```
제외·조건 문장이 이름인 행        22 / 367   (적합등록 2 · 자기적합확인 20)
그중 examples 가 있어 **닿는** 행   2        (LIN · IMV11)
item 이 30자를 넘는 행             71 / 367
```

**"…적합성평가 대상에서 제외한다…" 에 등급이 붙어 있다.** 화면에 올라가
있었다면 제외 문장을 품목명 자리에 띄우면서 "대상입니다" 라고 말했을 것이다.

⚠ `test_a_definition_paragraph_is_not_used_as_the_item_name` 이 막는 것처럼
읽히지만 **못 막는다** — `"(정의)"`·`"대표적인 품목"` 두 문자열만 본다.
이름을 `…definition_marker_paragraph…` 로 좁히고 docstring 에 범위를 적었다.
**이름이 실제 범위보다 넓으면 다음 사람이 덮였다고 믿는다.**

고치는 것(파서의 이름 경계를 `4)`·`⑫`·`o ` 에서 다시 긋기)은 본선으로
미루고, **래칫 검사**로 22 를 상한에 잠갔다 — 늘면 실패, 줄면 기준도 내리라고
말한다. 가짜 가드를 두는 것보다 **결함을 눈에 보이게 세어 두는 것**이 낫다.

### 15-4. 검사 — 셋 다 반대 방향으로 쟀다

```
머리 낱말을 빼면   FAILED …head_word_of_a_parenthesised_example_is_an_alias
                  FAILED …known_false_attachment_stays_visible
rstrip 되살리면    FAILED …no_alias_is_left_with_an_unclosed_parenthesis
```

```
검사      1560 → 1565 PASS (+5)
다섯 숫자  95/18/113/0/3 무변화 · 도매꾹239 168/239 무변화 · 판정 경로 무변화
화면연결   false 그대로 · 선행조건 둘을 미완 §0 에 적었다
실호출     0회
```

---

## 16. [⑦-a-4] 22 가 아니라 23 · 정규식이 찾은 것에 맞춰 짜여 있었다

총괄이 놓친 행 하나를 찾아 줬다. 내가 직접 확인했다:

```
LIT22 [자기적합확인]  "조광기(디밍) o 직류전원만을 사용하여 동작하는
                      기자재는 적합성평가 대상에서 제외"
                      examples 없음 → 매칭에 안 닿는다
```

옛 정규식 `^o\s|(?:한다|된다)[\s.]|제외한다` 의 **셋 다 안 걸린다** — `o` 가
문자열 중간이라 `^o` 가 아니고, 끝이 `제외` 라서 `제외한다` 도 아니고,
`한다/된다` 뒤에 공백·마침표가 없다.

⚠⚠ **또 같은 패턴이다. 정규식이 찾은 것에 맞춰 짜여 있었다** — 이번 주에만
네 번째다(`'포함'` 별칭 · `…come_from_the_notice…` 검사 · 내 손실 13 오독 ·
이것). 넓히는 쪽이 옳고 안전하다: **품목명에는 `제외` 도 ` o ` 도 들어갈 수
없다.** 지금 출력에 맞춘 규칙이 아니라 이름의 성질에서 나온 규칙이다.

```
새 정규식   제외|(^|\s)o\s|(?:한다|된다)[\s.]
결과        22 → 23행 (적합등록 2 · 자기적합확인 21) · 닿는 2 그대로
오탐        0 — 넓혀서 새로 걸린 것은 LIT22 하나뿐이다(전수 확인)
```

래칫이 `==` 로 잠겨 있어 **정규식과 상수를 한 커밋에서 같이** 옮겼다.
따로 했으면 검사가 먼저 깨진다.

여기서 ⑦-a 를 닫는다.

```
검사      1565 PASS 무변화 (수만 바뀜) · 다섯 숫자 무변화 · 판정 경로 무변화
화면연결   false 그대로 · 실호출 0회
```

---

## 17. [⑦-b-1] 키 마스킹 — **호출 전에 만든다**

### 17-1. 새는 자리를 재고 만들었다 (추측 아님)

```
httpx.HTTPStatusError · ConnectError · ReadTimeout
    str · repr 에 URL 이 **안 들어간다**            → 새지 않는다
    다만 `exc.request.url` 을 우리가 찍으면 샌다
도매꾹 (키가 쿼리 `aid`)
    `request.url` 에 들어 있다                      → 우리가 찍으면 샌다
국표원 (키가 헤더 `AuthKey`)
    URL 에 아예 없다                                → 안 샌다
HostNotAllowedError
    **str · repr · .url 셋 다 샌다**                ← 유일하게 확인된 누출
```

⚠ 총괄 지시에 "국표원 키가 쿼리에 있어 같은 문제를 이미 안고 있다" 고
적혀 있었는데 **헤더다**(`kats_client.py:706`). 고칠 것은 `HostNotAllowedError`
하나였다.

### 17-2. 두 겹으로 막는다

```
① 아는 값을 지운다      `.env` 에서 읽어 등록 → 경로·쿼리·본문 어디든 정확히
② 키 같은 쿼리 이름     ①이 모르는 키(스크립트가 손으로 넣은 것)를 위한 그물
```

②만으로는 부족하다 — 식약처는 키가 **경로**에 있어 쿼리 이름이 없다.
①만으로도 부족하다 — 스크립트가 등록 안 한 키를 쓸 수 있다.

`OC=test`(law.go.kr)는 **가리지 않는다.** 공개 값이고, 가리면 근거 URL 을
셀러가 못 연다 (R2).

### 17-3. 모양으로는 못 가린다 — **자리**를 본다

커밋 전 검사를 처음 `[0-9a-f]{40}` 로 짰더니 **git 해시가 전부 걸렸다**
(CLAUDE.md · test_commit_refs 의 옛 해시). 식약처 키도 40자 16진수라 모양이
git 해시와 같다 — 구분할 수 없다.

그래서 `키이름 = "긴 리터럴"` **자리**를 본다. 키가 파일에 박히는 것은 언제나
그 모양이고 해시는 그 자리에 안 온다.

```
KATS_SERVICE_KEY = "abc123…"                     잡는다
api_key: 3f2a9c4d…                               잡는다
aid="ABCDEFGH12345678IJKLMNOP"                   잡는다
kats_service_key=os.getenv("KATS_SERVICE_KEY")   안 잡는다 (이름이지 값이 아니다)
옛 HEAD 1ebf65f08…                               안 잡는다 (git 해시)
```

⚠ 짜면서 두 번 좁았다 — `\b` 가 `KATS_SERVICE_KEY` 의 `_` 때문에 안 걸렸고,
상수 이름 규칙 `[A-Z][A-Z0-9]{5,}` 이 **대문자 키까지** 건너뛰었다(도매꾹 키가
그 모양이다). 둘 다 실측으로 잡았다.

### 17-4. 호스트는 넣되 **`scripts/` 전용**으로 못 박았다

R4 표 → `ALLOWED_HOSTS` 순서로 넣었고, 비고에 "배포본은 부르지 않는다" 를 적었다.
`ALLOWED_HOSTS` 에 있다는 것은 "나가도 되는 곳" 이지 "앱이 부른다" 가 아니다 —
그 구분을 `test_the_food_safety_host_is_never_called_from_the_app` 이 지킨다.
없으면 다음 사람이 목록에 있다는 이유로 어댑터를 패키지 안에 만든다.

### 17-5. 반대 방향

```
self.url 을 원문으로 되돌리면
   FAILED test_the_host_error_never_carries_the_key
   FAILED test_the_host_error_does_not_keep_a_raw_copy
```

```
검사      1565 → 1586 PASS (+21)
다섯 숫자  95/18/113/0/3 무변화 · 판정 경로 무변화
실호출     0회 — **아직 식약처를 한 번도 부르지 않았다** (마스킹이 먼저다)
```

---

## 18. [⑦-b-1 수리] 키 가드가 커밋 전엔 통과하고 커밋 후엔 실패했다

`0a184a2` 는 **빨간 검사를 싣고 나갔다.** 그때 보고한 1586 PASS 는 커밋 전
상태에서 잰 값이다.

원인: `test_no_key_literal_is_committed` 가 `git ls-files` 로 훑는다. 새로 만든
`tests/test_masking.py` 는 커밋 전에는 그 목록에 없어서 **자기 자신을 안 봤고**
통과했다. 커밋한 순간 목록에 들어와 자기 예시 5개를 잡았다.

⚠ **`git ls-files` 를 쓰는 검사는 커밋 후 한 번 더 돌린다.** 스스로를 검사
  대상으로 삼는 가드는 이 저장소에서 **열다섯 번째**다.

고친 것 넷. 전부 반대 방향을 재서 나왔다.

    ① 자기 예시   파일을 빼지 않고 **값 4개를 적어** 면제한다. 파일을 빼면
                   그 파일에 진짜 키를 붙여넣어도 안 걸린다.
    ② 이름 열거   `api_key|service_key|auth_key` 만 적어 뒀더니
                   **`FOOD_SAFETY_KEY = "..."` 이 안 걸렸다** — 지금 붙이려는
                   식약처 키가 정확히 그 이름이 될 자리다. `key` 로 끝나는 낱말
                   전부로 넓혔고, 넓히니 `key = _normalize_number` 가 걸려
                   (실측 1건) "숫자가 없으면 이름이다" 를 덧붙였다.
    ③ 경로 위치   식약처는 키가 **URL 경로**에 들어가 이름이 없다. 모양으로
                   잡으면 git 해시가 전부 걸리므로 **그 호스트의 그 자리**만
                   본다.
    ④ 호스트 검사가 "이름이 나오는가" 를 재고 있었다. 키를 **가리는** 코드가
       호스트 이름을 적자 걸렸다 — 부르는 것과 정반대인 코드다. 증상(이름)
       말고 원인(스킴 붙은 주소 조립)을 잰다.

커밋 `a5e95dd` · 검사 1588 → 1594 PASS.

---

## 19. R5-b — 갈리는 것은 고시가 정한다

CLAUDE.md §2, R5 와 R6 사이에 텍스트로 끼웠다. 세 갈래(① 고시가 적어 뒀다 →
상식과 어긋나도 고시가 이긴다 / ② 못 읽었다 → 모름(R3) / ③ 안 갈라 준다 →
정하되 정했다고 적는다)와, 근거에 **행정규칙일련번호 · 시행일 · 부속서 번호**를
같이 적으라는 규칙.

검사 `tests/test_claude_md.py` 3건. 일련번호 2곳을 다 지우면 실패한다(실행함).

커밋 `528ad20` · 검사 1594 → 1597 PASS.

---

## 20. [⑦-c] 유아용·아동용 섬유제품 — **정답률이 내려갔다**

R5-b 를 처음 적용한 자리다. 미검수 18줄을 전수 검수했다.

### 원문이 답을 줬다 — 갈림은 품목이 아니라 **사용 연령**이다

`scripts/probe_child_textile_scope.py` 로 어린이제품 계열 고시 9건의 별표
제목을 훑어 섬유 부속서 둘을 찾았다(실호출 · lawSearch 1 + lawService 9).

    유아용 섬유제품   36개월 미만          안전확인
      「안전확인대상 어린이제품의 안전기준」 부속서 1
      행정규칙일련번호 2100000271284 · 시행 20260101
      [가방류] 가방, 배낭, 핸드백, 지갑, 보온주머니
      [신생아용품] … 모기장

    아동용 섬유제품   36개월~만 13세       공급자적합성확인
      「개별안전기준이 있는 공급자적합성확인대상 어린이제품」 부속서 15
      행정규칙일련번호 2100000269554 · 시행 20251214
      [가방류] 가방, 배낭, 핸드백, 지갑, 보온주머니
      [기타 제품류] 쿠션류, 방석류, 모기장, 커튼 등

    가정용 섬유제품   만 14세 이상          안전기준준수
      「안전기준준수대상생활용품의 안전기준」 부속서 1
      행정규칙일련번호 2100000263200 · 시행 20260827
      적용범위 첫 문장: "유아용 및 아동용 섬유제품을 제외한 **만 14세 이상의**"

**가방도 모기장도 셋 다에 있다.** 고시가 가르는 축은 사용 연령이고, 가정용
부속서가 스스로 "유아용·아동용을 제외한" 이라고 적어 **어린이제품 쪽이
우선한다는 것도 고시가 말한다.**

보호자가 드는 물건(기저귀 가방·유모차 모기장)이 어린이제품인지는
「어린이제품 안전 특별법」 제2조제1호가 답한다 (MST 276551 · 시행 20251001):

> "어린이제품" 이란 만 13세 이하의 어린이가 사용하거나 **만 13세 이하의
> 어린이를 위하여 사용되는** 물품 또는 그 부분품이나 부속품을 말한다.

### 판정 18줄

    정답  9   의류 6(모자·스카프·장갑·타올·앞치마·토시) · 전기면도기 · 완구 ·
              의류 이외의 섬유제품 1(성인용 러닝 힙색)
    오답  7   LED마스크 1(용도어 겹침) + **연령 구간 6**
    애매  2   성인 침대 규격어(1~2인용·패밀리·슈퍼싱글·1인용 싱글)와
              '아기침대' 가 한 상품명에 같이 있다 — 규격별로 답이 다르다

부록 2줄(넉박스 커피 찌꺼기통 → 커피메이커)도 오답으로 적었다. 애매 분류 줄이라
② 분모 밖이고 다섯 숫자는 안 움직인다. **커피메이커는 안전인증(가장 높은 등급)
이라 붙으면 없는 의무를 크게 만든다** — 매처가 볼 자리로 남긴다.

### 다섯 숫자 — **내려갔다**

    GPT(기준)    ① 95 → 104   ② 18 → 0   ③ 113 → 104   ④ 0   ⑤ 3
    Claude(대조) ① 99 → 105   ② 14 → 0   ③ 113 → 105   ④ 0   ⑤ 1
    배치          ① 96 → 103   ② 15 → 0   ③ 111 → 103   ④ 0   ⑤ 2

③ 이 ① 과 같아졌다. **113(83.7%)은 더 이상 말할 수 있는 수가 아니다** — 그것은
미검수 18 을 다 맞았다고 친 수였고 실제로는 9만 정답이었다.

⚠ 우리가 틀린 방향이 **비싼 쪽**이다. 안전확인(번호 필요)인 것을
  안전기준준수(번호 불필요)라고 말하고 있었다 — "번호 없어도 정상" 이라고
  **잘못 안심시키는** 쪽이다 (CLAUDE.md §6).

### 안 한 것

**별칭은 안 옮겼다.** `ALIASES_IF_CHILD_MARKED` 를 고치는 것은 판정 경로이고
이 단계가 여는 범위가 아니다. 여는 조건 둘을 `docs/미완_목록.md` §2-a 에 적었다 —
(1) 어린이 표지어가 **연령 구간까지** 갈라야 한다(지금은 둘, 고시는 셋),
(2) 연 뒤 비대상 부착 0 이 유지되는지 두 표본에서 잰다.

---

## 21. [⑦-b-2] 저장 전 제거 — **남길 목록**으로 짰다 (도매꾹과 반대 방향)

총괄 지시: "`ADDR` · `TELNO` 는 제3자 사업자 정보다. 대조에 쓰지 않으므로 받는
즉시 버린다. 디스크에 닿기 전에 제거하고, 정제 뒤에도 전화·주소 패턴이 남으면
파일을 만들지 말고 던져라."

### 방향을 뒤집었다

본보기인 `domeggook_pii` 는 **지울 목록**이다. 그대로 따라 하면 식약처도
지울 목록이 되는데, 그러면 **API 가 필드를 늘릴 때 새 필드가 남는다.**

    domeggook_pii   쓰는 필드가 많고 구조가 깊다 → 지울 목록이 현실적
    mfds_pii        쓰는 필드가 **11개뿐** → 남길 목록이 가능하고 더 강하다

총괄이 준 지시가 이미 "남기는 필드: …" 형태였으므로 그대로 allowlist 로 짰다.
목록 밖은 전부 버려진다. CLAUDE.md §6 에 두 방향을 함께 적었다 — 다음에 만들
때 이것부터 따지라고.

    남김  PRDTNM · BSSHNM · RTRVLPRVNS · BRCDNO · PRDLST_CD · PRDLST_CD_NM ·
          RTRVL_GRDCD_NM · CRET_DTM · RTRVLDSUSE_SEQ · MNFDT · DISTBTMLMT
    버림  ADDR · TELNO · IMG_FILE_PATH · LCNS_NO · **그 밖 전부**

### 봉투를 저장하지 않는다

저쪽 봉투(`total_count` · `RESULT` …)를 그대로 두면 남길 목록이 무의미해진다.
행만 뽑아 **우리 그릇**에 담는다 — `{"출처", "행_경로", "남긴_필드", "행"}`.

⚠ **봉투 경로를 가정하지 않았다 (R5).** 실호출 0회 상태에서 만들었으므로
  `rows_of()` 가 구조를 훑어 "행처럼 생긴 dict 들의 목록" 을 찾고 **어느
  경로에서 찾았는지 함께 돌려준다.** ⑦-b-3 에서 그 값을 기록하고 주석을
  실측값으로 고친다.

### 잔존 검사 — 자리로 가린다, 모양으로 가리지 않는다

⚠⚠ **전화번호 정규식을 코드 필드에 걸면 안 된다.** 바코드는 13자리 숫자고
  일련번호도 숫자다 — 도매꾹에서 인증번호 `YU101649-22001` 이 대표번호 패턴에
  걸렸던 그 자리다. 그래서 잔존 검사는 **자유 텍스트 5개만** 본다
  (PRDTNM · BSSHNM · RTRVLPRVNS · PRDLST_CD_NM · RTRVL_GRDCD_NM).

주소 패턴에서 **'동' 을 뺐다.** '동결건조'·'작동 불량' 이 주소로 읽힌다.
'시/도 … 구/군' 꼴만 본다. 반대 방향 검사 4건으로 잠갔다.

### 저장 경로 잠금 — 지금은 대상 0개다

식약처를 부르는 스크립트가 **아직 없다.** 빈 목록을 보고 통과하는 검사를 여러 번
겪었으므로 `_EXPECTED_COLLECTORS = 0` 으로 적어 **늘어나는 순간 실패**하게 뒀다.
⑦-b-3 에서 1 이 되고, 그때 `write_sanitized` 외의 저장을 막는 검사가 실제로 문다.
임시 파일을 만들어 무는 것을 확인했다.

### 확인

    반대 방향  `sanitize_row` 를 `dict(row)` 로 바꾸면 13건 중 **10건 실패**
    검사       1595 → 1609 PASS (test_mfds_pii 14건)
    실호출     식약처 **0회** — 이 단계는 호출하지 않는다

---

## 22. [⑦-b-3 준비] 첫 실호출 — **총괄 메모의 방향이 뒤집혀 있었다**

마스킹(⑦-b-1)과 제거(⑦-b-2)가 끝났으므로 식약처를 **처음 불렀다(실호출 3회).**

### 마스킹이 동작한다

    부르는 주소: http://openapi.foodsafetykorea.go.kr/api/***/I0490/json/1/3

키가 경로에 있는데 `***` 로 나갔다. 등록을 **호출보다 먼저** 한다.

### 봉투는 왔는데 행이 없었다 — 시간 제한

    HTTP 200 · 길이 145
    .I0490  키=['total_count', 'RESULT']      ← row 가 없다
    total_count='0'
    RESULT={'CODE': 'ERROR-503',
            'MSG': '09시~19시에는 서비스가 제한됩니다. 이용에 참고바랍니다.'}

⚠⚠ **방향이 반대였다.** 총괄 메모는 "샘플 키는 09~19시 제한" 이었고 나도 그것을
  "그 시간대에 열린다" 로 읽었다. 원문은 **그 시간대에 막힌다** 이다.
  지금 17:15 KST 라 차단 구간이고, **19시 이후**에 열린다.

⚠⚠ **HTTP 200 이 성공이 아니다.** 상태 코드만 보면 "받았다" 가 되고 빈 목록이
  정상처럼 흐른다 — `resultCode` 를 안 봐서 kats·rra 양쪽에서 이미 겪은 자리다.
  수집기가 `RESULT.CODE` 를 보게 짰고, **모르는 코드는 성공이 아니다**(R3).

  반대 방향으로 확인했다. 코드 검사를 끄면 오류 응답이 이렇게 흐른다:

        1~3         0행  (누계 0/0)      ← 오류가 "0건 수집" 이 된다

  검사 3건이 실패한다.

⚠ 키는 **20자 16진수**다. 총괄이 말한 40-hex 와 다르다 - 시간 제한이 걸린 것을
  보면 샘플 키로 보인다. 정식 키인지는 확인 못 했다.

### 수집기

`scripts/sync_mfds_recalls.py`. 저장은 `mfds_pii.write_sanitized` 하나뿐이고,
페이징·재시도 3회·3초·마스킹된 주소 출력. 성공 코드는 `INFO-000` 으로 뒀는데
**아직 못 본 값이다(R5)** — 틀려도 조용히 저장되지 않고 멈춘다.

저장 경로 래칫이 설계대로 물었다: `_EXPECTED_COLLECTORS` 0 → 1 이 되며 실패했고,
옮기기 전에 안쪽 검사가 무는지 양방향으로 확인했다(`write_sanitized` 를 빼면
실패 · `.write_text(` 를 넣으면 실패).

### 남은 것

**19시 이후에 전량 수집을 돌린다.** 그때 (1) 실제 성공 코드 (2) 봉투의 행 경로
(3) 페이지 상한 (4) 총건수를 기록하고 주석의 "확인 전" 을 실측값으로 고친다.

검사 1609 → 1618 PASS · 실호출 식약처 **3회**

---

## 23. [⑦-c 수리 ①] CLAUDE.md 기준선 표가 옛 숫자를 들고 있었다

⑦-c 에서 다섯 숫자를 옮길 때 `baseline.py` · 기획서 · 제출문 · README · design ·
사이드카를 다 고쳤는데 **CLAUDE.md R7 의 기준선 표를 빠뜨렸다.**

    옛  ① 95 (70.4%)  ② 18  ③ 113 (83.7%)  ④ 0  ⑤ 3
    새  ① 104 (77.0%) ② 0   ③ 104 (77.0%)  ④ 0  ⑤ 3 · 애매 4 · 오답 8 · 미매칭 19

⚠⚠ **이 파일은 내가 상시 지침으로 읽는 파일이다.** ⑦-b 를 하는 내내 옛 숫자를
  읽고 있었다. 문서가 코드보다 앞서 나가는 이 저장소의 반복 결함인데, 이번에는
  **규칙 파일 자체**가 그랬다.

### 검사로 묶었다

§6 이 "같은 판단을 두 곳에 적지 마라. 소유자를 하나 정하고 나머지는 그것을
부른다" 라고 한다. 그런데 CLAUDE.md 는 **사람이 읽는 사본**이라 `baseline.py`
를 부를 수 없다 - 부를 수 없으면 검사로 묶는다.

`tests/test_claude_md.py::test_the_baseline_table_matches_the_code` 가 여덟 줄을
대조한다(분모 · ①②③ · 애매 · 오답 · 미매칭 · ④ · ⑤). 양방향 확인:

    표를 옛값으로 되돌리면   없는 줄: ['① 검수된 정답 104 (77.0%)']
    코드 쪽을 바꾸면         없는 줄: ['⑤ 애매 부착 4']

같이 고친 것 둘:
  · 대조군 문장의 "상한 둘 다 113" → "검수 뒤 105 vs 104 · 1건 차이"
  · R5-b 예시의 "70.4%" → "77.0%"

검사 1618 → 1620 PASS

---

## 24. [④ M-5] 카테고리 가이드 — 화면 하나 · 표 하나

[M-3] 커버리지 지도의 198 카테고리에 "이 카테고리에서 **관찰된** 등급·부속서·
소관" 을 붙여 표 하나로 만들고, 셀러가 읽는 정적 페이지 `/guide` 를 붙였다.

### 만든 것

    scripts/build_category_guide.py      LLM 0 · 네트워크 0 · 253초
    sourcing_guard/data/category_guide.tsv   배포본이 읽는 자료 (198행)
    sourcing_guard/category_guide.py     로더 + 요약(세어서 낸다)
    sourcing_guard/static/guide.html/.js 화면. 숫자·문구를 안 들고 있다
    app.css "[M-5] 카테고리 가이드" 절     15px 미만 0
    docs/M5_카테고리_가이드_2026-09-14.md   사람용 사본
    GET /guide · GET /api/v1/guide

    카테고리 198 · 제목 18,938 · 말할 수 있는 카테고리 151 · 침묵 47
    유해물질 부속서가 붙은 카테고리 12 · 소관 표기가 관찰된 카테고리 34

### [M-3] 과 같은 숫자를 말하게 묶었다

생성기가 **같은 배치 경로**를 돌리고, 붙음 수가 커버리지 지도와 다르면
**멈춘다.** 검사도 같은 대조를 한다 - 두 문서가 다른 숫자를 말하는 것이 이
저장소의 반복 결함이다. 198개 전부 일치했다.

### 건수를 붙인 이유

첫 판에서 `남성가방 → 화장품법(식품의약품안전처)` 이 나왔다. 100건 중 **1건**
(화장품 파우치)이 카테고리 라벨이 된 것이다 - 미완 §1-d 가 적어 둔 그 오탐이
카테고리 단위로 확대된 모양이다. 그래서 소관 표기에 **관찰 건수**를 붙였다:
`화장품법(식품의약품안전처)(1건)`. 안내 축은 판정이 아니므로 읽는 사람이
세기를 볼 수 있어야 한다.

### 가이드가 매처의 약한 자리를 드러낸다

    장갑      의류(97); **스마트폰(24)**      ← 터치장갑이 스마트폰으로
    수영      공기주입물놀이기구(45); 휴대폰(10); 스마트폰(10)
    모자      공기주입물놀이기구(8)

⚠ **고치지 않았다.** 별칭·매칭은 판정 경로이고 이 축이 여는 범위가 아니다.
  가이드의 값 하나가 이것이다 - 카테고리 단위로 보면 낱개로는 안 보이던
  과잉 부착이 눈에 띈다. 프리즈 뒤 별칭 작업의 재료로 쓴다.

### 안내 축이라 판정을 안 건드린다

    다섯 숫자   104 / 0 / 104 / 0 / 3   **무변화**
    신호등·등급·별칭  손대지 않음
    검사        `verifier`·`scorer`·`item_grades`·`batch` 가 가이드를 부르면 실패

### 화면이 숫자를 안 들고 있다

카테고리 수·말할 수 있는 수·침묵 수·제목 수·라벨 문구까지 전부
`/api/v1/guide` 가 그린다. 검사가 **HTML 에 그 수가 박혀 있으면 실패**시킨다
(랜딩 ④ 와 같은 규칙).

### 문구

'붙음 0' 을 "비대상" 으로 적지 않는다 - 화면에 "못 붙인 것" 이라고 쓰고,
검사가 `비대상입니다`·`대상 아님`·`해당 없음` 을 막는다 (R3 · §9).

검사 1620 → 1632 PASS (test_guide 12건 · 기존 가드 목록에 guide.html 추가)
